"""解封读取的冻结常量、判据、用途限制与释放清单（**先于任何结果落笔**）。

本文件是判据的机器可读正本，**逐项对齐冻结版**
`experiments/confirmation_protocol_v4.md`（tag ``prereg-2026-09-05-v4``；
sha256 在每次运行时现算并写进 ``run_meta.json`` 与报告页头）。
另参 `docs/解封读取_任务书_2026-09-05.md`、`CLAUDE.md`。
**冻结版与任务书冲突时一律以冻结版为准。**

判据（v4 §2；每一条都是「先写下再看结果」）
--------------------------------------------
* **H1（全池逐日 RankIC）**：区间估计，**不做二元 PASS**。报点估计、NW(5)
  95% CI、正折数（/31），两臂各一份。另报「实现收缩比 = 实测 / 开发折基数」
  与预注册主预测区间（FT ``[0.0088812, 0.0182250]``、ZS
  ``[0.0080529, 0.0170925]``）的**相交关系**；敏感性预测 ``[0.0149, 0.0166]``
  一并披露但不作兑现判据。不相交只否定「选择偏差估计正确」这一元主张，
  **不构成对信号的否定**。
* **H4 读取闸门**：该臂 H1 的 95% CI 下界 > 0。**只记录、不触发任何动作**
  （信号 #2 未过准入，H4 空置）。
* **H2（ΔADV，现代型流动性放大）**：三门同时成立才判「机制证据成立」——
  ① CI 下界 > 0（z = 2.2414，k=2 配置族 Bonferroni）；② 正折数 ≥ 18/31；
  ③ ``mean RankIC(top500) > 0``。**没有量级门**（v3 的 ``≥ +0.0026`` 已删，
  修订 3）。三门未全过**不得**写成「机制不存在」。
* **H2-era（年代二分）**：切点先验固定 **2013-01-01**（按 val_start），
  **只切一次**，估计交付、无门槛。交互项区间覆盖 0 时按措辞强制写
  「在本样本量下年代差异不可回答」，**不得**写「两个年代相同」。
* **H1b（扩充控制集张成）**：v4 §2.4 **裁定 C-1 已采纳**，以估计交付进入确认集
  （默认开启）。**只在 FT 臂交付**；主规格先验固定 **S-TH-ind**（不得按结果更换），
  S-T / S-H 作次规格并列报；口径沿 K6b（**NT=6**），**与 NT=5 读数不并列**。
  不设门槛、不做 PASS/FAIL、不进 H1 判定、不参与 α 分配、不作部署依据；
  **无 SESOI，故不作功效关判定**，也不得在本协议生效期内补写 SESOI 并据以判定。
* **H6（ICT P1）**：探索性估计交付。只跑 ``P1_bull`` / ``P1_bear``，只算
  Q1 与 Q2(b)，NW lag = 6。不进 H1–H4 判定、不作部署依据。SESOI = 10bp/6 日。
* **E（经济端）**：估计交付，**无 5% 阈值、无二元 PASS**。报毛年化 + NW(5) CI
  + 正折数、单边换手、``BE ± 95% CI``、成本网格 2/4/8/12/16/22bp 净年化。
  部署线只报不判：``C ≤ BE_dev × (1 − h) − 6bp``，**h = 0.42 主判**
  （FT ``C ≤ 7.28bp``、ZS ``C ≤ 3.51bp``），敏感性 h ∈ [0.399, 0.440]；
  旧线 ``×0.75``（h = 0.25）**v4 生效后作废，只作一行对照披露**。
  **C 未实测前只报线、不判 go。**

用途限制
--------
* 本次读取是**一次性、不可重开**的：31 折读后即永久视为已消耗。
* 读数不得回头修改任何预注册区间、门槛、构造参数或配置选择。
* B 层与功效不足的读数一律写「**在本样本量下该问题不可回答，按预先规则取 X**」，
  **不得**写「不可分」「两者相等」「已检验为无差异」（`CLAUDE.md` §二措辞强制）。
* fold44–45 是独立交付（干净窗），同口径同脚本、**独立输出目录，
  不并入 31 折合并统计**。

释放清单（v4 §3；**只**释放下列各项，两臂各一份）
------------------------------------------------
1. **E**：毛年化点估计、NW(5) CI、正折数（/31）、单边换手、成本网格净年化、
   BE 点估计与 95% CI；
2. **H1**：全池逐日 RankIC 点估计、NW(5) 95% CI、正折数（/31）、实现收缩比
   及其与主预测区间的相交关系；
3. **H2**：ΔADV 点估计、CI、正折数（/31）、``mean RankIC(top500)`` 的符号与点估计；
4. **H3**：本任务书不含（树基线超出 2026-09-01 计算授权）；
5. **H4**：空置（信号 #2 未过准入）；
6. **H1b**：仅在取 C-1 时释放（三规格的 alpha、保留率、CI、正折数）；
7. **H2-era**：两段各自的 ΔADV / RankIC(top500) / RankIC(full) 点估计与 NW(5)
   95% CI，以及年代交互项的点估计、95% CI 与逐折方向；
8. **H6**：Q1 与 Q2(b) 各两个标记的均值、NW(lag=6) CI、t、事件数、触发日数、
   同向折数 / 有效折数。

**运行后禁止查看或生成**：逐年曲线、ADV 五档、最优年代段、单股贡献、替代
universe、替代阈值；**任何臂间比较**（``FT − ZS`` 的 IC 差 / 收益差 / ΔADV 差
一律不算不看）；K8（ZS+FT）合成的任何追加比较；清单以外的一切量。
**报告里只有合并量 + 正折数 + 逐折符号计数，不得出现逐折 IC 表。**
"""
from __future__ import annotations

from pathlib import Path

# --------------------------------------------------------------- 协议出处

#: 本次读取所依据的冻结协议（sha256 运行时现算，不写死——文件一改哈希即变）。
PROTOCOL_PATH: Path = (Path(__file__).resolve().parents[2]
                       / "experiments" / "confirmation_protocol_v4.md")
PROTOCOL_TAG: str = "prereg-2026-09-05-v4"
#: v4.1 附录：**用户裁定抛弃 ZS 臂**（冻结之后、读取之前的设计变更，随结果一并披露）。
#: v4 正文一字不改（哈希已钉），变更以本附录记。
ADDENDUM_PATH: Path = (Path(__file__).resolve().parents[2]
                       / "experiments"
                       / "confirmation_protocol_v4.1_addendum_2026-09-05.md")
ZS_DROPPED_NOTE = (
    "ZS 臂按 v4.1 附录已抛弃，未读取。**这不是预注册内容**——预注册是 k=2，"
    "读取前按用户裁定放弃 ZS 臂；该裁定发生在看过开发折 FT > ZS 之后"
    "（七折 RankIC +7.9%、4/7；毛主动收益 10.48% vs 4.78%，`ledger:280`），"
    "按 `CLAUDE.md` §二属 B 层不可回答的比较。"
    "**z 维持 2.2414 不放宽**：门槛只能在数据前定，变更后不得朝宽松方向调整。"
    "ZS 封存分数不删除、不读取，哨兵与清单原样保留。"
)

# --------------------------------------------------------------- 折与臂

#: 确认集：折 05–35（2005–2020），31 折。
CONFIRM_FOLDS: tuple[int, ...] = tuple(range(5, 36))
#: 干净窗：fold44–45（2024-07 起，合计 1 年），**独立交付**。
CLEAN_WINDOW_FOLDS: tuple[int, ...] = (44, 45)
#: v4 §1 的 k=2 双臂，读取与序贯顺序先验固定为 FT → ZS（ledger:409 minimax-regret）。
#: 代码保留双臂能力（附录第 4 节：生效前 R-build 仍保留双臂能力）。
ARMS: tuple[str, ...] = ("ft", "zs")
#: **默认只读 FT**（v4.1 附录第 3 节）。ZS 分支代码保留但默认不运行。
ARMS_DEFAULT: tuple[str, ...] = ("ft",)

# --------------------------------------------------------------- 口径核对

#: `SEALED_MANIFEST.json` 的 `config` 必须逐项等于这五项，否则中止整个运行。
REQUIRED_SCORING_CONFIG: dict[str, object] = {
    "lookback": 90,
    "predict": 6,
    "sample_count": 5,
    "amp": "bf16",
    "batch_size": 128,
}

# --------------------------------------------------------------- 冻结构造

NT: int = 5                 # 五袖套（ledger:407），恰一个日历周
TOPN: int = 500             # 当日 lagged ADV20 前 500
EXIT_PCT: float = 0.30      # 跌出前 30% 才卖
MIN_NAMES: int = 50         # 当日最少名字数
PREDICT_WINDOW: int = 6     # 标签视野（交易日）
#: 逐折面板切片：验证窗前留 60 个交易日（ADV20 20 日 + ICT 回看 45 日余量）。
PANEL_BACK_SESSIONS: int = 60

# --------------------------------------------------------------- 推断常数

NW_LAG: int = 5
H6_NW_LAG: int = 6
Z95: float = 1.959963984540054
#: k=2 配置族 Bonferroni（`outputs/k12_confirmation_config_power.json`
#: `design.z_both_bonferroni`；同值写死在 `scripts/k12_confirmation_config_power.py:23`）。
Z_BONFERRONI_K2: float = 2.241402727604947
#: MDE80 = (z_{0.975} + z_{0.80}) × SE
POWER_MULT_80: float = 2.8016
#: H2 的一致性门（`ledger:283`，50 万次 31 折联合模拟）。**不可跨终点移植**。
H2_POSITIVE_FOLDS_GATE: int = 18
H2_TOTAL_FOLDS: int = 31

# --------------------------------------------------------------- H1 预注册预测

#: 收缩比的分母，**逐字取 v4 §3 第 2 项 / §2.3.1、§2.3.2**：
#: 「实现收缩比 = 实测 / 0.0207」（FT）、「实测 / 0.01919」（ZS）。
#: FT 的 0.0207 是 0.0206679591（折 36–42 逐日合并均值，`ledger:132`）的协议写法；
#: ZS 的 0.01919 是折 36–42 的**七折均值**（`ledger:162`）——口径不同这一事实必须随数披露。
H1_DEV_BASE: dict[str, float] = {"ft": 0.0207, "zs": 0.01919}
#: 披露用：实验 6 实际使用的 FT 基数（与协议写法差 0.0000320409，量级可忽略）。
H1_DEV_BASE_EXP6_FT: float = 0.0206679591
#: 主预测区间（v4 §2.3.1 / §2.3.2），单位与 RankIC 相同。
H1_MAIN_PREDICTION: dict[str, tuple[float, float]] = {
    "ft": (0.0088812, 0.0182250),
    "zs": (0.0080529, 0.0170925),
}
#: 敏感性预测（v4 §2.3.3，只披露、不作兑现判据）。
H1_SENSITIVITY_PREDICTION: tuple[float, float] = (0.0149, 0.0166)

# --------------------------------------------------------------- H2-era

#: 切点先验固定，按 **val_start** 二分，**只切一次**，脚本内不得有分年循环。
ERA_CUT: str = "2013-01-01"
ERA_EARLY: str = "2005-2012"
ERA_LATE: str = "2013-2020"
#: v4 §2.5.1 已机械算定的两段折号（由 walk_forward_folds 生成，本处只作对账用）。
ERA_EARLY_FOLDS: tuple[int, ...] = tuple(range(5, 21))    # fold05–20，16 折
ERA_LATE_FOLDS: tuple[int, ...] = tuple(range(21, 36))    # fold21–35，15 折
#: 必须随 H2-era 一起披露的边界事实（v4 §2.5.1）。
ERA_BOUNDARY_NOTE = (
    "fold20 的验证窗为 2012-07-03..2013-01-02，末端越过切点 2 个交易日；"
    "机械规则是**按验证窗起始日归段**（val_start < 2013-01-01 归早期段），"
    "不按验证窗结束日、不裁窗、不丢折。"
)
#: v4 §2.5.1 的 MDE80 披露常量（本次运行不重算，原样引用）。单位与各量相同。
H2_ERA_MDE80_INTERACTION: dict[str, dict[str, float]] = {
    "delta_adv": {"ft": 0.00730, "zs": 0.00789},
    "ic_top500": {"ft": 0.01923, "zs": 0.01955},
}
H2_ERA_MDE80_SEGMENT: dict[str, dict[str, dict[str, float]]] = {
    "delta_adv": {"ft": {"early": 0.00508, "late": 0.00524},
                  "zs": {"early": 0.00549, "late": 0.00567}},
    "ic_top500": {"ft": {"early": 0.01338, "late": 0.01382},
                  "zs": {"early": 0.01360, "late": 0.01404}},
}
#: H2 在真值 0.0026 处的功效（v4 §4 表：k=2 口径 `ledger:508`；单臂 z=1.96 口径 `ledger:412`）。
H2_POWER_AT_0026 = {"k2": {"ft": 0.400, "zs": 0.343}, "single_arm_z196": (0.45, 0.52)}

# --------------------------------------------------------------- E 端

COST_GRID_BP: tuple[int, ...] = (2, 4, 8, 12, 16, 22)
#: 开发折 BE（`ledger:429`，NT=5 口径）——部署线用的是**开发折** BE，不是确认折 BE。
BE_DEV_BP: dict[str, float] = {"ft": 22.9, "zs": 16.4}
#: 部署线（v4 §2.2，冻结版）：`go ⟺ C ≤ BE_dev × (1 − h) − 6bp`，**h = 0.42 主判**。
#: h = 实验 6 IC 端在 N_high=148 下三档 Harvey–Liu haircut 区间 [0.399, 0.440] 的中点
#: （Bonferroni 43.966% / Holm 43.810% / BHY 39.919%，`ledger:499`、`:512`②）。
#: 主判值：FT 22.9 → BE_disc 13.28 → **C ≤ 7.28bp**；ZS 16.4 → 9.51 → **C ≤ 3.51bp**。
DEPLOY_HAIRCUT_MAIN: float = 0.42
DEPLOY_HAIRCUT_RANGE: tuple[float, float] = (0.399, 0.440)
#: **旧线（h = 0.25，即 ×0.75）在 v4 生效后作废**，只允许作「旧线对照」一行披露，
#: 不得再作主判或默认值（v4 §2.2「D2」）。旧线：FT C ≤ 11.18bp、ZS C ≤ 6.30bp。
DEPLOY_HAIRCUT_LEGACY: float = 0.25
DEPLOY_RESERVE_BP: float = 6.0
#: 只作披露、不门控部署（v4 §2.2）：「实测成本高于 4bp 时，15.5 年历史无法在统计上
#: 证明净超额 > 0」。连带事实：ZS 的新部署线 3.51bp 已低于该值。
C_STOP_BP: float = 4.0

# --------------------------------------------------------------- H1b / H6

#: v4 §2.4 **裁定 C-1 已采纳**：H1b 以估计交付进入确认集 → 默认开启。
H1B_DEFAULT_ENABLED: bool = True
#: 主规格先验固定（自由参数最少 + 唯一做了行业中性 = B 层优先序 ②④），**不得按结果更换**。
H1B_PRIMARY_SPEC: str = "S-TH-ind"
H1B_SPECS: tuple[str, ...] = ("S-TH-ind", "S-T", "S-H")
#: **H1b 只在 FT 臂上交付**（v4 §2.4 第 4 条：ZS 臂没有可外推的开发折 SE；
#: 不得据确认结果回溯改臂或补做 ZS 臂）。
H1B_ARMS: tuple[str, ...] = ("ft",)
#: H1b 沿 K6b 冻结构造（NT=6），**与 NT=5 读数不并列比较**（`CLAUDE.md` §八）。
H1B_NT: int = 6
#: v4 §2.4 冻结期披露的 31 折 MDE80（NT=6 口径外推，**不作解封后的引用值**）。
H1B_MDE80_NT6: dict[str, float] = {"S-TH-ind": 4.8352, "S-T": 5.5186, "S-H": 5.6519}

# --------------------------------------------------------------- H3

#: 冻结树基线（XGBoost）的封存分数目录（相对 outputs 根）。
H3_TREE_DIR_TEMPLATE: str = "gbdt_strong_jkp_v2/xgboost/sealed/fold{f:02d}"
#: v4.1 附录连带变更：H3 主口径由 XGBoost×**ZS** 改为 XGBoost×**FT**。
H3_ARM: str = "ft"
#: 披露的功效（`[gpt-exp 7]`，`ledger:479`）：7 折 MDE80 与 31 折外推（× √(881/3900)）。
H3_MDE80_DEV7: float = 0.0329
H3_MDE80_31FOLD: float = 0.0156
#: 候选 SESOI（`ledger:479`；取值理由未核，但两值都低于 MDE80，B 层结论不敏感）。
H3_SESOI_CANDIDATES: tuple[float, float] = (0.0072, 0.0108)

H6_MARKERS: tuple[str, ...] = ("P1_bull", "P1_bear")
H6_QUESTIONS: tuple[str, ...] = ("Q1", "Q2b")
H6_SESOI_BP: float = 10.0

# --------------------------------------------------------------- 措辞

UNANSWERABLE = "在本样本量下该问题不可回答"

#: 任何 JSON 键都不得包含这些片段（FT−ZS 差值禁令的机器可核对版本）。
FORBIDDEN_KEY_FRAGMENTS: tuple[str, ...] = (
    "ft_minus_zs", "zs_minus_ft", "arm_diff", "arm_difference",
    "ft_vs_zs", "zs_vs_ft", "between_arm", "cross_arm",
)
