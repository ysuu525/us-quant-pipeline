- 2026-09-05 | tooling-review | **实验 9 启动前审查（尚未启动 GPU 打分）**。用户要求开始实验 9，并重申 ZS 已抛弃；exp9 原脚本全部为 FT，无 ZS 算力。12 个逻辑 r1 复用格的 scores 文件存在，metrics.scoring_config 全部为 bf16/b128/lb90/predict6、sample_count 与档位相符，验证窗与 fold36/39/42 定义一致：sc5 来自 eval_amp_lb90_fold{f}，sc10/20/40 来自实验 3 eval_e1_sc{sc}；12 个 r2 尚无 metrics。PowerShell 语法检查无错误且文件带 UTF-8 BOM；合成测试 7 passed（首次默认临时目录权限错误、第二次自定义临时目录父目录缺失，改用 F:/quant 下独立临时目录后通过；不涉及真实结果）。**启动阻塞**：实查实验 8 MLP 驱动 PID 34156、子计算 PID 32104 正在运行，后者由 nvidia-smi 确认为 CUDA 进程；另有实验 8 ridge 驱动 PID 15176，可能提取 GPU 表示。exp9 的 Wait-KnownGpuQueue 仅检查实验 3 锁，不能保证与这些任务串行。树基线 PID 33816 也出现在 CUDA 列表，但已核代码和配置为 CPU，不能据 CUDA 上下文判为 GPU 训练。未强停任何进程。
- 2026-09-05 | tooling-review | **实验 9 结果交付前待修（真实 r2 结果尚未读取）**：① pooled_with_folds 将相隔半年的三段日期直接拼接，再做 NW 和 circular stationary bootstrap，导致跨折接缝被当作连续交易日；应保留逐日推断单位、按折阻断 HAC 滞后及 bootstrap 块，按天数加权合并。② spearman_brown 仅对合并 sc5 给方差比与 k95，需补每个 fold×sc 及合并 sc 的分解与逐折 SB 预测对照；这些是 SB 假设下、秩相关尺度的诊断量，不能称为已识别的原始分数信号方差。③ 十分位价差来自 predict=6 标签，当前 unit='bp/day' 错标，应为 bp per label horizon，不把重叠多日标签收益称为日收益。④ GPU 耗时目前只有 QUEUE.DONE 队列墙钟，需另汇总新增 r2 的 runtime.scoring_seconds，重试开销与历史复用成本分别披露，缺失历史 runtime 记未核。⑤ r 与 IC 的 SE 比较应核验同日期支持集，并交付实测比值；不预断言“高几个量级”。现有分析脚本尚未运行，未生成实验 9 JSON/SVG 或任何科学结论。**工程说明**：脚本及测试按仓库 §九应交 Opus 修复，但当前可用子代理模型列表没有 Opus，未擅自换模型或修改代码。
- 2026-09-05 | method-before-results | **用户四条件下的方法修订方案（实施前写定）**：Codex 获准直接修自身脚本，不属于 Fable 的 §九 派活限制；截至本轮检查未见任何 eval_e9 目录，未读取任何 r2 分数、指标或日志数值。保持逐日相关的日数加权平均为描述性合并目标；HAC 仅在折内算，合并方差为 Σ(n_f/N)^2 SE_f^2，折内 stationary bootstrap（平均块长 10、10000 次、seed 20260904）独立抽样后按日数加权。**同时交付用户指定的两种方法**：A = 各折 NW(5) 后 inverse-variance 合并（w_f∝1/SE_f²，SE=√(1/Σ1/SE_f²)，正态 95% CI；零/非有限 SE 时标未定义，不加任意方差下限）；B = 两层 fold-cluster stationary block bootstrap（每次有放回抽 3 个折，各被抽中折独立在自身内部做 stationary 抽样，按抽中日数合并，percentile 95% CI）。A 是共同效应/精度加权目标，B 是重抽时期的诊断；两者与固定三折的日数加权目标分栏，不挑最窄 CI。只有 3 个折聚类，B 不能升级为确认性推断。所有折接缝处均无 HAC 乘积或 bootstrap 跨折块。增加年份缺口、边界零方差、SE 配对支持集、NT=5 单位与 GPU runtime 缺失的合成测试。
- 2026-09-05 | method-before-results | **其余交付修订写定**：每个 fold×sc 及三种合并目标均给 SB 条件性秩尺度方差比 r/(1-r)、达到 0.95 的 sc 倍数与 sc5→10/20/40 预测偏差；r≤0 或 r≥1 时显式报告边界状态，不中断其余表。IC 的 SE 比值只在同折同日期支持集计算，各 repeat 分开，报告比值与 log10(比值)，不预断言量级。价差标 **bp per NT=5 holding period (t+1 open to t+6 open)**；predict=6 不改，NT=5 不改标签、不将多日收益除以 5。GPU 报新增 r2 成功打分 runtime.scoring_seconds 总和与逐格值、历史 r1 可核成本/缺失格、队列墙钟及日志可识别失败尝试数；无记录的失败耗时明记未核，不把等待时间冒充 GPU 时间。分析入口加显式审阅确认参数，默认在任何真实数据读取前拒绝运行；修完交 diff，由用户或 Opus 审阅后才读真实结果。队列保持 4ee8a6d 显存封顶版本；不强杀任何 CUDA 进程，用户已授权 P3 共存。登记簿正文不改。
- 2026-09-05 | tooling-ready-for-review | **Codex 修订已完成，等待用户/Opus 审阅，尚未启动或读取 r2**。精确变更仅 `scripts/exp9_test_retest_reliability.py`、`tests/test_exp9_test_retest_reliability.py` 与本草稿。上述三种统计分栏、逐折/各 sc SB、NT=5 持仓期单位、配对日期 SE 比值、严格 JSON null、`--reviewed` 读取入口均已实现。bootstrap 按抽样次数向量化（O(draws) 工作内存），固定 seed=20260904；实现调整改变 RNG 消耗次序，发生在真实结果读取之前，后续按本版复现。成功 CUDA 打分时间来自每格 runtime；失败尝试仅从现存编号日志给**次数下界**，因重启可覆盖日志，次数与失败耗时不得冒充完整计量，失败耗时明确“未核”。**测试：51 passed**（exp9 20 项 + signal_eval + evaluate_fold_memfrac；全部合成或 mock，未进行 GPU 计算）；`git diff --check` 通过。队列脚本与 `4ee8a6d` **无差异**，sc≤20 的 0.70、sc40 的 0.85 封顶及原启动命令均保留。本次未修改 `experiments/ledger.md` 正文，也未发出 GPU 停止命令。
- 2026-09-05 | review-notes | **对初审措辞的纠正与方法来源**：初审“相隔半年”不准确，fold36 结束至 fold39 开始、fold39 结束至 fold42 开始均约有一年空档；predict=6 标签实际为 t+1 open→t+6 open 的 **NT=5 持仓收益**，不是“持有 6 日”。逆方差方法权重与共同效应前提依据 [Cochrane Handbook §10.3](https://www.cochrane.org/authors/handbooks-and-manuals/handbook/current/chapter-10)；折内 stationary bootstrap 的方法来源为 [Politis & Romano (1994), The Stationary Bootstrap](https://doi.org/10.1080/01621459.1994.10476870)。两层抽折再抽折内块是本次在用户条件下明确固定的实现选择，不声称原论文验证了仅 3 个折聚类的区间覆盖率。审阅重点：三种目标分栏（不可挑最窄 CI）、所有 NW/bootstrap 调用都限折内、单位不改收益数值、失败耗时披露而不估造。**放行条件**按用户本轮第 4 条“修完先审再读”；真实分析命令仅在审阅通过后使用 `.venv\Scripts\python.exe scripts\exp9_test_retest_reliability.py --reviewed`。
