# 登记簿草稿：实验 10（污染探针 P3 / P4）—— 2026-09-05

> 草稿。**不改 `experiments/ledger.md`**（append-only 正文），由用户核后追加。
> 格式照登记簿一行一条。

---

- 2026-09-05 | authorisation | **[fable-exp 10] 读取口径许可（用户书面原话，逐字）**：「干净窗（折 44–45）的**原始 OHLCV 面板可以读**；禁止的是那两折的**分数、标签与任何指标**。」**这不是解封读取授权**：折 05–35 与 44–45 的分数 / 标签 / 指标、以及 `outputs/` 下任何封存目录一律未读未写。执行侧把这条做成可执行断言（`scripts/exp10_contamination_probes.py::assert_read_ok / assert_write_ok / assert_columns_ok / install_label_tripwire`）：读黑名单 = 封存哨兵（`crsp_pipeline.sealed.assert_readable`）+ 路径 token `fold43/fold44/fold45/zeroshot_base` + 文件名 `labels.parquet/metrics.json/daily_ic.parquet/report.md/scores.parquet/SEALED_MANIFEST.json/SEALED/panel_raw.parquet` + `outputs/` 白名单（只允许折 36–42 的模型权重与 `outputs/exp10_*`）；写白名单 = **只允许 `outputs/exp10_*`**（协调会话 2026-09-05 补充 ①：封存审计会把封存目录里多出的文件判为违规、目录哈希已钉住）；列白名单 = `PERMNO/DlyCalDt/in_universe/DlyOpen/DlyHigh/DlyLow/DlyClose/DlyVol/DlyPrcVol`（无 `DlyRet`/`DlyDelFlg`/`DlyCap`）；`crsp_pipeline.labels` 的四个公开可调用对象（`LabelResult`/`compute_label`/`compute_labels`/`unfillable_report`）在 main 入口被换成会抛错的绊线，**一行 label 都不生成**。「原始 OHLCV 面板」在本实验解释为**模型输入侧**的 `panel_kronos_adj.parquet`（§5 复权后；与训练 `kronos_ft/train.py`、打分 `scripts/evaluate_fold.py:191` 同一份），两侧同口径；`panel_raw.parquet` 一次未读。9 项边界断言有测试 `tests/test_exp10_contamination_probes.py`（28 passed）。

- 2026-09-05 | design-disclosure | **[fable-exp 10] ZS 臂已按用户 2026-09-05 裁定整体抛弃，故 P3 只在 FT 臂上做**（`outputs/zeroshot_base` 一次未读、未跑）——**不是漏跑**。口径与 `experiments/confirmation_protocol_v4.1_addendum_2026-09-05.md` 一致（该文件本次只读不改）。代价是一个**新混淆项**必须随读数披露：FT 模型除预训练语料外还见过**自己的训练窗**，污染侧窗口紧邻其训练窗、干净侧远离，两侧 NLL 差因此同时含「预训练记忆」与「微调近因」两个来源，本设计**无法分开**。

- 2026-09-05 | diagnostic-result | **[fable-exp 10 / P3] 无标签 NLL membership gap（FT 臂，7 折 × 2 干净窗）**。判据**先写死**（抄进脚本 docstring）：**诊断，无阈值，不作判定**；只报方向与量级并列混淆项，不得宣称「有/没有污染」。口径：模型 = `outputs/fold{36..42}_lb90_s0_poolB_universe` 的 `tokenizer_final`+`predictor_final`；窗口 = `windows.build_scoring_index`（lookback=90、**predict=0，不取任何未来行**）+ `filter_index_by_universe`，两侧同一套；折边界由 `scripts/emit_folds.py --first 36 --last 45` 机械生成（未手写）；NLL 抄 `src/kronos_ft/train.py:99-115` 的 predictor 分支，改逐样本 reduction（逐窗均值的 batch 平均 == `DualHead.compute_loss` 的 batch 均值，有单测），并把 `Kronos.forward` 的默认采样 sibling-embedding 换成 `use_teacher_forcing=True, s1_targets=s0[:,1:]`（确定性精确分解）；bf16 autocast、batch 128、显存封顶 0.20。**读数**（逐窗 NLL 均值；两侧窗口数各约 18.5–19.0 万，交易日 123–127，每日约 1499 只）：污染侧 fold36 3.3045 / fold37 3.2225 / fold38 3.3984 / fold39 3.5240 / fold40 3.4687 / fold41 3.5174 / fold42 3.3126；干净侧 fold44 3.1856 / 3.2493 / 3.3355 / 3.4352 / 3.3821 / 3.3941 / 3.3818，fold45 3.2334 / 3.2493 / 3.3563 / 3.4659 / 3.4159 / 3.4584 / 3.4344。**gap（干净−污染，按日期聚类的 bootstrap 2000 次、95% 分位区间）**：fold44 −0.1189[−0.1316,−0.1050] / +0.0268[+0.0062,+0.0469] / −0.0628[−0.0753,−0.0505] / −0.0888[−0.1008,−0.0766] / −0.0866[−0.0963,−0.0768] / −0.1233[−0.1395,−0.1078] / +0.0692[+0.0573,+0.0814]；fold45 −0.0711 / +0.0269 / −0.0421 / −0.0582 / −0.0528 / −0.0591 / +0.1218（CI 见 JSON）。**方向：5/7 折的干净窗 NLL 更低（拟合更好），与「记忆 ⇒ 语料内更好拟合」的朴素预测方向相反；2/7（fold37、fold42）反向。量级 |gap| 0.027–0.123 nat = NLL 水平的 0.8%–3.7%。** 产物 `outputs/exp10_contamination_probes.json` + `outputs/exp10_p3_nll.svg`（纯 python 画）+ 运行日志 `outputs/exp10_p3_run.log`；GPU 合计 14.3 分钟（墙钟约 26 分钟，含 9 个窗口集构建与内存闸门等待）。两次独立运行（3 折 / 7 折）在共有格上**逐位相同**（确定性路径，无采样）。

- 2026-09-05 | diagnostic-result | **[fable-exp 10 / P3 混淆项，必须与上一条同时引用]** 两侧不是随机分配，且差在正好会驱动 NLL 的变量上。逐窗已实现波动（lookback 段日对数收益 SD）均值：污染侧 fold36 0.0331 / 37 0.0279 / 38 0.0223 / 39 0.0276 / 40 0.0289 / 41 0.0253 / 42 0.0221；干净侧 fold44 **0.0231**、fold45 **0.0270**。逐日截面收益 SD 均值：污染侧 0.0216–0.0280，干净侧 0.0238 / 0.0259。成交额中位数：污染侧 41.3–58.7 百万美元，干净侧 **59.0 / 75.5** 百万美元（log10 均值 7.72–7.83 vs **7.83 / 7.93**）。名字数 1676–1718 vs **1648 / 1657**。**描述性（n=7/14，无推断）**：gap 与「两侧已实现波动之差」的相关系数 = **+0.56**（14 个 模型×干净窗 格）、**+0.49**（只用 fold44 的 7 格）——gap 的**符号与量级随波动率差移动**，这正是不能把它读成因果的理由。另有结构性混淆：Kronos 输入端**逐窗 z-score + clip**（`third_party/kronos/model/kronos.py:544,546-547` 单序列路径、`:629-631` 批量路径）已经压掉水平差异，NLL 差只反映形状层面的差异。**限定**：fold44–45 恰是 Kronos 作者自己的测试期，**checkpoint 级的开发者选择不外生**，不得当成完全外生的样本。

- 2026-09-05 | tooling | **[fable-exp 10 / P3] 口径偏离两处，均已披露并量化**。(1) `DualHead.compute_loss`（`third_party/kronos/model/module.py:494-507`）是 batch 均值，要逐窗 NLL 故改 `reduction="none"`——所有窗口等长，逐窗均值的 batch 平均与原函数**完全相等**（`tests/test_exp10_contamination_probes.py::test_per_window_nll_matches_official_batch_mean`）。(2) `Kronos.forward` 默认 `use_teacher_forcing=False`，用 `torch.multinomial` 采样 s1 构造 s2 的 sibling embedding（`kronos.py:266-274`，训练时的蒙特卡洛替身，**带随机性**）；任务书要求确定性 NLL，故主口径改教师强制。**实测两者几乎同一个数**：21 个格各取前 2560 窗对比，均值差落在 −0.00041..+0.00315（NLL 水平约 3.3，即 ≤0.1%），逐窗相关系数最小 0.9999899。

- 2026-09-05 | diagnostic-result | **[fable-exp 10 / P4] adaptation dynamics（TSFMAudit 式），开发折 36/39/42，零 GPU 小时**。判据：**诊断，无阈值，不作判定**；**限定**——TSFMAudit 的读法是「见过的数据适应得更快 / 位移更小」，本项目**只有开发折可微调**（干净窗不许微调）、**没有对照臂**，因此**只能报曲线形状，不能下污染结论**。实现选 **(ii) 新脚本包一层**：`src/kronos_ft/train.py` **一个字节未改**（sha256 `fdb7c6fd…`，`git status` 无该文件；`tests/test_kronos_train_infer.py` 11 passed，含 `test_resume_bitwise_identical` / `test_fast_path_bitwise_identical`）——理由是 `train_stage`（train.py:220-224）**每个 epoch 本来就把完整 state_dict 存成 `{stage}_epoch{NNN}.pt`**、逐轮 train/inner loss 也已在 `{stage}_summary.json` 里，P4 的两条曲线是**纯事后计算**；而方案 (i) 即使默认关闭也要改动被封存清单 `code_sha256` 钉住的文件。θ_0 = `NeoQuasar/Kronos-Tokenizer-base` / `NeoQuasar/Kronos-small`（本地缓存，离线加载）。**读数**（相对位移 ‖θ_e−θ_0‖₂/‖θ_0‖₂，总体）：predictor 首轮 0.0346 / 0.0415 / 0.0411（fold36/39/42），末轮 0.131（6 轮，早停）/ 0.2248（30 轮）/ 0.2270（30 轮）；tokenizer 首轮 0.0414 / 0.0425 / 0.0423，末轮 0.2889（8 轮）/ 0.4885 / 0.4881。**曲线形状三折一致**：位移逐轮单调上升并饱和（前 5 轮吃掉约一半，后 10 轮几乎不动），**首轮位移三折几乎相同**（predictor 0.035–0.042）。**分层（最优轮）**：predictor 的 dep_layer 位移最大（0.061 / 0.374 / 0.383）、head 次之（0.044 / 0.290 / 0.289）、transformer 中等（0.031 / 0.199 / 0.201）、time_emb 与 norm 最小（0.013–0.130）；tokenizer 的 head 位移最大（0.315 / 0.625 / 0.624）。**逐轮内层 loss**：fold36 的 predictor 在第 1 轮就到最优（3.1025）而后单调变差（早停于第 6 轮），fold39 最优在第 27 轮（3.4438）、fold42 在第 25 轮（3.3507）——**三折的 loss 形状不一致，位移形状一致**。产物 `outputs/exp10_p4_adaptation.json` + `outputs/exp10_p4_adaptation.svg`。重跑队列（**多半不需要跑**）`scripts/run_exp10_p4_queue.ps1`：折号写死 36/39/42、超参与原运行逐字段一致、互斥锁 + 失败重试 + 分尝试命名日志 + `Start-Process` 分离重定向 + 等 `exp9_reliability.queue.lock` / `exp3_samplecount.queue.lock` 释放 + 内存闸门；产物写 `outputs/exp10_p4_fold*`，**不写进原折目录**（否则 `_completed()` 会跳过重训）。

- 2026-09-05 | tooling | **[fable-exp 10 / P4] 已知分歧（不自行裁决，只披露）**：`scripts/emit_folds.py` 今天机械生成的边界与**原 FT 训练实际用的边界**不完全相同——fold36 训练窗 emit 给 `2017-07-03..2020-06-24`，原运行是 `..2020-06-22`（`scripts/run_supp_folds.ps1` + `ledger.md:98`）；fold42 emit 给 `2020-07-06..2023-06-22`、验证窗 `2023-07-03..2024-01-02`，原运行训练窗起点是 `2020-07-01`（`scripts/run_probe_recent.ps1` + `ledger.md:71`，那批 .ps1 注明边界取自 `walk_forward_folds(oos_start=2024-01-01)`）。**P4 重跑队列用原运行的边界**（要求是「与原 FT 训练一字不改」）；**P3 用 emit_folds 的边界**（要求是「折窗口不得手写」，且 P3 不与任何既有产物并列比较）。两处口径不同是刻意的，已写进各自脚本的注释。

- 2026-09-05 | fact-check | **[fable-exp 10 / P1、P2 零成本正面论证，纯文本核对，行号已逐条核实]** (a) **输入逐窗 z-score + clip**：官方单序列路径 `third_party/kronos/model/kronos.py:544`（`x_mean, x_std = np.mean(x,axis=0), np.std(x,axis=0)`）、`:546-547`（除以 `x_std+1e-5`、`np.clip(±clip)`）；**本项目实际走的批量路径**在同文件 `:629-631`；`KronosPredictor` 默认 `clip=5`（`:484`）。本项目复刻：打分侧 `src/kronos_ft/infer.py:158-159`，训练侧 `src/kronos_ft/dataset.py:121-125`。**审稿报告引用的「544-547」核实为正确**（545 为空行）。(b) **时间戳只含 minute/hour/weekday/day/month、无年份**：官方 `calc_time_stamps` 在 `kronos.py:472-479`（五个字段）、`KronosPredictor.time_cols` 在 `:492`；消费端 `third_party/kronos/model/module.py:536-566` 的 `TemporalEmbedding` 只建 minute(60)/hour(24)/weekday(7)/day(32)/month(13) 五个 embedding，**没有 year 字段可用**；本项目 `src/kronos_ft/infer.py:31`（`TIME_COLS`）与 `:36-45`（`_stamp_matrix`）、`src/kronos_ft/dataset.py:41-49`。**审稿报告引用的「infer.py:35-43」略有偏移，精确行号是 31 与 36-45**。(c) **无 ticker embedding**：`Kronos.__init__`（`kronos.py:212-222`）只有 `embedding`(HierarchicalEmbedding)/`time_emb`/`transformer`/`norm`/`dep_layer`/`head`；`HierarchicalEmbedding`（`module.py:400-417`）只嵌 s1/s2 两个**量化后 OHLCV token id**；`grep -rni "ticker|symbol|instrument|asset_id|series_id" third_party/kronos/model/` **零命中**。⇒ 污染只能走「形状识别」，不能「查表」。**容量算术**：Kronos-small predictor **实测 24,741,376 参数**（本地权重逐参数计数，与 README 表的 24.7M 一致）、tokenizer-base 3,958,042，合计 28,699,418。预训练语料「45 交易所、>120 亿根 K 线、截至 2024-06」系**二手核实**（`docs/审稿_席位报告_2026-09-03/R2_domain.md` 与 `R3_perspective.md` 记其核对 arXiv 2508.02739；**本会话未联网核对论文原文，标「二手」**；`third_party/kronos/README.md` **只给参数量与 context length，不含 bar 数与语料截止日**，标「未核」）。CRSP 日线全样本 bar 数：本项目 `panel_kronos_adj.parquet` **实测 49,875,213 行**（2000-01-03..2025-12-31，parquet 元数据），即 4.99×10⁷。⇒ CRSP 日线占语料 ≈ **0.42%**（用 1.2×10¹⁰ 作分母；语料是「>12B」的下界，故 0.42% 是**上界**），审稿人的「≲0.5%」成立；records/param ≈ **486**。

- 2026-09-05 | deviation | **[fable-exp 10] 执行侧偏离三条，全部披露**。① **P3 只跑折 36/39/42 还是全 7 折**：任务书规则是「干净侧每个 FT 模型都跑 44 与 45；若总墙钟估算超过 2 小时则缩到 36/39/42」。首次 7 折运行时本机被别的会话的作业（gbdt 封存队列、实验 8 MLP、岭探针）把系统提交内存长期顶在 42 GB 以上，内存闸门连续阻塞，估算超 2 小时 → 按规则缩到 3 折并完成；随后其它作业释放、实测单格仅约 45–80 秒，重估远低于 2 小时 → **按任务书的首选口径重跑全 7 折**（本条是资源判断，不是判据或超参；诊断无阈值，无选择偏差；3 折与 7 折在共有格上逐位相同）。② **内存闸门加了 10 分钟上限**：原规则「超过 42 GB 等 60 秒再查」无上限，本机长期顶在 42 GB 附近会把实验拖成数小时，而本进程自身峰值只有几 GB；超时即继续并在日志明说（`outputs/exp10_p3_run.log` 可查）。③ **显存封顶取 0.20、batch=128**（任务书的保守档）：任务书的判定条件是「有无命令行含 `evaluate_fold.py` 的 python」，实测没有；但当时 GPU 上有实验 8 的两个 CUDA 作业在跑（利用率 76–80%），故取保守档而非 0.50/256。

- 2026-09-05 | incident | **[fable-exp 10] 自己犯的错：误执行了 P4 重跑队列**。为给新写的 `scripts/run_exp10_p4_queue.ps1` 做语法检查，错用了 `powershell -Command "& { . '<script>' }"`——**点源即执行**，队列真的启动了 `kronos_ft.train`（fold36，写 `outputs/exp10_p4_fold36_lb90_s0_poolB_universe`）。约 3 分钟后发现并强杀（当时进程刚建 CUDA 上下文、未进重负载；GPU 与并存的实验 8 / gbdt 作业均未受影响，无驱动崩溃）。**善后**：删除该目录（当时只有 `tokenizer_epoch001.pt` 与 `tokenizer_resume_state.pt`）、删除队列锁与日志目录；`experiments/ledger.md` **未被追加任何 `train |` 行**（`kronos_ft.train.run` 只在两阶段全部完成后才写登记簿），已核。**教训**：`.ps1` 的语法检查只能用 `[System.Management.Automation.PSParser]::Tokenize()`（不执行），**绝不能用点源或 `-Command "& { . }"`**。事后已用 Tokenize 复检，parse OK。
