# 登记簿草稿（实验 8，待主会话审后合并进 experiments/ledger.md）

> 本文件不是登记簿正本。格式照 `experiments/ledger.md`，一行一条。

- 2026-09-05 | diagnostic-result | **[fable-exp 8] 读出方案对比表补齐到七折 36–42（估计交付，不用于选读出）。** 脚本 `scripts/exp8_run_mlp_folds.py`（驱动）+ `scripts/exp8_mlp_one_fold.py`（只把 `train_rank_head.py` 的 `append_ledger` 重定向到 `outputs/exp8_run_ledger_lines.log`，**超参与算法一字未改**）+ `scripts/ridge_probe_folds.py`（新增 `--daily-ic-dir`，默认关闭）+ `scripts/exp8_readout_table.py`（汇总）；测试 `tests/test_exp8_readout_table.py`（6 项，合成数据）。产物：`outputs/exp8_readout_table.json` / `.md`、`outputs/exp8_mlp_fold{36,37,38,39,41,42}/eval_exp8_mlp_fold*/`、`outputs/exp8_ridge_probe_7fold.json`、`outputs/exp8_ridge_daily_ic/`、`outputs/exp8_folds_36_42.json`。**MLP 排序头七折（本次新跑五折 + fold40 复用同参数同窗口的既有产物 `outputs/rankhead_fold40/eval_rh_zeroshot_fold40`）**：逐折 36→42 `-0.00537 / +0.00838 / -0.00092 / +0.01163 / +0.00753 / +0.00527 / +0.02252`，**折等权均值 +0.00701、5/7 折为正**；合并 881 日均值 +0.00695、NW(5) SE 0.00583、**t = +1.19**。`scoring_config` 七折一致：`{"amp":"bf16","batch_size":256,"sample_count":0,"lookback":90,"predict":6,"mode":"rank_head","loss":"ic","seed":0,"backbone":"outputs/zeroshot_base","frozen_backbone":true}`，`--inner-months 6`（脚本默认，与线性探针的内层窗同为训练窗尾部 6 个月、purge 7 日）。**线性探针（岭回归、mean 池化、内层选 alpha）七折**：逐折 `+0.00723 / -0.00257 / +0.00670 / +0.02368 / +0.02773 / +0.00003 / -0.00036`，**折等权均值 +0.00892、5/7 折为正（其中 fold41 仅 +0.00003，数值为正但实质为零，按实质计为 4/7）**；合并 881 日均值 +0.00895、NW(5) SE 0.00614、**t = +1.46**；**事后最优（oracle）七折均值 +0.01513（不可用于判定）**，与诚实口径之差 **+0.00621**，与 ledger:164 在五折上量出的选参偏差 +0.0070 同量级。折 36–40 用既有表示缓存重算，与 `outputs/ridge_probe.json` **逐位一致**（alpha / rank_ic / t / oracle 全部 <1e-12），确认新增的 `--daily-ic-dir` 未改动任何读数；折 41–42 为新抽表示。**表中生成式 ZS/FT、树基线、MLP fold40 单折四类行一律抄自登记簿（ledger:162 / 164 / 186 / 141），未重算。** 口径与限定：① 本条只补口径、**不用于选读出**——读出方案属 B 层，按先验定（CLAUDE.md §二），本实验不重开该选择；② 折 36–42 已被消耗数十次决策读数，读数为方向性证据、非无偏估计（CLAUDE.md §四）；③ MLP 七折均值高度依赖 fold42 单折（+0.02252，t=2.85），去掉该折后余六折等权 +0.00442；④ MLP 行与探针行的逐日 IC 口径略异——MLP 用 `signal_eval.daily_rank_ic`（pandas 秩、不丢日），探针用 `argsort` 秩且丢弃当日不足 50 只的交易日；⑤ MLP 用 last 池化（`extract_representations` 默认，与 fold40 原冻结一致），探针用 mean 池化（与 ledger:164 一致），两者池化方式不同，属各自冻结口径，不得据此比池化。**未核**：登记簿未给出 ZS/FT 各行的天数、NW t 与合并日口径读数，表中一律记「未核」，本次也未重算（实验 8 条文明令 ZS/FT/树基线行只抄不算）；fold40 那次运行的 `--inner-months` 未落进 `metrics.json`，本次按脚本默认值 6 认定（与线性探针内层窗口径一致），属推断而非核实。

- 2026-09-05 | deviation | **[fable-exp 8] 发现并披露：`scripts/emit_folds.py` 的折表与本项目既有全部产物所用的折表不是同一张，实验 8 因此未用 emit_folds 的窗口。** `emit_folds.py:34` 硬写 `walk_forward_folds(cal, "2000-01-03", ...)`，产出 **45 折**；而 `scripts/gbdt_baseline.py::FOLDS`、`scripts/ridge_probe_folds.py::FOLDS`、以及折 36–42 全部 Kronos eval 产物的 `metrics.json.val_window`，对应的是 `walk_forward_folds(cal, "2000-01-01", ...)` 的 **46 折**表（HANDOFF.md:154 记的「42 折，oos_start=2024-01-01，fold01 训 2000-01-03..2002-12-20」也是这一张）。两表在折 36–42 上的差：fold36 val `2020-07-01..2020-12-31` vs `2020-07-06..2020-12-31`；fold38 `2021-07-01..` vs `2021-07-06..`；fold40 `2022-07-01..` vs `2022-07-05..`；fold37/39 val 末尾各多 1–2 日；**fold42 val 由 `..2023-12-29` 变成 `..2024-01-02`，即越过 2024-01 的封存线**；fold41 两表相同。实验 8 的目的是把表格补成**同口径**，被比较的 ledger:162/164/186 四行全部落在「2000-01-01」那张表上，且 emit_folds 的 fold42 会触碰封存窗，故本次按「与被比较对象同口径」取 2000-01-01 表，并由 `scripts/exp8_run_mlp_folds.py` 用 `walk_forward_folds` **机械生成**、与 `ridge_probe_folds.FOLDS` **逐位断言一致**后才允许运行（未手写任何窗口）；两张表同时落盘在 `outputs/exp8_folds_36_42.json` 存证。**此分歧影响面超出实验 8**：`scripts/h1b_power_31fold.py:60` 与 `scripts/signal2_devfold_readout.py:86` 已改用 `2000-01-03`（注释写「同 scripts/emit_folds.py」），而 ledger:528 记录的封存树基线折 05–35 亦由 emit_folds 产出（其 fold35 val 止于 2020-07-02，与 2000-01-01 表下 fold36 val 起点 2020-07-01 **重叠 2 个交易日**；在 2000-01-01 表下 fold35 止于 2020-06-30，无重叠）。**须由用户/主会话裁定以哪张表为准**；在裁定前，实验 8 的 MLP 行与探针行只可与同表产物比较。
