# 确认集协议 v4.1 附录：零样本（ZS）臂整体抛弃 —— 2026-09-05

> v4 正文（`experiments/confirmation_protocol_v4.md`，sha256 `c3c823e2…`，第二章 OTS 区块 965508，
> tag `prereg-2026-09-05-v4`）**一字不改**。本附录记录一项**冻结之后、读取之前**的设计变更，
> 随确认结果一并披露。本附录本身将进入第二章 b 的清单与时间戳。

## 1. 变更的性质（必须原样披露）

1. **时点**：发生在 v4 冻结（2026-09-04 18:37:21 UTC 前已存在）之后、任何折 05–35 / fold44–45
   数据读取之前。树基线封存计算仍在进行，Kronos 封存分数一份未读。
2. **来源**：用户裁定，原话「ZS直接抛弃，没有任何继续的必要，实验也不要管它节省时间」，
   系在会话 quant-3f 书面顶回之后重申（顶回要点见 `experiments/ledger.md` 2026-09-05 DECISION 行）。
   **本裁定经另一会话转达**；解封负责会话将在读取闸门向用户本人再确认一句，确认后本附录生效。
3. **性质**：这是**看过开发折 FT > ZS 之后的设计变更**（七折 RankIC +7.9%、4/7；毛主动收益
   10.48% vs 4.78%，`ledger.md:280`），按 CLAUDE.md §二属 B 层不可回答的比较。它**不是预注册内容**，
   不得在论文中写成「预注册只含 FT」；须写成「预注册 k=2，读取前按用户裁定放弃 ZS 臂」。
4. **不可逆性**：ZS 的封存分数（`outputs/zeroshot_base/eval_sealed_zs_fold*`）**不删除、不读取**，
   哨兵与清单原样保留；若日后要读，须另行预注册并盖章。

## 2. 对 v4 各条款的后果（条文原样保留，标注「已抛弃」）

| v4 条款 | 后果 |
|---|---|
| §1 k=2 双臂、z=2.2414 | k 实际为 1。**z 维持 2.2414，不放宽到 1.96**：门槛只能在数据前定，变更后不得朝宽松方向调整。 |
| §2.2 ZS 部署线 3.51bp | 已抛弃。FT 线 7.28bp（h=0.42）不变。 |
| §2.3.2 ZS 平行收缩预测 [0.0080529, 0.0170925] | 已抛弃，不评估。FT 主预测 [0.0088812, 0.0182250] 不变。 |
| §2.5 H2-ZS、§2.5.1 H2-era-ZS | 已抛弃，不读取。FT 侧三门与 MDE 披露不变。 |
| §2.6 H3 主口径 XGBoost×**ZS**（ρ=+0.339，MDE80 0.0259） | **连带变更**：主口径改为 XGBoost×**FT**（[gpt-exp 7]：ρ=−0.208，MDE80 0.0329，31 折外推约 0.0156）。仍为 B 层估计交付，层级不变；MDE 更大须披露。 |
| §2.7 H4-ZS 序贯位 | 已抛弃。H4 本就空置。 |
| §3 释放清单中的 ZS 各量 | 不产出。**FT−ZS 差值本就禁止，现无从计算。** |
| §7 第 11 条同刻读取 | 读取范围改为 FT 臂 + 树基线 + fold44–45（FT）。 |

## 3. 对读取工具链（R-build / R-read）的要求

- `scripts/unseal_read_confirm.py` 增加 `--arms`，默认 `ft`；ZS 分支代码保留但默认不运行。
- H3 配对 ΔIC 以 Kronos **FT** 封存分数与树基线 XGBoost 封存分数在同一观测集上配对。
- 报告页头写明：「ZS 臂按 v4.1 附录已抛弃，未读取」。

## 4. 生效条件

用户本人在本会话读取闸门确认后生效；在此之前，R-build 仍保留双臂能力，R-read 不运行。
