# 本仓库的强制规则

> 每个会话开始前必读。违反下列任何一条，产出的读数一律作废。
> 这些不是风格偏好，是**已经踩过并造成实际损失**的坑（每条都附实例）。

---

## 一、绝不允许的四类错误

这四类在 2026-08-29..31 三天内**全部真实发生过**，且**无一例外都朝"让结果更好看"的方向错**。
有偏差的实现天然偏向好看的一侧，因为写代码时脑子里想的是"让它工作"。

### 1. 前视：用未来信息构造特征

**规则**：`label`（未来 6 日收益）**只能作为目标或结果**，绝不能进入任何特征。
构造任何"当日截面统计量"时，必须问一句：**这个量在信号日收盘时能不能算出来？**

**实例**（2026-08-31）：横截面上下文检验里把"当日截面离散度"写成
`groupby(signal_date)["label"].transform("std")` —— 当天不可能知道未来收益的离散度。
**前视贡献了约 37% 的表观增益，并把结论从"有价值"翻成"边际"。**

**自查**：提交任何新分析前，`grep -n label <脚本>`，逐条确认每处 label 都是目标或结果。

### 2. 在评估窗上选超参 / 选方案

**规则**：任何超参（正则强度、早停轮次、阈值、特征子集）**必须在与评估窗分离的
数据上选**。跨折滚动优于折内切分。报"事后最优"时必须显式标注"不可用于判定"。

**实例甲**（2026-08-31）：岭探针在外层验证窗上挑 alpha，fold40 得 +0.0278（看似超过
生成式 +0.0265）；改内层选参后五折均值 **+0.0126**，明显劣于生成式。
**选参偏差实测 +0.0070 —— 比信号本身（约 +0.02）的三分之一还大。**

**实例乙**（同日，**修完实例甲当天重犯**）：横截面探针按"每折后 30%"切分，
alpha 在该段选、又在该段评估。

### 3. 判据只有量级门槛、没有一致性门槛

**规则**：任何判据必须**同时**含"量级"与"折数同向"两项。小样本下均值会被离群折劫持。

**实例**（2026-08-31）：横截面探针初版只写"均值提升 ≥ +0.003"，结果在
**2/7 折为正**的情况下仍判"有价值"——均值完全由两个离群折拉起。

### 4. 执行时点比可行时点早

**规则**：规范 §4 冻结口径为 **t 日收盘算分 → t+1 开盘成交**。任何净值模拟
必须让新建仓位从 t+1 开盘起计收益（当日只吃 Open→Close 段）。

**实例**（2026-08-30）：净值脚本让持仓从 t 日收盘就开始吃收益，早了整整一个交易日。

---

## 二、判据先于结果

**任何实验，判读标准必须在看到结果之前写下来**（写进 `experiments/ledger.md` 或脚本
docstring）。看到结果再定标准 = 在噪声里挑最高点。

如果确实需要修订判据，**必须显式记录"修订发生在看到结果之后"**，并说明修订理由
是先验的方法学问题（而非"新判据给了我想要的答案"）。


### 判据还必须先过功效关（2026-08-31 新增，代价是 K7a 的一次空跑）

**规则**：预注册任何判据时，必须**同时**写下三样东西：

1. **SESOI**（最小科学相关效应）—— 小于它就不值得据以决策的效应量；
2. **MDE**（最小可检出效应）—— 用**考虑序列相关后**的长期方差算（HAC / block bootstrap），
   且**用正确的推断单位**（有配对就用逐日配对，折数只作一致性指标，不作标准误的基本单位）；
3. **目标功效**。

**若 MDE > SESOI，该实验不得设计成确认性检验**。可以降级为探索性诊断，
但**不得据其未通过门槛得出否定结论**。

**实例**（2026-08-31，K7a）：预注册门槛 ΔIC >= +0.007，实测均值 +0.00516。
初版按折级（n=4）推断报 t=+0.34、MDE≈0.034，判『不可判读』——**推断单位就错了**：
零样本与微调是同日同名字集打的分，手上有 504 个逐日配对观测。
改逐日配对后 NW(lag=5) SE = 0.01214、t = +0.43、95%CI [-0.0205, +0.0302]。
CI 同时覆盖 0 与门槛 +0.007，『功效不足』这才正式成立。
而 MDE ≈ 0.024 始终是门槛的 3.4 倍——**这个实验从设计上就分辨不了预设效应量，本不该启动为确认性检验**。

**配套自查**：写完判据后问两句——
「我的标准误用的是哪个推断单位？有没有配对结构被浪费掉？」
「MDE 是多少？它比我的门槛大还是小？」


### 比较分层：A 层做推断，B 层按先验决定（2026-09-01 新增）

**这不是一条原则，是一次实测的结果。** 本项目的臂间比较在现有样本量上
**结构性地不可分辨**——不是门槛定高了，是问题在这个数据规模上不可回答。

逐日配对 NW 稳健标准误（实测）：

| 比较类型 | 配对 SE | 99% 淘汰门槛 | 门槛 / 信号本身 |
|---|---|---|---|
| 相关臂（同模型，换训练池 / 换 lookback） | 0.0036–0.0053 | 0.009–0.014 | 0.45–0.66× |
| 正交臂（零样本 vs 微调，秩相关 −0.045） | 0.0088–0.0121 | 0.023–0.031 | **1.10–1.51×** |
| 臂间毛日收益差（7 折） | 3.24bp | 8.35bp | **2.01×** |

**正交臂的淘汰门槛超过信号本身（0.0207），永远不可能触发**；相关臂要差出信号的
45% 才触发。样本量的账：分辨 50% 的相对臂间差需 **20 年**（比确认集 15.5 年还长）、
30% 需 55 年、20% 需 124 年。

唯一触发过的一次是训练池 A/B（配对差 +0.00855 = 信号的 41%，SE 0.0036）——
那是一次大幅度的设计改动。项目真正想调的那些（lookback 档、训练轮次、
sample_count、读出微调、多档合成）差异都在 5–30% 区间，**全部在触发线以下**。

**规则**：每个比较在预注册时先按上一节算出 MDE，然后二选一。

#### A 层（MDE ≤ SESOI）——做推断

典型：信号 vs 零；年代 vs 年代；结构关系（η、σ_cs 趋势、ΔADV 的跨年代 DiD）。
保留确认性地位，用常规 95% 门槛。**统计资本全部花在这一层。**

本项目已验证有功效的 A 层读数：全池 RankIC vs 零（t=3.63）；信号 vs 树基线
（差一个量级）；ΔADV 跨年代 DiD（95%CI [+0.0034,+0.0191]）；η=0.902
（95%CI [0.813,0.960]）；σ_cs 年趋势（95%CI [−0.630,−0.198]）。

#### B 层（MDE > SESOI）——按先验决定，不装成推断

典型：变体 A vs 变体 B。**不做 CI 淘汰。** 改用写死的优先序：

1. 官方 / 上游默认；
2. 自由参数最少；
3. 算力 / 成本最低；
4. 对机制变化最稳健。

选择记为**披露的设计假设**，不是发现。

**先验固定不是退而求其次，是严格更优**：先验固定规则的后悔上界 = MDE；
而本项目实测的「看数据选」偏差是 **+0.0070**（岭探针内层选参 vs 外层选参）。
在本项目的信噪比下，数据驱动选择引入的偏差**大于**它能买到的改进。

#### 措辞强制

B 层的结果**不得**记作「不可分」或「两者相等」。必须写成：

> **在本样本量下该问题不可回答，按预先规则取 X。**

「不可分」会被后续读者当成 null result 引用（"我们检验过，两者一样"），
也会诱发「再跑一次看能不能分开」——两者都是错的。

#### 例外：零参数且理论单调的改动（2026-09-03 新增，用户裁定）

**规则**：若一项改动 (a) 不引入任何在本项目数据上估计的参数，且 (b) 理论上对目标单调有益
（典型：纯方差削减，如加大采样路径数、多 seed 平均），则**可按理论采纳、只需登记、不需臂间检验**。
理由：这类改动的期望效应方向由理论给定，而本项目样本量分辨不了 5–30% 的效应，
用一致性门槛去卡它等于系统性放弃真实的小改进。

**首个适用**：实盘打分 `sample_count=20`（2026-08-29 五读数 +4.9%、4/5 折）。
**封存确认队列仍为 5，不追溯**；两口径读数不得并列比较（§八）。

**不适用**：任何有自由参数的改动（lookback、轮数、退出线、合成权重、特征集）。

#### B 层平局（2026-09-03 新增，用户裁定）

优先序 ①②③④ 内部出现平局（如 ZS vs FT：①②③ 指向 ZS、④ 指向 FT）时，
按**预注册的效用函数（minimax-regret）**破局，把双方的最大机会损失算出来取小者，
并在登记簿注明「效用函数系看过 X 后修订」。HANDOFF §11.2 第 1 条（k=2）追认为本条款首次适用。

---

## 三、样本量纪律

**七折（3.3 年）不足以证明任何东西。** 本项目实测：策略夏普 0.70 时，
3.3 年样本的 t 天花板是 1.3；要 t>2 需约 8 年，t>2.8 需约 15.5 年。

**少于七折的结论一律不算数。** 本项目已被小样本推翻**四次**：
- "lb200 近代领先 41%"（3 折）→ 7 折反转为落后 56%
- "年化 +2.96%、回撤 4.7%"（3 折）→ 7 折为 +1.45%、回撤 13%
- "合成较最优臂 +11%"（3 折）→ 7 折仅 +3.1%，方案否决
- "微调提升 77%"（1 折）→ 7 折仅 +7.9%，且 4/7 折

---

## 四、评估数据是消耗品

- **折 01–04、36–42 已被消耗**（数十次决策读数），其读数为**方向性证据，非无偏估计**。
- **折 05–35（31 折，2005–2020）完全未碰**——动用前必须先冻结设计，且需用户裁定。
- **封存窗 2024-07 起一眼未看**，最终判读前不得触碰。

详见 `docs/开放问题与决策困境_2026-08-31.md` 困境 A。

---

## 五、指标：RankIC 已降级为诊断

实测 RankIC 与实际净收益只有 `corr≈0.87` 的松散关系，逐折可差 2.7 倍
（fold40 IC 更高却只赚 fold37 的 1/2.7；fold41 IC 为正但毛价差为零）。
机制：**收益 ≈ IC × 当日截面离散度**，而 IC 不含离散度；且 IC 看全截面、组合只吃两端。

- **主指标**：冻结组合构造下的**净夏普**（可直接换算显著性：`t ≈ SR × √年数`）
- **辅指标**：配对的逐日净收益差 + 块自助置信区间（`scripts/compare_evaldirs_money.py`）
- **诊断**：RankIC（便宜、能抓粗错，但**不作决策依据**）

**组合构造必须先冻结再比臂**，否则是拿同一批数据同时优化"信号"与"怎么用信号"。

---

## 六、登记簿

`experiments/ledger.md` 是 **append-only** 的（预注册 §3 要求，随 git 走）。
所有决定、读数、偏离、以及**自己犯的错**都要写进去，不得删改历史条目。
这份记录是本项目能够量化并披露选择偏差的唯一依据。

---

## 七、机器与工程约束

| 约束 | 说明 |
|---|---|
| **提交上限 55.9GB 常年吃紧** | 已多次 OOM。脚本必须**列裁剪 + 日期下推过滤 + 分块**，禁止整表读 `panel_raw`（4980 万行） |
| **单张 16GB 显卡** | 评估用 `--amp bf16`（1.7×）；批量 >128 无益甚至有害（显存溢出触发换页，反向劣化 4×） |
| **训练 venv 隔离** | 不要往 `.venv` 装 lightgbm/wrds 等（有装包破坏环境的前科）；树模型用 `.venv-gbdt` |
| **分离启动的后台进程会夭折** | 用受管的后台任务；批处理脚本一律加**互斥锁 + 失败重试** |
| **并发 CUDA 进程** | 可以共存，但**强杀其中一个会引发驱动层崩溃**（已发生一次） |
| **`git add -A` 会卷进别人的在制品** | 多会话并行时用精确路径 add |

---

## 八、口径必须可机器核对

`metrics.json` 里有 `scoring_config`（精度/批量/采样路径数/lookback/predict）。
**不同口径的读数不得并列比较**；比较前先核对该字段。

---

## 九、AI 助手的工作约定（2026-09-03，用户指示）

- **派 agent 一律用 opus**（web 调研、写代码、写文档都是）。不要为省钱降级到更小的模型；调研质量比 token 重要。
- **主会话（Fable）不亲自写代码**，只做分析、决策与指挥（用户 09-03 晚：「你太贵了，你只负责分析当司令」）。脚本、模块、测试一律派 opus 写，主会话只写任务书、审结果、登记、提交。
  用户 09-04 补充：目的是**省主会话 token**。所以：长代码 / 联网检索 / 文献综述 / 长文档改写 → opus；**短文档、登记条目、任务书主会话自己写**，不必为了形式再派一层。
- 调研任务书必须写清：背景（自足）、要证伪的具体假设、交付格式、「有证据 / 推断」分开标注、不得编数字（没核到就写「未核」）。
- 后台监控只在终态（完成 / 失败 / 卡住）通知，不按进度逐条通知。
- 用户是 ML 方向学生、金融/量化萌新：策略层面的判断由助手凭文献与联网调研严谨给出，**用户直觉与证据冲突时要直接指出**，不得顺着用户走；每条建议标「文献证据 / 本项目实测 / 推断」。
