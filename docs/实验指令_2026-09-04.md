# 实验指令（给执行者逐条跑；2026-09-04）

> **执行者**：GPT/Codex（用户另一窗口）。**指挥**：主会话。**裁定**：用户。
> 每条实验都是「判据先写、再跑、只报读数」。执行者**不得**据任何读数改设计，只交回数字。
> 队列已跑完，GPU/CPU 空闲；系统提交内存约 31 GB / 45 GB。

---

## 0. 总则（每条实验开始前重读一遍）

**先读** `CLAUDE.md`（全文）与 `HANDOFF.md` §12。

**铁律（违反任一条，产出作废）**

1. **封存**：不读、不生成折 05–35 与 2024-01 起任何窗口的 labels / 指标 / 分数。`outputs/*/eval_sealed_*`、`outputs/sealed_confirm/` 一律不碰。新脚本里不得出现字符串 `sealed_confirm`、`eval_sealed`、`unseal`。**任何代码改动后必须跑** `.venv\Scripts\python.exe -m pytest tests\test_sealed_mode.py -q` **且全绿**。
2. **只读已消耗的开发折**：36–42（年代诊断可加 01–04）。读 Kronos 分数与标签一律经 `src/signals/kronos_adapter.py` 的 `scores_path(fold, arm)` / `labels_path(fold, arm)`（内置折号白名单 {1–4, 36–42} 与 `assert_readable`）。ZS 目录没有 labels，标签统一从 FT 目录读。
3. **判据先写后跑**：本文每条实验的「判据 / 用途限制」在跑之前**原样抄进脚本 docstring**。
4. **不调参、不扫描**：每条实验只跑本文写死的那一个配置。「顺手多跑几个看看」= 在开发折上搜索 = 作废。
5. **不改冻结文件**：`scripts/compare_arms_money.py`、`src/portfolio/construction.py::frozen_long_only_returns` 的行为、`scripts/k9_*`、`k11_*`、`k13_*`、`k14_*`。允许**只加带默认值的 CLI 参数**，且默认输出必须与现有产物逐位一致（先对拍再用）。
6. **登记**：每条实验的读数写成一行登记簿草稿，**追加**到 `experiments/ledger_drafts_gpt_2026-09-04.md`（格式照 `experiments/ledger.md` 的一行一条）。**不要直接改 `experiments/ledger.md`**，那是 append-only 正文，由用户核后追加。
7. **git**：只用精确路径 `git add`；禁止 `git add -A`；不 push；提交信息前缀 `[gpt-exp N]`。`outputs/` 在 .gitignore 里，产物不进 git。
8. **内存**：读 `panel_raw.parquet`（4980 万行）必须列裁剪 + `filters` 日期下推 + 分块（一次 ≤ 2 年）。跑前查 `powershell -NoProfile -Command "(Get-Counter '\Memory\Committed Bytes').CounterSamples[0].CookedValue/1GB"`，> 40 不跑。GPU 实验串行，绝不并发（并发会把单折从 17 分钟拖到 3 小时）。
9. **编码**：文件 UTF-8；运行 python 时设 `PYTHONIOENCODING=utf-8`；`.ps1` 存 UTF-8 带 BOM。
10. **新脚本放 `scripts/`，命名 `expN_*.py`**；新测试放 `tests/test_expN_*.py`，只用合成数据。

**可复用的接口（已验证）**

| 用途 | 位置 | 备注 |
|---|---|---|
| Kronos 分数/标签路径 | `signals.kronos_adapter.scores_path / labels_path / eval_dir` | FT 折 36–42：`outputs/fold{f}_lb90_s0_poolB_universe/eval_amp_lb90_fold{f}`；ZS：`outputs/zeroshot_base/eval_zeroshot_fold{f}` |
| 价格/成交额加载（同口径） | `scripts/compare_arms_money.py::load_prices()` → `(ret, oc, adv)` 三个 `dict[date, dict[PERMNO, float]]` | 2020-06-01..2024-01-05；`oc = Close/|Open|−1`；`adv = 20 日 DlyPrcVol 均值 shift(1)`。用 `importlib` 加载该文件（见 `scripts/nt5_baseline_readout.py::_load_cam`） |
| 冻结构造 | `portfolio.construction.scores_frame_to_by_day(df)`；`frozen_long_only_returns(by_day, ret, oc, adv, topn=500, cost_bp, exit_pct=0.30, nt=5, min_names=50)` → `DataFrame(index=date, r, turn, n_names)` | **现行冻结 NT=5**。已在真实折上与原脚本逐位对拍通过 |
| NW t | `crsp_pipeline.signal_eval.newey_west_tstat(series, lags=5)` → `{mean, se, t, n}` | |
| 逐日 RankIC | 参考 `scripts/e4_metric_alignment_power.py::daily_metrics` 的写法 | |
| K6b 8 因子构造 | `scripts/k6b_spanning.py`（只读，import 其函数或抄其逻辑；不改该文件） | 因子：rev1、rev5、mom_skip(t−24..t−5)、mom_12_1、Amihud、成交量冲击、特质波动、hlrange |
| 封存守卫 | `crsp_pipeline.sealed.assert_readable(path)` | 读任何 parquet 前调用 |

**交回格式**（每条实验一段）：命令 / 退出码 / 关键数字表 / 登记簿草稿行原文 / 异常与未做。

---

## 实验 0：CRSP `DlyOpen` 缺失率与口径核查（CPU，约 30 分钟）

**背景**：用户文献综述核实 CRSP 文档称 `DlyOpen` = 开盘后**首笔成交**（非竞价印价），且仅 60% 非缺失、覆盖 77% 证券。本项目的建仓价 = `t+1` 的 `DlyOpen`（`src/crsp_pipeline/labels.py:118-128`），若在 top500 里缺失率高，标签口径与小试基准都要复核。

**判据**：诊断，无阈值。**用途限制**：只报数，不改任何口径。

**步骤**：
1. 新建 `scripts/exp0_dlyopen_coverage.py`。分块（每 2 年）读 `F:\quant\processed\crsp_ciz_2026-08-24_20260825T130601Z\panel_raw.parquet`，列 `[PERMNO, DlyCalDt, DlyOpen, DlyClose, DlyPrcVol]`，`filters` 按 `DlyCalDt` 下推，范围 2000-01-01..2025-12-31。
2. 每块内按 PERMNO 算 `adv20 = DlyPrcVol.rolling(20, min_periods=10).mean().shift(1)`；每日取 adv20 前 500 名 = top500 股票日。
3. 对 top500 股票日按**年**统计：N；`DlyOpen` 为 NaN 的比例；`DlyOpen < 0` 的比例（CRSP 负价 = 买卖中价约定，单独计）；`DlyOpen == 0` 的比例；`|DlyOpen| == |前一日 DlyClose|` 的比例（疑似无成交承接）；`|DlyOpen/前收 − 1| > 20%` 的比例（疑似异常）。另对**全池**同样统计一份。
4. 对折 36–42 的 FT `labels.parquet`（列 `status`）统计每折 `ok / unfillable / invalid` 的占比（这是缺失开盘价在标签层的实际后果）。
5. 输出 `outputs/exp0_dlyopen_coverage.json` + 控制台年度表。

**交回**：两张表（top500 年度、全池年度）+ 折 36–42 的 status 占比 + 草稿行。**若 top500 的 NaN 或 ≤0 比例任何一年 > 2%，在交回时单独标红。**

---

## 实验 1：信号#2 准入读数（CPU，约 1 小时）

**前置（缺一不跑，交回说明）**：
- `experiments/signal2_prereg_v2.md` 存在；
- `experiments/ledger.md` 末尾存在登记它的一行（含该文件 sha256 与 `spec_hash 896b0de2…`）。

**判据**（来自 v2 §4，机械执行）：准入 ⟺ `IC₂(top500, 折 36–42 拼接均值) > 0` ∧ `折内均值 > 0 的折数 ≥ 4/7` ∧ `与 FT 的逐日秩相关均值 ρ < 0.30`。不准入的措辞：「在本样本量下该问题不可回答，按预先规则不为信号#2 分配确认集资源」。**用途限制**：只产出读数；不计算任何合成分数的收益或 IC；不改 `LOOKBACK/MIN_VALID/MAX_SPAN_DAYS`。

**步骤**：
1. 先 `--dry-run`：`set PYTHONIOENCODING=utf-8 && .venv\Scripts\python.exe scripts\signal2_devfold_readout.py --dry-run`，退出码 0 才继续。
2. 真实运行：`.venv\Scripts\python.exe scripts\signal2_devfold_readout.py --folds 36-42 --arm both --include-early --out outputs\signal2_devfold_readout.json`。
3. 从 JSON 读出并列表：折 36–42 的 top500 IC 均值与 NW t、全池 IC、逐折均值与符号、ρ(FT)、ρ(ZS)、自身 top-decile 日换手、`score_overnight` 的同一套（诊断）；折 01–04 同一套（年代诊断，**不进判据**）。
4. 按判据机械写出「准入 / 不准入」。

**交回**：上表 + 机械结论 + 草稿行。**不要**加任何「如果放宽到…就能过」之类的话。

---

## 实验 2：分数加权 vs 等权（**盲功效关先行**；CPU，约半天）

**背景**：用户想知道「按分数加权」是否优于等权。文献在纯多个股层面无直接实证。本项目规则：构造变体比较先算 MDE，MDE > SESOI 则 B 层（按先验取等权 = 上游默认 1/N），**不看均值**。K9（2026-08-31）用过同一套盲法。

**判据（先写死）**：
- 配对量 `d_t = r_rank,t − r_equal,t`（FT，折 36–42，NT=5，cost=0，逐日毛超额差）。
- **SESOI = 0.50 bp/日**（= NT=5 毛 12.68%/年 的 10% ÷ 252）。
- **盲阶段**只算 `SE_NW5(d)`、`MDE80 = 2.8016 × SE`、两构造的年单边换手比。**均值必须遮住**（写入 `outputs/exp2_weighting.blind.json` 并在控制台打印 `***`）。
- `MDE80 > SESOI` → 结论「在本样本量下该问题不可回答，按先验取等权（上游默认 1/N）」，**不揭盲，实验结束**。
- `MDE80 ≤ SESOI` → 用 `--unblind` 揭盲：报 mean、NW t、stationary bootstrap 95% CI（块长 10、10000 次、seed 20260904）、7 折中 d 均值 > 0 的折数、换手比。**通过判据**：`t ≥ 1.96` ∧ `≥ 5/7 折同向` ∧ `换手增幅 ≤ 20%` → 记「设计修订候选，待用户裁定」；否则「不采用」。
- **用途限制**：无论结果如何，**执行者不得改动任何冻结构造**；只交回。

**步骤**：
1. 在 `src/portfolio/construction.py` **新增**函数 `frozen_long_only_returns_weighted(..., weighting="equal")`：
   - `weighting="equal"` 时**逐位复现** `frozen_long_only_returns`（写测试 `tests/test_exp2_weighting.py`，合成数据 3 个种子 `np.array_equal`）；
   - `weighting="rank"`：每个套袖选定的 k 只名字内，权重 ∝ 名次分（分数最高 = k，最低 = 1，归一化和为 1；**只做这一种线性名次权重，不扫其它**）；
   - 收益 = 权重加权（新买入名字当日吃 `oc`，其余吃 `ret`，与原函数同）；换手 `turn` 改为该套袖权重变动的 `|Δw|` 之和 / 2（等权时退化为原定义，测试里断言）；成本公式不变 `2×bp×turn/NT`。
   - docstring 写明「本函数仅供实验 2，不是冻结构造」。
2. 新建 `scripts/exp2_weighting_blind_gate.py`：加载同 `nt5_baseline_readout.py`；两构造各跑 7 折；算 `d_t`；默认盲模式；`--unblind` 揭盲。判据原样抄进 docstring。
3. 跑盲模式 → 按判据决定是否揭盲。

**交回**：SE、MDE80、SESOI、是否揭盲；若揭盲则 mean/t/CI/折数/换手比与判据结论；草稿行。

---

## 实验 3（E1）：采样数曲线（GPU，约 16 小时，安排夜间串行）

**背景**：Kronos 打分 = 采样 `sample_count` 条路径取均值；实测同模型换批量重打两次分数秩相关仅 0.75（采样噪声）；`sample_count` 5→20 五读数 +4.9%（4/5 折）。本实验把「信号 vs 采样噪声」拆开。**CLAUDE.md §二例外条款已裁定实盘打分用 sc=20；封存队列仍 sc=5，不追溯。**

**判据**：诊断；预期 IC 随 sc 单调不降、信度单调上升。**用途限制**：不据此改封存口径；不选 sc（sc=20 已由理论条款定）。

**步骤**：
1. `.venv\Scripts\python.exe scripts\evaluate_fold.py --help` 核参数名（sample_count、amp、batch、lookback、predict、模型目录、输出 tag）。冻结口径：`amp=bf16, batch_size=128, lookback=90, predict=6`，**只改 sample_count**。FT 模型目录 `outputs/fold{f}_lb90_s0_poolB_universe`（f=36..42）。**不要用 `--sealed`**（开发折正常模式，会写 labels/metrics，允许）。
2. 运行顺序（串行）：sc=20 折 36–42 全部（tag `e1_sc20`，每折约 68 分钟）；然后 sc=10 与 sc=40 只跑折 36、39、42（tag `e1_sc10` / `e1_sc40`，每折约 34 / 136 分钟）。用带互斥锁 + 失败重试的 `.ps1`（参照 `scripts/run_sealed_confirm_queue.ps1` 的骨架；输出用 `Start-Process` 重定向，别用 `*>`）。
3. 新建 `scripts/exp3_samplecount_curve.py`：对每个 (fold, sc) 读 `scores.parquet`（含现有 sc=5 的 `eval_amp_lb90_fold{f}`），与 FT labels 合并，算 top500 与全池逐日 RankIC 均值 + NW(5) t、十分位价差；**信度** = 同折 `sc5 vs sc20` 分数逐日秩相关均值（以及 `sc20 vs sc40` 在三折上）。stationary bootstrap 95% CI。输出 JSON + SVG 曲线（纯 python 画，`.venv` 无 matplotlib，勿安装）。

**交回**：IC/价差/信度 对 sc 的表（含 CI）、GPU 总耗时、草稿行。

---

## 实验 4（E8）：STRATA 式分数残差化后的 IC（CPU，约 2 小时）

**背景**：STRATA（arXiv 2608.28060）规范：横截面打分先对 8 个价量风格因子做截面残差化，再算 rank IC，否则「偏向共同风格因子的打分也能拿高 IC」。K6b 做的是**收益层**张成（alpha 保留 141%），本实验做**分数层**残差化，两者并报。

**判据**：诊断。预期残差 IC 低于原 IC；**若残差 IC / 原 IC < 0.5，论文须披露「信号大部分可由价量风格解释」**。用途限制：不改信号、不改构造。

**步骤**：
1. 新建 `scripts/exp4_strata_residual_ic.py`。8 因子按 `scripts/k6b_spanning.py` 的定义构造（import 其函数；若不可 import 则逐字抄逻辑并在 docstring 注明来源行号；**不改 k6b**）。
2. 折 36–42 FT：逐日在 top500 内做 `score ~ 8 因子` 的截面 OLS（加截距），取残差；残差 RankIC vs label；与原 IC 并列；比值；两者的 NW(5) t；逐折。
3. 输出 JSON + 表。

**交回**：原 IC、残差 IC、比值、t、逐折；草稿行。

---

## 实验 5：K13 订单审计按 NT=5 重跑（CPU，约 30 分钟）

**背景**：持仓期已改 5 日（ledger 09-03），小试协议 v2 要按 NT=5 的订单流写规模预算。

**判据**：机械换算，无阈值。用途限制：**不得**用于选持有期 / 不交易带 / universe（K13 docstring 原有限制照旧）。

**步骤**：
1. 给 `scripts/k13_order_audit.py` 加 `--nt`（默认 6）与 `--out`（默认现有路径）。**先跑默认**，与现有 `outputs/k13_order_audit.json` 逐字段对拍（数值 `np.allclose` 且结构相同），不一致则停。
2. `--nt 5 --out outputs/k13_order_audit_nt5.json`。
3. 报：每笔名义额占 AUM（应 ≈ 1/250）、日订单数均值/p50/p90（FT 与 ZS）、到 200/500/1000 笔所需日数、被交易名字价格与 ADV 分位、两臂反向碰撞笔数（虽已裁定单臂，仍报）。

**交回**：与 NT=6 表并列（这是机械换算，不是收益比较，允许并列）；草稿行。

---

## 暂不做（需用户单独授权或另立任务）

| 项 | 原因 |
|---|---|
| **H3 扩展窗口树模型基线**、**E3 学习曲线** | 训练要用 2005–2020 的 6 日标签，等于**生成确认窗 labels**，违反 2026-09-01 的封存授权（「不允许生成或读取验证窗 labels」）。须用户单独授权后再做 |
| **E2 路径离散度校准** | 需改 `kronos_ft/infer.py` 保存逐路径预测，工程量大，另立任务书 |
| **任何合成分数（Kronos+信号#2）在折 36–42 上的收益或 IC** | v2 §5 明文禁止；H4 只在折 05–35 与 44–45 读 |
| **改持有期 / 退出线 / universe / 采样数（除 E1 的三档）** | 全是开发折搜索 |

---

## 交回模板

```
[gpt-exp N] <实验名>
命令：
退出码：
关键数字：
| … | … |
登记簿草稿行（已追加到 experiments/ledger_drafts_gpt_2026-09-04.md）：
- 2026-09-04 | <type> | **…**
异常 / 未做 / 与指令不一致处：
test_sealed_mode：passed/failed
git：<commit hash>（精确路径）
```
