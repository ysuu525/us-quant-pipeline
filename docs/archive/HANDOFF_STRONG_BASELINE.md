# 交接：强基线任务（GBDT + JKP 因子）

> 给新窗口。**这份文档只负责一件事：把树模型基线做扎实。**
> 项目全局现状见 `HANDOFF.md` 与 `experiments/ledger.md`（append-only，权威）。
> 本文写于 2026-08-31，对应提交 `b140736`（分支 phase5-ablations-and-monetization）。

---

## 1. 你要回答的问题

**"Kronos 到底有没有赢过一棵调好的树？"**

已有的弱基线答案是"赢了"，但那条基线很弱，不足以下结论：

| | 七折均值 RankIC | 备注 |
|---|---|---|
| Kronos lb90 | **+0.02067**（t=3.63） | 七折全正 |
| LightGBM 弱基线 | **-0.00099** | 三折为负；26 个纯价量特征、300 轮、未调参、单种子 |

**弱基线吃亏的地方很明确**：只用了价量特征，而这恰恰是树模型最不擅长的场景（序列结构树天然弱势）；树模型的主场是"几十上百个异质特征"，也就是**基本面因子**。外部调研（`docs/横截面选股模型选型调研_2026-08-31.md` 若已落盘，否则见 ledger 2026-08-31 条目）明确指出：Rahimikia 等在美股日频上实测**加入 JKP 因子后树模型与基础模型均显著改善**，这是 JKP 在文献里被证明有增量价值的地方。

所以：**没有加 JKP 的树基线，不算数。**

---

## 2. 判据（先于结果写死，不要看到结果再定）

强基线跑完后，与 Kronos 的 `+0.02067` 在**同七折、同池、同官方标签**下比较：

| 强基线七折均值 | 结论 | 后续 |
|---|---|---|
| < +0.010 | Kronos 明显胜出 | 继续 Kronos 路线，按 §5 做三项改造 |
| +0.010 ~ +0.020 | Kronos 领先但不决定性 | 两条线并行，最终做合成 |
| > +0.020 | **树模型追平或反超** | 重新论证 Kronos 路线的投入，优先做合成 |

另需报告：**七折中为正的折数**（Kronos 是 7/7，这比均值更能说明稳定性）。

---

## 3. 现成的东西（直接用，别重造）

| 东西 | 位置 | 说明 |
|---|---|---|
| 弱基线脚本 | `scripts/gbdt_baseline.py` | 特征构建、截面秩变换、折循环、官方标签评估全都写好了，**只需加 JKP 特征 + 调参** |
| 隔离环境 | `.venv-gbdt` | 已装 lightgbm 4.7.0 / pandas 3.0.5 / numpy 2.5.2。**绝不要往训练 venv 装 lightgbm**（本项目有装包破坏环境的前科，见 HANDOFF 坑表） |
| JKP 数据 | `F:\quant\external\jkp\` | `usa_all_factors_daily_vw_cap.csv`（153 个因子日收益，225MB）、`usa_all_themes_daily_vw_cap.csv`（13 个主题）、`usa_mkt_daily_vw_cap.csv` |
| CRSP 面板 | `F:\quant\processed\crsp_ciz_2026-08-24_20260825T130601Z\` | `panel_raw.parquet`（未复权全量，含 DlyCap/DlyPrcVol）、`universe.parquet`（逐日 in_universe 标志） |
| 官方标签 | `outputs/fold{NN}_lb90_s0_poolB_universe/eval_amp_lb90_fold{NN}/labels.parquet` | **评估必须用这个**，否则和 Kronos 不同口径 |
| 折边界 | `scripts/gbdt_baseline.py` 顶部 `FOLDS` 常量 | 七折 36–42，训练窗/验证窗都在里面 |

---

## 4. 具体要做的四件事

### 4.1 加 JKP 因子（最重要）
**注意一个坑**：JKP 给的是**因子组合的日收益序列**（一个因子一天一个数），**不是个股特征值**。所以不能直接当特征拼到个股上。两条可行路径：

- **路径 A（推荐，快）**：把 JKP 因子收益当**市场状态特征**——每只股票在某一天共享同一组 JKP 因子的近期表现（如各因子过去 5/20/60 日累计收益）。这让模型看到"当前是什么因子环境"，是纯价量模型完全没有的信息。实现简单，几十行。
- **路径 B（慢但更强）**：用 Compustat 自建个股层面的基本面特征（B/M、盈利、投资、应计等），对标 GKX 的 94 个特征。数据需要另外拉，成本高很多。

先做 A，跑出来看增量；不够再考虑 B。

### 4.2 调参
现在是拍脑袋的一组参数（lr 0.03 / leaves 63 / 300 轮）。用**折内验证**调（训练窗尾部切一段做内层验证，**不要用验证窗调参**，那是泄漏）。至少扫 learning_rate、num_leaves、min_data_in_leaf、num_boost_round。

### 4.3 多种子
单种子的读数不可靠（Kronos 那边实测分数信度只有 0.75）。跑 3–5 个种子取平均，报均值和离散度。

### 4.4 补 XGBoost / CatBoost
调研里美股日频实测是 **CatBoost 最好**。lightgbm 已装，另外两个装到同一个 `.venv-gbdt` 即可。

---

## 5. 与此同时主线在做什么（避免撞车）

主窗口正在做 Kronos 的**三项改造**（互相独立、可分别 A/B）：
1. **标签改截面去均值**——把大盘成分从目标里删掉；
2. **损失改排序损失**——目标函数与使用方式对齐；
3. **去掉 5 路径采样，改确定性排序头**——顺带修掉分数信度 0.75 的问题。

**不要动的文件**（主线在改）：`src/kronos_ft/train.py`、`src/kronos_ft/infer.py`、`src/kronos_ft/dataset.py`、`scripts/evaluate_fold.py`。
**你的地盘**：`scripts/gbdt_baseline.py` 及新建的任何文件、`.venv-gbdt`。

**GPU 归主线**，树模型跑 CPU，不冲突。若需要 GPU（如 XGBoost 的 gpu_hist），先问。

---

## 6. 必须遵守的项目纪律

1. **登记簿 append-only**：所有决定、读数、偏离写进 `experiments/ledger.md`，不要删改历史条目。
2. **判据先于结果**：任何新实验的判读标准要在看到结果之前写下来（§2 已给）。
3. **口径一致才能比**：同折、同池、同标签、同评估函数。Kronos 那边的分数还带打分口径字段（`metrics.json` 的 `scoring_config`），树模型这边也请记录参数。
4. **封存窗禁碰**：2024-01-01 之后的数据是最终考卷，**不得用于任何训练、调参、查看**。七折 36–42 的验证窗最晚到 2023-12-29，安全。
5. **小样本会骗人**：本项目已经栽过两次——三折时"lb200 领先 41%"，七折反转成落后 56%；三折时年化 +2.96%/回撤 4.7%，七折变 +1.45%/回撤 13%。**不要用少于七折的结果下结论。**

---

## 7. 一句话交接

`scripts/gbdt_baseline.py` 能跑通、口径正确、但特征太弱（只有价量）。
**加 JKP 因子环境特征 + 调参 + 多种子 + CatBoost，然后回答"+0.02067 打不打得过"。**
