# 解封读取任务书（折 05–35 + fold44–45）—— 2026-09-05

> 主会话写。执行者：opus。分两个阶段，**R-build 可立即开工但一个字节的封存分数都不读；
> R-read 只在登记簿出现用户对冻结版 v4 的书面「读」之后才允许运行。**
> 依据：`experiments/confirmation_protocol_v4.md`（冻结版，以其为准；本文与之冲突时以 v4 为准）、
> `CLAUDE.md`、HANDOFF §12.0 解封前置清单、Explore 勘察（2026-09-05，登记簿 `review` 行后）。

## 0. 硬禁令（违反即全部作废）

1. R-build 阶段：不打开任何 `eval_sealed_*/scores.parquet`；合成数据用 `src/kronos_ft/train.py::make_smoke_panel`。
2. R-read 阶段：**一次运行**，唯一输出目录 `outputs/unseal_<UTC>/`，含配置哈希、代码快照 sha256、工作树 porcelain。
3. **禁止自动 `append_ledger`**。`evaluate_fold.py:356` 的自动追加会把 62 条逐折 IC 写进 append-only 登记簿，
   越过 v4 §3 释放清单，并把实验 6 的试验数 N 从 148 推高、连带改动自己要兑现的预测区间。登记条目由主会话手写。
4. **不得计算、不得输出 FT−ZS 差值**（v4 §3）。两臂各自的量可报。
5. 释放清单严格限于 v4 §3；报告里**只有 31 折合并量 + 正折数**，不得出现逐折 IC 表。
6. fold44–45 是独立交付（研究计划书 H5，干净窗），同口径、同脚本、**独立输出目录，不并入 31 折合并统计**。
7. 措辞按 CLAUDE.md §二：B 层与估计交付不写「不可分 / 无效」，写「在本样本量下该问题不可回答」。

## 1. R-build：新写与改造清单（对应勘察缺口 G0–G8，G5 除外）

### G0 唯一解封入口
- `scripts/unseal_read_confirm.py`：唯一传 `assert_readable(..., unseal=True)` 的脚本。
  加入 `tests/test_sealed_mode.py` 的 `UNSEAL_ALLOWLIST`（:105-108）与 `MARKER_ALLOWLIST`（:100-103）。
- `src/signals/kronos_adapter.py`：**不放宽** `ALLOWED_FOLDS`；新增 `SEALED_FOLDS = {05..35, 44, 45}` 与显式 `unseal: bool` 参数。
- 每折读前核对 `SEALED_MANIFEST.json`：`scores_sha256`（`sealed.sha256_file`）与 `config` 五项
  （lookback 90 / predict 6 / sample_count 5 / amp bf16 / batch_size 128），失配即中止整个运行。
- 入口先跑「合成假数据全链路」再跑真数据（v4 §7 第 3 条）；提供 `--smoke` 开关。

### G1 标签 + 指标补算器（最关键）
- `scripts/compute_labels_for_sealed.py`：复用 `evaluate_fold.py:281-309` 的整段（`panel_raw` 列裁剪 + 日期下推、
  `distributions` 现金股息、`compute_labels(predict_window=6)`、DlyCap/ADV20 合并）。
- 产物写到 `outputs/unseal_<UTC>/{ft,zs}/foldNN/{labels.parquet,metrics.json,daily_ic.parquet}`，
  **绝不写进封存目录**（`sealed.write_seal` / `audit_dir` 会因目录内出现 labels 而失败）。
- 05–35 跨 2004–2021：`panel_raw` **按折切片读**（§七禁整表读）。预算：每折约 1 分钟，66 折约 1 小时。
- `metrics.json` 键沿 `evaluate_fold.py:313-334`；**注意** `ic_by_adv_quintile` 的 ADV20 是当日口径，
  与 H2 的滞后口径不同，只作诊断、不进 H2。

### G2 H1 汇总器
- 合并 31 折逐日全池 RankIC → 点估计、NW(5) 95% CI、正折数（/31）；两臂各一份。
- 实现收缩比 = 实测 / 0.0207（FT）与 / 0.01919（ZS），报其 95% CI 与 v4 §2.3 主预测区间
  （FT `[0.0088812, 0.0182250]`、ZS `[0.0080529, 0.0170925]`）及敏感性区间 `[0.0149, 0.0166]` 的相交关系。
- H4 读取闸门（v4 §2.3）：该臂 H1 的 95% CI 下界 > 0，只记录、不触发任何动作（H4 空置）。

### G3 H2 / ΔADV 31 折版
- 以 `scripts/k11_zs_ft_candidate_audit.py:144-174` 为模板，参数化 `FOLDS`、`ARM_PATHS`、`LO/HI`、**`NT=5`**。
- ADV20 必须用 `k9_cost_alpha_frontier.py:160-161` 的**滞后**口径 `rolling(20,min_periods=10).mean().shift(1)`。
- 三门（v4 §2.5）：CI 下界 > 0 ∧ 正折数 ≥ 18/31 ∧ mean RankIC(top500) > 0；**没有量级门**，不得照抄 `k11:193`。
- z = 2.2414（k=2）。交付点估计、CI、正折数、三门各自的真假；仍是估计交付措辞。

### G4 H2-era 年代二分
- 抽出 `scripts/k6c_era.py:263 run_era()` 参数化；切点 **2013-01-01 按 val_start 写死，只切一次**，脚本内不得有分年循环。
- 交付两段各自 ΔADV / RankIC(top500) / RankIC(full) 点估计 + NW(5) 95% CI，加交互项（晚 − 早）的点估计 + CI + 逐折方向。
- 两段 MDE80 取 v4 §2.5.1（定稿时已由算术补上），只作披露。

### G6 H1b 张成
- `scripts/exp11_spanning_extended.py` 加 `--folds` 与 `FOLD_WINDOWS` 注入；分数取用走 G0 的 `SEALED_FOLDS`。
- 口径：沿 K6b（NT=6）冻结构造，**与 NT=5 读数不并列**，报告页头写明；主规格 **S-TH-ind** 先验固定，S-T / S-H 并列作次规格。
- 交付 alpha 点估计、NW(5) 95% CI、正折数；无门。

### G7 E 端
- 新写 31 折驱动，直接调 `src/portfolio/construction.frozen_long_only_returns(..., nt=5)` + `src/backtest/money.run_frozen_money`；
  价格窗口扩到 2004–2021，**按块读**。
- 交付（v4 §2.2）：毛年化 + NW(5) CI + 正折数、单边换手、`BE ± 95% CI`、成本网格 2/4/8/12/16/22bp 净年化；
  部署线按 v4 §2.2 的 `C ≤ BE_dev×(1−h) − 6bp`，h=0.42 主判、[0.399, 0.440] 敏感性；**C 未实测前只报线，不判 go**。

### G8 H6 / ICT P1
- `scripts/ict_pattern_probe.py` 加 `--folds` / `--eval-root` 与面板块注入（2004–2021，回看余量 > 45 交易日）；
  **只跑 P1_bull / P1_bear，只算 Q1 + Q2(b)**；NW lag = **6**；Q2(b) 五分位用 FT 封存分数（sc=5，跑前核对）。
- 交付 v4 §2.8.3 所列各量；估计交付，无门。

### 测试
- `tests/test_unseal_read_confirm.py`：合成数据上跑通 G0–G8 全链路；断言不写封存目录、不调用 `append_ledger`、
  不产生任何 FT−ZS 差值键、报告中无逐折 IC 表。
- 原 `test_sealed_mode.py`（白名单更新后）与 `test_prereg_manifest.py` 全部通过。

## 2. H3 树基线（G5）：本任务书**不包含**

树基线在 05–35 上训练需要**封存窗内的标签作训练目标**，超出 2026-09-01 「计算授权」（该授权只覆盖 Kronos FT 训练与打分）。
另需重建 JKP 派生快照（2002–2020）、扩折表、重建年度缓存，冻结配置 sha256 必然变化，须登记「口径延伸」。
处置由用户裁定：(a) 授权一次**计算专用**的树基线 31 折训练与打分（封存模式，不算指标），在 R-read 前完成；
(b) R-read 后按冻结配置另行计算，披露「晚于 H1 计算、无设计自由度」。v4 §2.6 已把 H3 定为 B 层估计交付，两条路都不改结论层级。

## 3. R-read：运行与交付

- 前置（缺一不跑）：v4 冻结版已提交；第二章 `.ots` 已生成并提交；tag 已推公开仓库；登记簿有用户「读」的书面行。
- 单命令：`.venv\Scripts\python.exe scripts\unseal_read_confirm.py --out outputs\unseal_<UTC>`，先 `--smoke` 再真跑。
- 报告 `outputs/unseal_<UTC>/report.md`：按 v4 §2 顺序 H1 → H1b → H2 → H2-era → H6 → E，每项只含释放清单允许的量；
  另一份 `report_fold44_45.md`。任何解释性文字标【推断】，读数标【实测】。
- 执行者最终消息 ≤ 500 字：每个假设一行（点估计、CI、正折数、闸门/相交关系），运行时长、任何偏离。
- 登记条目由主会话按报告手写，一条 `confirmation-readout` 行。
