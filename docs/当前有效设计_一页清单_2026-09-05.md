# 当前有效设计：一页清单（2026-09-05）

> 依据 `docs/研究诊断与收口建议_2026-09-05.md`（用户 09-05 采纳）第一步交付。
> 本页只回答「现在这一轮究竟交付什么、有效设计是什么、离读取还差什么」。
> 旧的进度描述不再作为当前状态；与本页冲突的以 `experiments/confirmation_protocol_v4.md`
> 及其 `v4.1_addendum_2026-09-05.md` 为准。每条附出处；「未核」即未核。

## 1. 本轮的主问题与交付

**主问题**（采纳审计原句）：固定的 Kronos FT 设计，在研究者未用于选择的历史段上保留多少横截面预测信息？其经济估计与泛化证据止于哪里？

**交付三步**（缺一不得往下走，完成第三步即本轮结束）：

| 步 | 交付 | 完成条件 |
|---|---|---|
| 1 | 本页 | 已写（09-05） |
| 2 | 既有确认读取的验收 | **已完成（09-06）**：用户书面授权 `ledger.md:689`（四项均确认）→ 一次性读取折 05–35（FT 臂）与干净窗 fold44–45；读数在 `ledger.md:701`、`:703` 两条 confirmation-readout 行；报告存档 `docs/unseal_readout_20260905T151817Z/`（主段）与 `docs/unseal_readout_20260905T160632Z_cleanwin/`（干净窗，独立交付、不并入 31 折）。干净窗段曾在 H3 处按设计中止（树基线无 44–45 清单），按 v4 §7 第 4 条以 `--clean-window-only --no-h3` 独立目录补跑，主段产物未动，deviation 行在前 |
| 3 | 一份完整结果报告 | **起草中**：opus 按 `docs/结果报告_任务书_2026-09-06.md` 起草，用户 09-06 审。原范围交付 H1 / E 与已冻结辅助终点；开发观察、历史确认、较干净窗口三者分列；原预测落空照报 |

**停止条件**：报告完成即结束本轮开发。结果不支持原主张则收窄结论；不确定则如实交付区间。**不得**再调候选、换指标、加规则来推迟结题。**不再启动**新读出、新信号、新控制集、采样扩展（审计「唯一工作顺序」）。**唯一例外**：exp16（`--loss topk` 排序头七折估计），用户 09-06 指示启动，`ledger.md:687` 明记「本条覆盖收口计划『不再启动新读出』一项，仅此一项，其余停止条件不变」。

## 2. 有效设计（确认对象）

| 项 | 取值 | 出处 |
|---|---|---|
| 候选臂 | **只有 FT**（微调生成式）。预注册 k=2（FT、ZS），读取前按用户裁定放弃 ZS；论文须写「预注册 k=2，读取前放弃 ZS」 | v4 §1；v4.1 §1 |
| 多重性门槛 | z = 2.2414 **维持不放宽** | v4.1 §2 |
| 模型输入 | `panel_kronos_adj`（§5 复权 OHLCV），lookback 90，predict 6 | v4 §1；`evaluate_fold.py` |
| 训练池 | 未过滤全量面板（用户 08-27 拍板） | `ledger.md:6` |
| 打分池 | pool B：anchor 日在 §2 universe 内 | `evaluate_fold.py` docstring |
| 采样数 | 封存分数 **sc=5**（不重打） | `ledger.md:408`；`CLAUDE.md` §二例外 |
| 组合构造（冻结） | top500 流动性过滤 / 进前 10%、跌出前 30% / **五袖套 NT=5** / t 收盘出分、t+1 开盘执行 / 纯多减同池等权基准 | v4 §1；`ledger.md:407`、`:409` |
| 折表 | **CONFIRM = 表 B**（`emit_folds.py:34`，起算 2000-01-03）：折 05–35 与 fold44–45；Kronos FT 封存、XGBoost 封存、`src/unseal/folds.py` 同表 | v4.1 §5 |
| 读取前剔除 | 从封存 fold35 剔除信号日 2020-07-01、07-02（与开发 fold36 重叠），报告写剔除行数 | v4.1 §5.2 |
| 终点 | E（估计交付）、H1（区间 + 点预测 [0.0088812, 0.0182250]）、H1b（S-TH-ind 主规格，估计交付）、H2（符号 + CI + 折数）、H2-era（2013-01-01 切点，A 层）、H3（XGBoost×**FT**，B 层估计交付）、H6（探索性）；H4 空置 | v4 §2.1；v4.1 §2 |
| 释放边界 | 只释放 v4 §3 九条；逐年曲线、五档、替代阈值、任何臂间差一律不算不看；打开后 31 折永久视为已消耗 | v4 §3 |
| 部署判据（仅披露） | FT：`C ≤ BE_dev×(1−0.42) − 6bp` = **7.28bp** | v4 §2.2；`ledger.md:512`② |

## 3. 有效设计（实盘对象，**不是**确认对象）

确认结果**只**认证 §2 的对象。下列每项都使实盘版本成为另一个对象，报告与协议里必须分开标：

| 项 | 实盘取值 | 与确认对象的桥接证据 |
|---|---|---|
| 采样数 | **sc=20**（零参数、理论单调，按 `CLAUDE.md` §二例外采纳） | 实验 3：七折全池 IC sc5 0.02067 → sc20 0.02185；三折 5/10/20/40 单调；sc5 对 sc20 逐日秩相关 0.75，sc20 对 sc40 0.91（`outputs/exp3_samplecount_curve.json`）。实验 9 test–retest 进行中 |
| 进场过滤 | **Rule E** 财报日回避（预期公告日 ±1 日落在 [t+1, t+6] 则不新建仓、递补，不强退），零自由参数、先验固定 | 无组合层证据；exp13 的 20.60% 是股票层平方偏差份额，**不是组合方差**；措辞只能是「意在减少公告暴露」（`ledger.md:587`；审计第 4 条）。exp14 为其开发折估计 |
| 持有语义 | NT=5 是套袖重平衡周期，旧持仓可保留，不是固定五日必卖；预测窗仍为 6 日 | `construction.py`；审计第 2 条。Rule E 文档「六日冻结持有期」措辞待改 |
| 资本层 | 默认 L ≤ 1，待用户看过测算后书面定 | `ledger.md:485`② |
| 执行 | Alpaca；t+1 开盘须走收费路由；小试协议 v2 待用户 AUM 与 606 路由答复 | HANDOFF §12.0 待办 |
| 前瞻日历 | 回测用 Compustat `rdq_hat`（禁商用）；实盘须公开源（SEC 8-K 2.02 + 公司确认），**未建，上线前置** | `ledger.md:587` |

**部署轨道待办**（不是本轮报告前置）：exp14（另一 Fable 会话已启动，估计交付，结果不改 Rule E 定义）、公开前瞻日历、小试协议 v2、杠杆裁定。**不得**借用确认结果替代实盘版本的验证。

## 4. 读取前置（v4 §7）——**09-06 已全部走完，本节留作记录**

读取已于 09-06 完成（见 §1 第 2 步）。**折 05–35 与 fold44–45 自此永久视为已消耗**（v4 §3 末段），任何再读取须另行预注册并盖章。下表为读取前的状态快照：

| 条 | 状态 |
|---|---|
| 7 第二章外部时间戳 | 已做：区块 965508（v4.1 页头）。**第二章 b**：树基线 31 折封存算完后重生成清单并盖章，**待树基线队列完成**（fold13 已过，队列在跑） |
| 8 tag 推公开仓库 | 已做：`prereg-2026-09-05-v4`（v4.1 页头）；GPG 签名已登记放弃 |
| 1–3 R-build 配置哈希 / 无候选循环 / 合成数据冒烟 | **已做，运行时产出哈希**（quant-db 09-05 验收）：唯一入口 `scripts/unseal_read_confirm.py`（提交 65d15e0、2276766）运行时写 `run_meta.json`（配置哈希 / 代码快照 / 唯一输出目录）；脚本无分年分档候选循环（有测试断言）；合成数据 smoke 1.4 分钟跑通；`test_unseal_read_confirm` + `test_sealed_mode` 173 passed |
| 9 用户对冻结版的书面「读」 | **未做**。届时由 quant-db 请用户一并确认四项：(a) 折 05–35 + fold44–45 范围；(b) 只读 FT 臂（ZS 已抛弃）；(c) 按 v4.1 §5 剔除 fold35 两个信号日；(d) 一次性读取不回头。§3 释放清单含 H2-era / H6 / H1b / D2 |
| 11 一次性同刻读取 | R-read 先同参数 `--smoke` 再真跑：`scripts/unseal_read_confirm.py --arms ft`，fold44–45 独立目录 |

顺序（以 quant-db 为准）：树基线队列完成（09-05 晚约 fold20，剩约 4 小时）→ 树基线 opus 草稿并入登记簿并提交 → 第二章 b 重生成清单（含全部树基线 SEALED_MANIFEST、R-build 代码、v4.1 附录）+ `ots stamp` + 推公开 → 用户书面「读」并确认上述四项 → R-read 先 `--smoke` 再真跑 → 2b 的 `ots upgrade` 入块后补。

## 5. 开发折上已知的事（方向性证据，折 36–42 已消耗）

| 读数 | 值 | 出处 |
|---|---|---|
| FT 七折全池 RankIC | +0.02071（7/7） | `ledger.md:162` |
| 八因子残差化后 top500 IC | 0.0258 → **0.0070**（保留 27%） | exp4，`ledger.md:482` |
| 读出对比（七折） | 生成式 FT +0.0207 ＞ 岭探针 +0.0089（t 1.46）＞ MLP +0.0070（t 1.19）＞ XGBoost +0.0063 | exp8，`ledger.md` 09-05 合并行 |
| 污染探针 P3 | 5/7 折干净窗 NLL 更低，差 0.8–3.7%，随波动率移动；**不判定** | exp10 |
| 选择偏差实测 | 内层 vs 外层选参 +0.0070；探针 oracle 与诚实口径差 +0.0062 | `ledger.md:164`；exp8 |

这些只支持「有值得验证的候选信号」；**不**支持独立新 alpha、现实可部署性或无污染的前向泛化（审计结论）。

## 6. 不再当作当前状态的东西

HANDOFF 历史章节与 `docs/HANDOFF_archive_0826-0904.md` 的进度描述；折 36–42 上的一切臂间比较（一律「本样本量下不可回答」）；NT=6 口径的任何读数（不得与 NT=5 并列）；ZS 臂的一切计划（封存分数不删不读）。

**数据消耗状态（09-06 起）**：折 01–04、36–42 早已消耗；**折 05–35 与 fold44–45 于 09-06 一次性读取后永久视为已消耗**。本项目已无未读的历史窗口；下一份干净证据只能来自前向实盘（v4 §6）。
