# 结果报告初稿任务书（一页清单第三步）—— 2026-09-06

> 主会话写，opus 执行。用户 09-06 凌晨指示：「R-read 完成后直接起草结果报告初稿，按一页清单第三步，
> 开发观察、历史确认、较干净窗口三者分列，原预测落空照报，我明早看。」
> 交付 `docs/结果报告_初稿_2026-09-06.md`，文首标「初稿，待用户审」，≤ 4000 字。**只写文档，不算任何新数。**

## 0. 主问题（审计原句，报告第一节原样引用）

固定的 Kronos FT 设计，在研究者未用于选择的历史段上保留多少横截面预测信息？其经济估计与泛化证据止于哪里？

## 1. 输入（只读这些；任何不在下列的数字不得出现）

| 来源 | 用途 |
|---|---|
| `outputs/unseal_CURRENT_RUN.txt` 所指目录下的 `report.md`、`report_fold44_45.md`、`summary.json`、`run_meta.json`、`caliber_check.json` | 历史确认（折 05–35）与较干净窗口（fold44–45）的**全部**读数 |
| `docs/当前有效设计_一页清单_2026-09-05.md` | 对象定义（§2）、实盘对象不是确认对象（§3）、交付与停止条件（§1） |
| `experiments/confirmation_protocol_v4.md` §2、§3、§5、§6；`confirmation_protocol_v4.1_addendum_2026-09-05.md` | 各终点的估计量、预测区间、释放清单、污染与干净窗职责、ZS 抛弃与折表披露 |
| `HANDOFF.md` §2「当前最好的诚实读数」；`experiments/ledger.md` 2026-09-05/06 各行（实验 6 DSR/haircut、实验 4 残差 IC、实验 11 张成、ICT 探针、对抗评估结论、各裁定） | 开发观察（折 36–42，已消耗，方向性证据） |
| `outputs/exp6_deflated_sharpe.md` | 试验次数 N、DSR、haircut，用于原预测对账的语境 |

**禁止**：读任何 `eval_sealed_*` 或树基线封存目录内的 `scores.parquet`；读 unseal 目录下的逐折文件（`ft/foldNN/*`、`per_fold_index.json` 只允许核对折数与剔除行数，不得摘录逐折 IC）；计算任何新统计量；引用 ZS 的任何读数。

## 2. 结构（章节固定）

0. **结论**（≤ 200 字）：对主问题的直接回答，三段各一句。
1. **主问题与确认对象**：一页清单 §2 的表压缩成一段；写明「预注册 k=2，读取前按用户裁定放弃 ZS」，不得写成「预注册只含 FT」。
2. **开发观察（折 36–42，已消耗，方向性证据）**：RankIC +0.0207 / t 3.63；净纯多超额、夏普、回撤、日收益 t 1.32；同模态树基线；exp4 残差 27%；exp6 N=148、DSR 0.044、haircut 40–44%；ICT 六概念不可回答；每条标【本项目实测】，并明写「这些数参与过设计选择，是方向性证据不是无偏估计」。
3. **历史确认（折 05–35，未消耗，2005-01..2020-07）**：按 v4 §3 释放清单逐项：H1（点估计、NW(5) 95% CI、正折数/31）、H1b（S-TH-ind 主规格 + 两个次规格，alpha、CI、正折数；注明 K6b/NT=6 口径）、H2（ΔADV 点估计、CI、正折数、三门各自真假，无量级门）、H2-era（早期 fold05–20 / 晚期 21–35 各自读数与交互项 CI；两段 MDE80 只作披露）、H3（ΔIC Kronos−XGBoost、CI、正折数；主口径 XGBoost×FT，MDE80 0.0329）、H6（P1_bull / P1_bear 的 Q1 与 Q2(b)）、E（毛年化、CI、正折数、单边换手、BE ± CI、成本网格 2/4/8/12/16/22bp、部署线 7.28bp 只披露不判 go）。每项一行结论 + 一张小表。fold35 剔除行数写明。
4. **较干净窗口（fold44–45，2024-07..2025-07）**：同样的量，独立成节；明写三条限制：仅 1 年、预期 t≈0.65；与 Kronos 作者自己的测试期重合、checkpoint 级开发者选择不外生；该窗结果不并入 31 折。
5. **原预测对账（落空照报）**：H1 主预测 [0.0088812, 0.0182250] 与实测 95% CI 是否相交；实现收缩比 = 实测 / 0.0207 及其 CI；敏感性区间 [0.0149, 0.0166]；按 v4 §2.3 写明「不相交不构成对信号的否定，只否定『选择偏差估计正确』这一元主张」；H2 三门是否满足只作陈述。**任何落空按原文标准如实写，不得改标准、不得补事后解释。**
6. **经济估计与部署线**：E 端读数对照部署线；写明 C 未实测、只报线不判 go；引用对抗评估结论一句（散户产品不值得做、研究项目值得做完）作为语境，不展开。
7. **披露与限制**（逐条）：ZS 抛弃（冻结后读取前、看过开发折后的设计变更）；两张折表与 fold35 两日剔除；v4 §2.8.1 勘误（以 §2.2 为准）；H3 主口径由 XGB×ZS 改为 XGB×FT 及功效变差；树基线只跑 XGBoost；H1b NT=6 口径不与 NT=5 并列；折 05–35 全部落在 Kronos 预训练语料窗内、无法独立识别污染；实验 6 的 N 随登记簿增长；H1b/signal2 读数的 1–3 日窗口边缘差未重算；内存门 40→52 GB 的工程偏离；打分无随机种子的可复现性限制。
8. **收窄后的主张**：写出 3–5 句论文可以直接用的句子，每句只依赖上面已报的数字，并标 A 层 / B 层 / 估计交付。
9. **附录：数字来源清单**（文件、键或行号）。

## 3. 措辞与标注

- 每个数字标【实测】，每句解释标【推断】；未核到的写「未核」。
- B 层与估计交付一律用「在本样本量下该问题不可回答，按预先规则取 X」，不写「无效 / 相等 / 不可分」。
- 不出现任何逐折 IC 表、任何 FT−ZS、任何未在释放清单中的量。
- 报告是研究报告，不是产品判决；产品轨道搁置一句带过。

## 4. 最终消息（≤ 300 字）

三段各一句结论；H1 主预测是否兑现；哪些终点落空；报告字数；任何你认为应由用户裁定的措辞问题。
