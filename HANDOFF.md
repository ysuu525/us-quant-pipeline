# 交接文档（2026-08-31 下午）

> 给下一个会话。**先读仓库根目录的 `CLAUDE.md`（强制规则），再读本文。**
> 权威流水账在 `experiments/ledger.md`（append-only）。
> 给外部调研的自足简报在 `docs/开放问题与决策困境_2026-08-31.md`（困境 A–H）。
> 上一版交接（2026-08-27，已过期）归档在 `docs/HANDOFF_2026-08-27_已过期.md`。
> 对应提交 `0b98140`，分支 `phase5-ablations-and-monetization`（已推 GitHub）。

---

## 1. 三句话现状

数据、环境、工具链就绪；**训练池 = B、lookback = lb90 已定案**（IC 与净收益两个
独立指标一致支持）；近代七折（2020H2–2023H2）纯多超额 **年化 +6.4%、夏普 0.70、
最大回撤 −12.4%、最长水下 17 个月**，但 **策略自身 t 仅 1.32，不显著**。

三天里推翻了大量早期结论（§5），并发现四类我方方法学错误（§6）。
**当前卡在两个需要用户裁定的决策上（§7），在裁定前不得动用折 05–35。**

---

## 2. 当前最好的诚实读数

七折 = fold36–42（2020-07 至 2023-12，832 个交易日）。口径：lb90 微调版 /
top500 流动性过滤 / 六档错位 / 进入前10%、跌出前30%才卖 / t+1 开盘建仓 /
8bp 单边 / bf16 打分 / 纯多超额（相对同池等权基准）。

| 指标 | 值 |
|---|---|
| 逐日 RankIC | +0.0207（NW t = 3.60），七折全正 |
| 年化超额 | **+6.43%** |
| 波动 / 夏普 | 9.20% / **0.70** |
| 最大回撤 | **−12.4%** |
| 最长水下 | **359 个交易日（约 17 个月）** |
| 月度胜率 | 52%（42 个月中 20 个为负），最长连续哑火 4 个月 |
| **日收益 NW t** | **+1.32 —— 不显著** |
| 张成回归后 alpha | +6.21%（保留 89%），t(alpha) = 1.46 |

**多空版**年化 +22.62%、夏普 0.98，但隐含 2 倍总敞口、未扣保证金利息、
空头腿七折均不显著、alpha 仅保留 55% → **不作为可执行方案**。

**成本敏感性**：8bp 成立、15bp 时大部分配置转负。成本档是生死变量。

---

## 3. 已定案 / 已否决

**已定案**：训练池 B ｜ lookback = lb90 ｜ 持仓 6 日 ｜ t+1 开盘成交 ｜
打分 bf16 + batch 128 ｜ 只做流动性前 500 ｜ 省着换（退出线 30%）｜ 纯多为主线。

**已通过的否决性检验**：延迟建仓 1 日保留 85% 毛价差（绿线 75%，排除买卖价差伪影）；
市值中性化后 RankIC 保留 88%、毛价差 92%（绿线 60%，排除"只是买小盘股"）。

**已否决**（均附实测）：
- 多档合成（七折 +3.1%、2/7 折最优，不值双倍成本）
- 拉长持仓期（6 日最优；21 日 −0.72%；信号半衰期约 2 日）
- 夜盘/盘前执行（越早成交越吃亏；收盘竞价需成本减半才追平）
- 冻结主干 + 线性探针（+0.0126）、+ MLP 排序头（+0.0075）
- 继续加大微调轮数（e1 与 e30 不可分；微调整体仅 +7.9%）
- 截面上下文（便宜版：均值 +0.0022、3/5 折为正 → 判"边际"）

**同模态基线**：LightGBM（26 个价量特征）七折 **−0.0010**，三折为负 → Kronos 胜出。
另一窗口在跑加 JKP 因子的强基线，见 `HANDOFF_STRONG_BASELINE.md`。

---

## 4. 正在跑 / 刚跑完

| 任务 | 状态 |
|---|---|
| 零样本 sample_count=20（三折） | **进行中**（fold39）；fold40 已出 +0.01644（vs sc=5 +0.01499，+9.7%） |
| 微调 sample_count=20 | 完成：fold40 +0.02900（+9.4%）、fold39 +0.04521（+4.9%） |
| 树基线加 JKP（另一窗口） | 进行中 |

**采样路径 5→20 结论**：三读数平均 **+8%**，方向确定、机械性增益（纯降噪、
无过拟合风险），**代价是四倍打分时间**（22 → 75 分钟/折）。
建议**留到设计冻结后的确认集跑批再启用**；它是官方冻结采样参数，改动须先记差异清单。

---

## 5. 被推翻的结论（小样本四次翻车）

| 早期结论（小样本） | 七折修正后 |
|---|---|
| lb200 近代领先 41%（3 折） | **落后 56%**；按净收益是 +6.43% vs **−4.48%** |
| 年化 +2.96%、回撤 4.7%（3 折） | **+1.45%、回撤 13%**（全池口径） |
| 合成较最优臂 +11%（3 折） | **+3.1%**，方案否决 |
| 微调提升 77%（1 折） | **+7.9%**，4/7 折 |

**规则已写进 `CLAUDE.md`：少于七折的结论一律不算数。**

---

## 6. 我方方法学错误（必读，否则会重犯）

三天内引入并自行发现**四类**，**无一例外朝"让结果更好看"的方向**：

1. **前视**——把"当日截面离散度"用 `label`（未来 6 日收益）的标准差构造。
   **贡献约 37% 表观增益，把结论从"有价值"翻成"边际"。**
2. **在评估窗上选超参**——偏差实测 **+0.0070**（比信号本身的三分之一还大），
   且**修完当天在新脚本里重犯一次**。
3. **判据只有量级门槛、无一致性门槛**——2/7 折为正时仍判"通过"。
4. **执行时点早一天**——净值模拟从 t 日收盘起计收益。

**已建的两道防线**：
- `CLAUDE.md`：四条强制规则，每条附实例与自查方法（如"提交前 `grep -n label`
  逐条确认每处都是目标或结果"）；
- **`src/crsp_pipeline/leak_check.py`（新增，已验证）：打乱标签自检**。
  在**每日截面内**打乱标签重跑，干净管线的零分布应居中于零。
  自测：干净管线零分布 +0.003±0.005；人为泄漏的管线零分布立刻 +1.000。
  **下一个会话的首要任务是把它接进每个新实验的例行检查**——
  这是唯一不依赖人工审计的防线，人工审计已被证明会漏（我自己就重犯过）。

---

## 7. 等待用户裁定（在此之前不得动折 05–35）

### 7.1 开发 / 确认 / 测试如何划分

**已消耗**：折 01–04（早期筛选）、折 36–42（三天内数十次决策读数）——
读数为方向性证据、**非无偏估计**。
**未碰**：折 05–35（31 折，2005–2020，含 2008 与 2020Q1）；封存窗 2024-07 起。

**拟议（未定）**：开发 = 01–04 + 36–42；确认 = 05–25（21 折）；
后备 = 26–35（10 折）；最终测试 = 封存窗。

**定量约束**：夏普 0.70 时，3.3 年样本的 t 天花板是 1.3；t>2 需约 8 年、
t>2.8 需 15.5 年。**折 05–35 覆盖 15.5 年，不是"太大"而是"刚够"。**

### 7.2 跑确认集之前必须先写死"若早期亏损则结论是什么"

**警告**：同一套冻结构造套到折 01–04（2003–2004）上，年化 **−3.15%**，
而同期 RankIC +0.0178（与近代相当）。且 8bp 成本对 2003 年严重乐观。

**所以确认集很可能不是"确认它行"，而是"发现它只在近几年行"。**
若结论是"只用近几年重新设计"——这本身又是在数据上挑时期，须按多重检验处理。

用户已表示两件事要带给更强的模型调研，材料见
`docs/开放问题与决策困境_2026-08-31.md`（困境 A–H，每条附实测与待调研问题）。

---

## 8. 环境与路径

| 东西 | 位置 |
|---|---|
| 代码 | `F:\quant\us-quant-pipeline`，分支 `phase5-ablations-and-monetization` |
| 训练 venv | `.venv`（**勿装 lightgbm/wrds**） |
| 树模型 venv | `.venv-gbdt` |
| 整理后面板 | `F:\quant\processed\crsp_ciz_2026-08-24_20260825T130601Z\` |
| JKP 因子 | `F:\quant\external\jkp\`（因子**组合日收益**，非个股特征值） |
| 表示缓存 | `outputs/repr_cache/`（5.1GB，folds 36–40 的 mean 池化） |
| 零样本底座 | `outputs/zeroshot_base/`（未微调的 Kronos-small） |

**机器硬约束**（均已踩坑）：提交上限 55.9GB 常年吃紧 → 脚本必须列裁剪 +
日期下推过滤 + 分块，**禁止整表读 `panel_raw`**（4980 万行）；单卡 16GB，
批量 >128 有害；**分离启动的后台进程会夭折**，用受管后台任务；并发 CUDA 可共存但
**强杀其中一个会引发驱动崩溃**；多会话时 `git add` 用精确路径（曾把另一窗口的
在制品卷进提交）。

**建议用户做的一次性系统改动**：页面文件设为固定 64GB（现系统托管、实际仅 1GB），
可根治提交上限问题。需重启，**等所有跑批结束后再做**。

---

## 9. 下一步（按性价比）

1. **把 `leak_check` 接进例行检查**（半天，零算力）——最高优先级。
2. **等用户对 §7 的裁定**；在此之前只在开发集上探索。
3. **向 Alpaca 发工单**：空头卖出所得是否生息 / 能否抵扣借记余额。
   免费、异步，决定多空方案净收益的符号，两轮检索均未找到答案。
4. 调研结果回来后再决定是否投入横截面模型（便宜版已判"边际"，
   无新证据前不建议开新 session）。
5. **不要做**：继续加大微调轮数、上 GNN、在开发折上继续调参数。
