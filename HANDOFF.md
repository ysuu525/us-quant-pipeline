# 交接文档（最后更新 2026-09-05）

> **新窗口只需读本文件的 §12.0（下一节，约 6 KB），再读
> `experiments/ledger.md` 最后 30 行。其余按需。**
>
> 本文件 09-04 做过一次结构整理：**只改编排，一字未改内容**。
> §1、§4–§7、§9、§10、§11、§12.1–§12.11 全部原样移入
> `docs/HANDOFF_archive_0826-0904.md`，本文件末尾有逐节索引。
> 留在这里的四节是每会话都要用到的：§12.0（当前状态）、§3（环境与路径）、
> §2（关键读数）、§8（坑）。
>
> **章节号一律沿用原编号、不重排**——`experiments/ledger.md` 是 append-only
> 且大量按「HANDOFF §N」引用，改号会让历史引用全部失效。
> 所以本文件的物理顺序是 12.0 → 3 → 2 → 8 → 附录，看着跳，但引用永远对得上。
>
> 权威文件：`experiments/ledger.md`（append-only 流水账，唯一权威）、
> `CLAUDE.md`（每会话必读的强制规则）、
> `experiments/confirmation_protocol_v4_revisions.md`（开工即用的待改项）。

---

## 12.0 当前状态（2026-09-05 晚快照）

> 本节即原「§12.0 新窗口从这里读」，编号未变；登记簿里以 §12.0 引用的就是这一节。
> 原属 09-03 的过程记录 §12.1–§12.11 已移入 `docs/HANDOFF_archive_0826-0904.md`。
> 本节所有 `ledger.md:<行号>` 按 **515 行版本**（工作树，commit `f4279cc` 之后未再追加）。

**一句话**：确认集协议 **v4 已冻结**（`experiments/confirmation_protocol_v4.md`，sha256
`c3c823e2189f53137f158c04dca6240022a03774e28a57388c3556fd65bcfe3d`，1053 行，`ledger.md:515`）；
第一章外部时间戳已升级到**比特币区块 965321**（最早区块时间 2026-09-03 13:41:37 UTC，`ledger.md:484`）；
**GitHub 仓库已转 public**（`ledger.md:514`）；用户已裁定「**开始解封**」并另行授权树基线在折 05–35 上做
**计算专用**的训练与打分（`ledger.md:512`①、`:513`）；**产品轨道搁置**——对抗评估结论是
「**作为散户产品不值得做；作为研究项目值得做完**」（`ledger.md:511`）。
**实际读取仍未发生**，卡在 v4 §7 第 7–9 条（第二章时间戳 / tag 推公开 / 用户书面「读」）。

**权威文件（按需读）**：`experiments/confirmation_protocol_v4.md`（**冻结版，解封时真正生效的协议**，
与任何其它文档冲突时以它为准；§7 是十一条机械检查、§8 是裁定记录与披露项）、
`docs/解封读取_任务书_2026-09-05.md`（R-build / R-read 两阶段，硬禁令七条）、
`docs/审稿_产品可行性_对抗评估_2026-09-05.md`（红队，为什么产品搁置而研究继续）、
`experiments/ledger.md` 第 472–515 行（09-05 全部裁定与读数）。
`docs/研究计划书_v0.2_2026-09-04.md` 仍是论文骨架，但其 `:663` 的收缩区间**已撤回**，引用前先看 `ledger.md:506`③。

**已定（09-05 新增，均发生在解封之前、未读任何确认数据）**：
- **A-1 年代分解采纳**：折 05–35 内**一次**切分 2005–2012 vs 2013–2020，切点 **2013-01-01 写死**，
  估计交付、无门槛、**A 层**（`ledger.md:506`①，落地 v4 §2.5.1 = H2-era）。
- **B-1 ICT P1 → H6**：「扫流动性后收回」作探索性估计交付，Q1 属 A 层边界、Q2(b) 属 B 层只报数，
  不进 H1–H4 判定、不作部署依据、与解封同刻读取（`ledger.md:506`②，v4 §2.8；编号避开已占用的 H5）。
- **D 收缩点预测改口径**：主预测 = 实验 6 由**登记簿实测 haircut** 张成的 **FT [0.0088812, 0.0182250]**，
  ZS 平行区间 **[0.0080529, 0.0170925]**（v4 §2.3.2）；09-04 的 [0.0149, 0.0166] 降为敏感性预测保留披露；
  研究计划书 v0.2:663 的 [0.0102, 0.0208] **撤回**（`ledger.md:506`③，冲突登记见 `:503`）。
- **D2 部署线传导 haircut**：判据由 `C ≤ BE_dev×0.75 − 6bp` 改为 `C ≤ BE_dev×(1−h) − 6bp`，
  **h = 0.42**（实验 6 IC 端 N_high=148 三档 haircut 区间 [0.399, 0.440] 的中点，两端作敏感性）→
  **FT ≤ 7.28bp / ZS ≤ 3.51bp**（旧线 11.18 / 6.30；`BE_dev` 取 `ledger.md:429` NT=5 口径 22.9 / 16.4bp）。
  **ZS 新线已低于 C_stop 4bp，此事实已在 v4 披露**（`ledger.md:512`②、`:515`）。
- **C-1 H1b 进确认集**：以**估计交付**进入，主规格 **S-TH-ind 先验固定、不得按结果更换**，
  31 折 MDE80 ≈ **4.835 %/年**（NT=6 口径外推，**须披露与 NT=5 的口径差**），无 SESOI、不设门、不进 H1 判定；
  S-T / S-H 作次规格并列报（`ledger.md:512`③，前置补算 `:509`）。
- **实验 6（Deflated Sharpe / haircut，估计交付）**：登记簿快照 `f27c0b83…`/250764 字节/484 行 → `eval` 94 条、
  `ablation-read` 54 条 ⇒ **N_low=94 / N_high=148**；开发折 FT 年化夏普 0.85989、`t=1.569`；
  **DSR = 0.0657(N=94) / 0.0444(N=148)**，且**即便 N=1、零缩水 DSR 也只有 0.9430** —— 门槛不过与 N 无关，须原样披露；
  IC 端 haircut：N=94 为 38.8–49.8%、**N=148 为 39.9–44.0%**（后者即 h 的来源）；夏普端三档全部封顶 haircut 100%
  （`ledger.md:499`，计数与披露要点见 `:501`）。
- **ICT 六概念探针（探索性，已收口）**：主块 fold36–42（7 折）**24 个检验 0 个 |t|≥2**，MDE 17–64bp（中位 25bp）
  全部 > SESOI 10bp ⇒ 按措辞强制「**在本样本量下这 24 个问题都不可回答**」；次块 fold01–04 仅 4 折、不与主块并列。
  六概念中唯一值得再看的是 **P1**，已升格为 H6；其余五个在本宇宙 × 6 日持有 × t+1 开盘执行下无可用读数
  （`ledger.md:472` 预注册、`:473` 读数）。
- **杠杆归「资本层」**：作对未杠杆逐日收益的确定性后处理（融资利率、Reg T、维持保证金与强平路径），
  水平**按预注册先验规则定、不在回测上挑**（B 层）；长多头加杠杆放大的主要是 beta 而非 alpha、
  融资成本量级远大于 alpha ⇒ **产品 v1 默认 L ≤ 1**，待用户看过资本层测算后书面裁定（`ledger.md:485`②）。

**代码状态**：v4 冻结与两条 09-05 登记已提交（`f4279cc`）；`scripts/preregistration_manifest.py` 的固定文件清单
已加入 v4 与 `ict_pattern_probe_prereg_v1.md`，`test_prereg_manifest` 18 passed（`ledger.md:515`）；
`scripts/ict_pattern_probe.py` + `tests/test_ict_patterns.py`（24 项含前视断言）已提交。
**分支 `phase5-ablations-and-monetization` 领先 origin 7 个提交**——最后一次推送止于 `93761a3`（`ledger.md:514`），
v4 冻结之后的提交**尚未推**。
**工作树未提交（09-05 晚快照；两个后台 agent 仍在写，这份清单随时在变，提交前重跑 `git status`）**：`HANDOFF.md`（本次改）、
`docs/HANDOFF_archive_0826-0904.md`、`docs/archive/README.md`（09-04 结构整理）、`README.md`、
`src/crsp_pipeline/sealed.py`、`third_party/kronos`；**R-build 在制品** `src/unseal/`、
`scripts/exp11_spanning_extended.py`、`scripts/ict_pattern_probe.py`、`src/signals/kronos_adapter.py`；
**树基线在制品** `scripts/build_jkp_state_snapshot.py`、`scripts/gbdt_baseline.py`、`scripts/gbdt_sealed_smoke.py`、
`configs/gbdt_strong_v2_sealed.json`、`tests/test_gbdt_sealed.py`。
**多会话并行中，一律用精确路径 `git add`，禁止 `git add -A`**（`CLAUDE.md` §七）。

**进行中（三个 opus 后台任务 + 一条 GPU 队列）**：
1. **R-build**（解封读取工具链，只用合成数据、**一个字节封存分数都不读**）：`src/unseal/`
   已有 `config/folds/paths/perfold/aggregate`；入口 `scripts/unseal_read_confirm.py` 与
   `scripts/compute_labels_for_sealed.py` 尚未出现。任务书 `docs/解封读取_任务书_2026-09-05.md` §1（缺口 G0–G8，G5 除外）。
2. **树基线 XGBoost 31 折封存计算**（授权 `ledger.md:513`）：只跑 XGBoost 主口径，冻结配置超参 / seeds / 内层选参
   一字不改、只扩窗口；已产出 `scripts/build_jkp_state_snapshot.py`（JKP 派生快照 2002–2020 重建），
   `outputs/gbdt_strong_jkp_v2/` 在写。**每折只产 scores + 哨兵 + `SEALED_MANIFEST.json`，不算任何指标**，
   登记簿只允许追加不含指标的 `sealed-compute` 行。
3. **本任务**：HANDOFF §12.0 改写。
4. **实验 3 sc40 队列**（GPU，与解封无关）：`ledger.md:471` 记「第二轮进行中」、`:474` 有 fold36 读数；
   `outputs/exp3_queue_logs/` 最后写于 09-05 01:52（`sc40_fold39`），该轮无 `QUEUE.DONE`，**终态未确认**。

**其它待办（不在解封链上）**：小试协议 v2（单臂、NT=5、官方竞价印价基准、Alpaca 606 路由追问，依赖用户 AUM）；
v4 §8.2 唯一未闭合的披露项 **#3**（H3 候选 SESOI 0.0072 / 0.0108 的**取值理由**未核，不阻塞，引用时须写明）；
`HANDOFF.md:<行号>` 引用**全仓 84 处、6 个文件、49 个行号 > 124**，全部指向已归档正文，须逐条重定位
（旧 140 → 归档 115 有 25 行偏移，**不可批量替换**，`ledger.md:505`①）。

**Compustat 到位 + exp13 已完成（09-05，不在解封链上）**：合作者经 WRDS 提供的年度 / 季度宽表在 `F:\quant\external\compustat\received_20260905\`，只读验收通过、两个 ZIP 哈希已独立复核（`ledger.md` 09-05 `data-receipt`）。**无 CCM 链接表**，exp13 暂用 header CUSIP 接 CRSP（`ledger.md` 09-05 `DECISION` + `[exp13]` 六条 + `review`）。
**开发折读数（36–42，方向性证据，估计交付、不判定）**：v4 §2.4 两个缺口补做后 S-TH-ind 的 alpha **未被解释掉**（+6.76 → +6.93%/年，6/7 折，NT=6 口径），`ea_prox` 载荷 −0.106（CI 不覆盖 0）；财报日二阶矩 **3.38×**（7/7 折），含财报日的 6 日窗占 11.9% 却承担 20.6% 方差，均值差 CI [−23, +11] bp ⇒ **「财报窗均值是否更差」在本样本量下不可回答**，「不规避财报」维持 09-03 决定。
**v4 §2.4 的缺口措辞原样保留、不改冻结协议。** 待办：向合作者要 `crsp.ccmxpf_lnkhist` 与原 WRDS 查询回执，到手后 exp13 §3.3 整节替换重跑。任务书 `docs/任务书_exp13_Compustat_开发折诊断_2026-09-05.md`（sha256 `f8d1ec2b…`，已冻结）；产物 `outputs/exp13_compustat_dev_diag/`（gitignored）；派生表 `F:\quant\external\compustat\derived\`（不进 git）。

**财报日回避规则（Rule E）已裁定采纳（09-05 用户裁定，B 层设计假设，不是数据发现）**：实盘构造加进场过滤
「预期下次公告日 ±1 日历日落在 [t+1, t+6] 则不新建仓、名额递补；已持仓不强制退出」，定义一次写死于
`docs/设计_财报日回避规则_2026-09-05.md` §2，**此后不得在回测上调**。理由是方差偏好（D3：财报日二阶矩 3.38×、
含财报日的窗占 11.9% 承担 20.6% 方差），**不主张改善收益**（均值侧不可回答；文献公告溢价方向相反；策略本已偏离临近公告者，
助手已指出，用户维持裁定；`ledger.md` 09-05 第二条 `DECISION`）。**不进 v4、不进释放清单、不在解封时计算**；写入小试协议 v2 作固定条款。
待办：① exp14 开发折估计（折 36–42、NT=5 冻结管道 + Rule E，估计交付，**待用户启动**）；
② 实盘前瞻日历需公开源（SEC 8-K Item 2.02 + 公司确认日期），回测只能用 Compustat `rdq_hat` 代理，**未建，上线前置项**。

**解封前置清单（v4 §7 十一条，逐条状态）**：

| # | 内容 | 状态 |
|---|---|---|
| 1 | 确认运行的配置哈希 / 代码快照 / 唯一输出目录 | ⏳ 由 R-build 交付（任务书 §0.2） |
| 2 | 确认脚本无分年 / 分档 / 候选循环（唯一例外 A-1 那一次年代二分） | ⏳ 同上，交付时核 |
| 3 | 先用**合成假数据**跑通、不读折 05–35 | ⏳ R-build `--smoke` |
| 4 | 锁定停止规则（只修工程错误、不据部分结果改统计设计） | ✅ v4 冻结即生效 |
| 5 | 用户单独授权才可打开折 05–35 | ❌ 等同第 9 条；**计算授权 ≠ 读取授权** |
| 6 | E/S 的 `MDE80 > SESOI` 如何处置 | ✅ 已闭合，改为披露项（`ledger.md:433`） |
| 7 | **第二章外部时间戳**（v4 冻结后重生成清单并 `ots stamp`） | ❌ **未做**（第一章已完成 ✅：区块 965321/965333/965361、最早 2026-09-03 13:41:37 UTC、merkle 6/6 匹配、对 tag 检出 `--verify` 76/76 unchanged，`ledger.md:484`） |
| 8 | 签名 tag 推公开仓库 | ⚠️ 仓库公开 ✅（`ledger.md:514`）；**v4 冻结版 tag 未生成未推 ❌**；本机无 GPG ⇒ 须显式登记「放弃签名、只依赖 OTS」❌ |
| 9 | 用户对**冻结版**的书面确认（须逐项覆盖 (a) 折 05–35 + fold44–45、(b) §3 释放清单含 H2-era/H6/H1b/D2、(c) ZS 平行区间 `[0.0080529, 0.0170925]` 过目、(d) 一次性不可重开） | ❌ **未做**。「开始解封吧」只授权走前置流程，**不是读取授权** |
| 10 | 每个终点都有已登记的 MDE 与层级 | ✅ v4 冻结时全部满足（§7 表） |
| 11 | 一次性、同刻读取（E/H1/H1b/H2/H2-era/H3/H6 同刻；fold44–45 独立目录） | ⏳ R-read 时执行 |

**未完成部分的执行顺序（缺一不得往下走）**：
① 本节更新（本次）→ ② 精确路径 `git add` + 提交 → ③ `.venv\Scripts\python.exe scripts\preregistration_manifest.py`
重生成清单 → **第二章 `ots stamp`**（§7 第 7 条）→ ④ 对 v4 冻结版打 tag 并**推公开仓库**，同时登记「放弃 GPG 签名、
只依赖 OTS」（§7 第 8 条；`--verify` **必须对打了 tag 的那个 commit 的检出跑，不得对工作区跑**）→
⑤ **树基线 31 折封存计算完成后再生成一次清单并盖章（第二章 b）**——新增的 31 个 `SEALED_MANIFEST.json` 会改变清单内容，
不重盖则「树基线在解封前已算完」这一事实落在时间戳之外 → ⑥ 用户对冻结版 v4 的一句书面「**读**」 →
⑦ R-read **单命令一次读取**：`.venv\Scripts\python.exe scripts\unseal_read_confirm.py --out outputs\unseal_<UTC>`
（H1 / H1b / H2 / H2-era / H3 / H6 / E 同刻，fold44–45 独立输出目录、不并入 31 折合并统计）。

**不要做**：
- 读任何 `eval_sealed_*` / 折 05–35 / ≥2024-01-01 的**分数、标签、指标**（`CLAUDE.md` §四）；
- 走 `evaluate_fold.py` 的**自动 `append_ledger`**（`evaluate_fold.py:356`）——它会把 **62 条逐折 IC** 写进 append-only
  登记簿，越过 v4 §3 释放清单，并把实验 6 的试验数 N 从 148 推高、**连带改动自己要兑现的预测区间**。登记条目由主会话手写；
- **计算或输出 FT−ZS 差值**（v4 §3）；两臂各自的量可报；
- 在任何报告里放**逐折 IC 表**——只报 31 折合并量 + 正折数；
- 在树基线上跑 **LightGBM / CatBoost**（授权只含 XGBoost 主口径，其余须披露不跑，`ledger.md:513`）；
- 改任何**冻结文件**（`confirmation_protocol_v4.md`、`ict_pattern_probe_prereg_v1.md`、`signal2_prereg_v2.md`、
  `configs/gbdt_strong_v2.json`）；修订须另起版本并注明修订时点；
- 在折 36–42 上比较构造或合成变体的收益；手动放宽退出线；为过功效关抬 SESOI；
- `git add -A`（两个后台 agent 的在制品正躺在工作树里）。

---

## 3. 环境与路径（未变）

| 东西 | 位置 |
|---|---|
| 代码 | `F:\quant\us-quant-pipeline`（git，分支 main，上游 `8a18419`） |
| 训练 venv | `.venv`（**不要装 wrds / lightgbm**） |
| 树模型 venv | `.venv-gbdt` |
| 下载器 venv | `downloader\.venv-dl` |
| 整理后面板 | `F:\quant\processed\crsp_ciz_2026-08-24_20260825T130601Z\` |
| JKP 外部因子 | `F:\quant\external\jkp\`（CC BY-NC **禁商用**） |
| 登记簿 | `experiments\ledger.md`（append-only，**权威流水账**） |
| 强制规则 | `CLAUDE.md`（每会话必读，四类禁止错误） |
| OpenTimestamps | `downloader\.venv-dl\Scripts\ots.exe`（`stamp`/`upgrade`/`verify` 都在这里；启动前须做下表「坑」的 `ssl.dll` 处置） |

折边界（42 折，oos_start=2024-01-01）：fold01 训 2000-01-03..2002-12-20 /
验 2003-01-02..2003-06-30，此后每折整体前滚 6 个月。
用 `crsp_pipeline.splits.walk_forward_folds` 生成。

```bash
.venv\Scripts\python.exe -m pytest -q
```

## 2. 当前最好的诚实读数（近代七折 = 已消耗的开发折）

| 指标 | 值 | 备注 |
|---|---|---|
| 逐日 RankIC | **+0.0207**（NW t=3.63） | 七折全正；SPA_c p=0.0008（仅覆盖 4 配置，见 §4） |
| 零样本 RankIC | +0.0192 | 微调只贡献 +7.9%，且仅 4/7 折更好 |
| 毛纯多超额 | **+10.48%/年** | 扣成本前 |
| 净纯多超额 | **+6.66%/年**（8bp，正确口径） | 现行脚本报 +7.00% |
| 波动 / 夏普 | 9.20% / 0.72 | |
| 最大回撤 / 最长水下 | −12.3% / 359 日 | 两者都正好落在夏普 0.72 的期望上，**不构成独立负面证据** |
| 日收益 NW t | **+1.32** | 策略本身不显著——3.3 年的 t 天花板就是 1.3 |
| breakeven 单边成本 | **21.9bp** | 日均多头腿换手 0.569 |
| K6b alpha（8 因子+市场） | +9.85%，保留 **141%**，6/7 折 | 判「非翻版」 |

**同模态基线**（26 个价量特征）：XGBoost +0.00628(t=0.69)、CatBoost +0.00584、
LightGBM +0.00239 —— Kronos 打赢一个量级。**（09-03 更正）** Rahimikia et al. (2025) 支持的是「金融原生预训练 > 通用预训练」，但其从头预训练的 TSFM 拟合优度**仍不及树集成**，且**该文未评测 Kronos**——「Kronos 属第三档」在该文献中无对应实验，此前表述有误（见用户 09-03 文献综述 §5）。本项目「Kronos 零样本 > 同模态树基线」是本项目自己的读数，且树基线为 3 年窗训练（数据饥饿），公平基线见研究计划书 H3。

## 8. 坑（都踩过，未变）

| 坑 | 处理 |
|---|---|
| 装 `wrds` 会把训练 venv 的 pandas 降级 | 下载器用独立 venv |
| 训练中 CUDA OOM | 原命令重跑即自动续训 |
| 并发 CUDA 进程可共存，**但强杀其一会引发驱动崩溃** | 等它跑完，别 kill |
| 两个 GPU 评估同时跑会把单折从 19 分钟拖到 3 小时 | 排队，别并发 |
| PowerShell 传 `-c "..."` 引号被吃 | 写成脚本文件再跑 |
| `.ps1` 存成无 BOM 的 UTF-8 | PS 5.1 把中文注释解析成语法错误，必须带 BOM |
| 消融/粗筛期间改 batch size 或精度 | 会改变 RNG 消耗 → 采样路径变化 |
| 训练无声退出无 traceback | 查 `Get-WinEvent ... nvlddmkm`（Event 153 = GPU 复位） |
| Windows 下 python 默认 GBK 编码 | 读写 UTF-8 文件一律显式 `encoding='utf-8'` |
| `ots.exe` 启动即崩：python-bitcoinlib `find_library('ssl')` 返回 None | 把 CPython 自带 `libcrypto-3.dll` 复制到 scratch 目录改名 `ssl.dll`，**只对当前进程**前置 PATH 后再调；`upgrade`/`verify` 同样需要（`ledger.md:484`） |

## 附录：历史章节索引（全部在 `docs/HANDOFF_archive_0826-0904.md`）

原文一字未改、原顺序排列。「状态」列是**原文件头自己的判定**，不是本次整理新加的。

| 节 | 日期 | 内容 | 状态 |
|---|---|---|---|
| §1 / §1.1 | 08-31 | 一句话现状 + 需要人拍板的三件事 | 已被 §12.0 取代 |
| §4 | 08-29..31 | 三天结论压缩版：模型层无低垂果实、η/传导率/σ_cs、**A.1 跨年代断裂**、K6b 张成、成本主战场、方法学教训 | 证据仍有效，逐条见登记簿 |
| §5 | 08-31 | 确认集怎么用（主终点建议用 MR 单调性） | **已被推翻**（原文件头自陈） |
| §6 | 08-31 | 七项不消耗确认集的实验清单 | 第 2、5、7 项**已被推翻**；其余部分已做 |
| §7 | 08-31 | 当天新增脚本清单（k6/k6b/bootstrap/optimal_holding） | 已过期，脚本仍在 `scripts/` |
| §9 | 09-01 | 增量，覆盖上文已过期的确认集建议 | 前两条**已被 §10 覆盖** |
| §10（含 10.1–10.8） | 09-01 | 深夜收尾：两条新强制规则、待裁定两件事、当天实验、三个硬结论、权威文件、不要做的事 | §10.3 两件事**已在 §11 拍板**；标题里的「新会话从这一节开始读」已过期 |
| §11（含 11.1–11.8） | 09-02 | 六项裁定、**K13 机械订单审计**、小试设计参数、**券商 Alpaca（11.4a）**、反向单 403（11.4b）、期限序贯（11.4c）、执行顺序、**K15 市场侧结构序列（11.7）**、封存队列启动（11.8） | 大部分仍有效，但 §6 第 7 项被 §11.3 推翻；标题里的「新会话从这一节开始读」已过期 |
| §12（含 12.1–12.11） | 09-03 | 思路整理与三份调研、深夜自主工作新增四个包、**待用户裁定项（12.2）**、构造修订待登记（12.6）、文献综述影响（12.9）、模拟审稿（12.11） | 多数已被 §12.0 收口；12.2/12.3 里未打勾的仍是待办 |

**查阅提示**：归档文件里章节号与本表一致，`grep -n '^## '` 一眼定位。
需要 Alpaca 券商细节看 §11.4a–11.4c，需要 A.1 跨年代断裂的原始读数看 §4，
需要封存队列口径看 §11.8。
