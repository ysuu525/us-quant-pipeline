# 交接文档（最后更新 2026-09-02）

> **新会话请先读 §11，再回头看需要的章节。**
> §1–§8 是 08-31 的状态，其中 §5（确认集主终点建议用 MR 单调性）与
> §6 第 2、5 项**已被推翻**，§9 的前两条已被 §10 覆盖。
> §10 是 09-01 的状态，其 §10.3 的两件待裁定事项**已在 §11 拍板**，
> §6 第 7 项的「500 笔 / 约 6 周」**已被 §11.3 的机械审计推翻**。
> **§11 是当前状态。**
>
> 权威细节在 `experiments/ledger.md`（append-only 流水账）与
> `experiments/confirmation_protocol_v4_revisions.md`（开工即用的待改项）。
> `CLAUDE.md` 每会话必读，09-01 新增两条强制规则。

---

## 1. 一句话现状

**信号层已基本立住，钱的路径没打通。** 近代七折（fold36–42，832 日）RankIC
+0.0207（NW t=3.63、七折全正、Hansen SPA p=0.0008），零样本就拿到其中 93%；
K6b 张成检验判「非翻版」。但**同一构造在 2003–04 的毛超额只有 +0.83%/年**
（近代 +10.48%），与成本假设无关——**传导在跨年代上是断的**。

### 1.1 需要人拍板的三件事

1. **确认集（折 05–35）到底跑不跑、主终点是什么。** 见 §5。
2. **持仓期 T 的候选集**：解析最优 T\*=3~5（bootstrap 中 T=6 只占 4%），
   但 T=4 vs T=6 的比较不显著（P=0.84）。建议冻结时把 T=4 与 T=6 并列预注册，
   **不要**凭已消耗折的数据直接改。
3. **成本口径**：现行 `analysis_sevenfold.py` 对纯多线用「两腿平均换手」，
   略微少扣。正确纯多口径下净超额 +6.66%（现报 +7.00%）、breakeven **21.9bp**
   （现报 24.1bp）。§4.1 停机规则填数应改用 21.9bp。

## 2. 当前最好的诚实读数（近代七折 = 已消耗的开发折）

| 指标 | 值 | 备注 |
|---|---|---|
| 逐日 RankIC | **+0.0207**（NW t=3.63） | 七折全正；SPA_c p=0.0008（仅覆盖 4 配置，见 §4） |
| 零样本 RankIC | +0.0192 | 微调只贡献 +7.9%，且仅 4/7 折更好 |
| 毛纯多超额 | **+10.48%/年** | 扣成本前 |
| 净纯多超额 | **+6.66%/年**（8bp，正确口径） | 现行脚本报 +7.00% |
| 波动 / 夏普 | 9.20% / 0.72 | |
| 最大回撤 / 最长水下 | −12.3% / 359 日 | 两者都正好落在夏普 0.72 的期望上，**不构成独立负面证据** |
| 日收益 NW t | **+1.32** | 策略本身不显著——3.3 年的 t 天花板就是 1.3 |
| breakeven 单边成本 | **21.9bp** | 日均多头腿换手 0.569 |
| K6b alpha（8 因子+市场） | +9.85%，保留 **141%**，6/7 折 | 判「非翻版」 |

**同模态基线**（26 个价量特征）：XGBoost +0.00628(t=0.69)、CatBoost +0.00584、
LightGBM +0.00239 —— Kronos 打赢一个量级。这与 Rahimikia et al. (2025) 的三档
结论一致：通用 TSFM 弱于树集成，**金融原生预训练**才有效，而 Kronos 属第三档。

## 3. 环境与路径（未变）

| 东西 | 位置 |
|---|---|
| 代码 | `F:\quant\us-quant-pipeline`（git，分支 main，上游 `8a18419`） |
| 训练 venv | `.venv`（**不要装 wrds / lightgbm**） |
| 树模型 venv | `.venv-gbdt` |
| 下载器 venv | `downloader\.venv-dl` |
| 整理后面板 | `F:\quant\processed\crsp_ciz_2026-08-24_20260825T130601Z\` |
| JKP 外部因子 | `F:\quant\external\jkp\`（CC BY-NC **禁商用**） |
| 登记簿 | `experiments\ledger.md`（append-only，**权威流水账**） |
| 强制规则 | `CLAUDE.md`（每会话必读，四类禁止错误） |

折边界（42 折，oos_start=2024-01-01）：fold01 训 2000-01-03..2002-12-20 /
验 2003-01-02..2003-06-30，此后每折整体前滚 6 个月。
用 `crsp_pipeline.splits.walk_forward_folds` 生成。

```bash
.venv\Scripts\python.exe -m pytest -q
```

## 4. 2026-08-29..31 的结论（压缩版，逐条见登记簿）

**模型层已无低垂果实**
- 微调贡献 <8% 且 4/7 折；训练 1 轮 ≈ 30 轮（判「不可分」）；
- 冻结主干 + 线性探针（513 参数，诚实内层选参）+0.0126、MLP 头（66k）+0.0075
  —— **凡是从本项目数据估的新映射，都输给预训练的解码链当固定映射用**；
- `sample_count` 5→20：**五读数平均 +4.9%，4/5 为正**（fold36 只 +0.7%）。
  机制上是纯方差削减、零自由参数，但幅度应按 **+5%** 而非论文的 +9.7% 计入预期；
- 便宜版截面上下文探针（手工读出 + 8 个截面特征 + 岭）：五折 +11.6%、3/5，
  判「边际」—— **功效不足，值得扩到七折再判**（见 §6）。

**钱的链条**（全部在近代七折上实测）
- η（IC → 毛十分位价差的无截距斜率）= **0.902，95% CI [0.813, 0.960]**。
  乘子是稳的。桥公式 `毛十分位价差 ≈ 3.51 × IC × σ_cs` 成立；
- 传导率（隐含年毛 → 实际净超额）≈ **0.46**，corr=0.92。
  Clarke–de Silva–Thorley 的 transfer coefficient 实务区间是 0.3–0.8，
  **0.46 是正常的长纯多约束代价，不是可修的低效**；
- **σ_cs 年化趋势 −0.396pp/年，CI [−0.630, −0.198]（相对 −7.2%/年）**。
  按桥公式这是对前瞻毛收益的直接逆风。替代解释未排除：窗口起点 2020H2 是
  疫情后离散度尖峰，可能是回归正常而非长期趋势。

**⚠ A.1：跨年代断裂（当前最大的负面证据）**
- 折01–04（2003–04）**毛**纯多超额 = **+0.83%/年**（成本=0），breakeven **3.4bp**；
  近代 +10.48%、breakeven 21.9bp。换手几乎相同（0.582 vs 0.569）；
- **所以 A.1 不是「拿 2024 年成本回测 2003 年」的产物**——零成本下也几乎不赚钱；
- 断裂定位：限制到 top500 交易池时，IC 在 2003–04 **掉 35%**（+0.0178→+0.0116），
  近代 **涨 25%**（+0.0207→+0.0258）；σ_cs 4.38% vs 5.47%；形状系数 1.03 vs 1.45；
- 十分位曲线（相对池均值 bp，最低分档在左）：
  - 2003–04：`+3.5 +1.0 −6.3 +1.1 −6.7 −3.3 −1.6 +3.0 +0.2 +9.2` ← **非单调、两端皆正**
  - 2020H2–23：`−29.7 −15.0 −7.5 −3.5 −5.7 +3.8 +2.3 +3.9 +15.4 +36.0` ← 单调
- **两个未区分的解释**：(a) 基座适配现代微结构、早期不适用 → 确认集大概率失败；
  (b) 2003–04 是全样本最糟的一次训练-验证机制错配（训 2000–02 崩盘 / 验 2003–04
  暴力反弹 + 垃圾股行情）→ 属单一异常。**区分只能靠确认集。**

**K6b 张成检验（判据先于结果）**
- 控制集 = 1日/5日反转 + mom_skip(t−24..t−5) + mom_12_1 + Amihud + 成交量冲击
  + 特质波动 + 相对价差代理，全部走与策略逐行相同的管道、多空价差腿、毛收益；
- **S1b 保留 141%（t=2.24, 6/7）、S2b 126%、S3b 110% → 判「非翻版」**；
- 策略在**短期动量**侧（mom_skip β=+0.07, t=+3.6；rev1 β=−0.12），不是反转/流动性
  提供侧。文献一致：短期动量在大盘 0.62%/月，且随换手上升反转让位于动量；
- **⚠ 必须同时记住**：保留率 >100% 的机制是策略正向暴露于**本样本内亏钱**的因子
  （mom_skip −6.28%、amihud −11.20%、idiovol −10.70%、hlrange −11.45%，
  相关 +0.21~+0.33）。**换个这些因子赚钱的年代，同样的正载荷会变成净拖累**——
  这很可能就是 A.1 的机制，尚未直接检验（见 §6 第 1 项）。

**成本是主战场**
- 策略在 top500 池内系统性偏向低价/不流动/高波动一端
  （corr(1/价格)=+0.382、Amihud +0.326、特质波动 +0.317、价差代理 +0.291）；
- **含义：全池统一 8bp 低估了真实成本**，持仓落在该池最贵的尾部。

**方法学**
- 本会话两次把噪声读成结构（η 的比值假象、K6 一致性门槛用逐折独立回归），
  共同机制是**在 n=7 上做推断而手上有 832 天**。现已有
  `scripts/bootstrap_errorbars.py`，新结论一律先装误差棒；
- 一致性门槛的正确写法是「**固定合并回归载荷后的逐折残差**」，
  不是「逐折独立回归」（后者 7 回归元配 119 天、重度过拟合）；
- Hansen SPA_c p=0.0008，但**只覆盖 4 个配置**（lb90/lb200/零样本/e001）——
  universe、持有期、执行时点、合成、正则那些试验没有逐日 IC 产物，
  **不构成整个多重检验问题的答案**；
- **预训练污染**：Kronos 语料 45 交易所 120 亿 K 线、截至 2024-06 且含美股，
  **折 01–42 全部在语料窗口内**。2025–26 已有针对性文献
  （TSFMAudit arXiv 2605.26161；arXiv 2510.13654，记录 0.1% 重叠即可使误差
  被低估 8–29pp），推荐标准是「严格的全局 post-training 测试期」。
  **含义：折 05–35 的通过是弱证据、不通过是强证据；唯一干净的是封存窗 2024-07 起。**

## 5. 未决的主要决定：确认集怎么用

**资源清点**：折 05–35（31 折，15.5 年，2005–2020，含 2008 与 2020Q1）**完全未碰**；
封存窗 2024-07..2025-12 未碰。折 01–04 与 36–42 已消耗（数十次决策读数，
选择偏差粗估 +0.002~0.003，但对抗性复核认为应为 +0.004~0.006 即 20–28%）。

**建议（未拍板）**：
- **主终点改为十分位曲线的单调性**（Patton-Timmermann MR 检验，R 包 `monotonicity`）。
  理由：2003–04 真正坏掉的就是它；估计量来自约 500 名字 × 约 3900 天，功效远高于
  折级收益；**对成本模型完全不敏感**（毛的量），因而失败可归因——
  而固定 8bp 的净超额在 2005–2020 是已知错误的模型，失败无法归因；
- 次终点 = 年代适配成本下的净超额；RankIC 降为诊断；
- **预注册其解释力上限**（见 §4 污染条）；
- 候选配置 ≤3（Bonferroni t 门槛 2.39），把 T=4 / T=6 并列写进去；
- **动确认集之前先做 §6 的第 1、2 项**——它们可能直接改变要不要跑。

## 6. 还能跑、且不消耗确认集的实验（按性价比排序）

1. **折 01–04 上的因子年代对照**（纯 CPU，半天）。把 K6b 的 8 个控制因子在
   2003–04 上跑一遍，看 amihud / idiovol / mom_skip 那时是不是**赚钱**的。
   若是，则 A.1 = 「策略的因子倾斜在近代是顺风、在 2003–04 是逆风」，
   **A.1 的机制就此闭合**，且它不再是「信号不外推」的证据。
   顺带把 K6b 张成回归整套套到折 01–04，看 alpha 在那里是正是负。
   **这是当前最高性价比的一次计算。**
2. **拟议主终点的功效分析**（纯 CPU，1 天）。用近代七折实测的十分位曲线与噪声
   做参数自助，问：MR 检验在 31 折上能以多大功效分辨「单调」与「2003–04 那种
   非单调」？**必须在花掉确认集之前知道**，否则是预注册一个无信息的实验。
3. **EDGE 有效价差估计器**（Ardia-Guidotti-Kroencke 2024，闭式、只需 OHLC，
   约 50 行，纯 CPU）。给 2000–2025 全样本逐股逐月的有效价差 → 同时解决
   「年代适配」与「截面适配」两个成本问题。配合按持仓实际流动性分位计成本，
   替换全池统一 8bp。**成本是主战场，这是最实的一步。**
   （禁用 Corwin-Schultz：大市值相关性仅 18%。Hasbrouck Gibbs 可作交叉校验。）
4. **容量分析**（纯 CPU，2 小时）。按持仓的实际 ADV 与参与率，算 8bp 在多大
   AUM 上还成立。这个数决定「一个 alpha 分量」的定位在经济上是否成立，
   **至今一次都没算过**。
5. **截面上下文探针扩到七折**（纯 CPU，半天）。现为五折 +11.6%、3/5、判「边际」，
   功效不足。这正是 CLAUDE.md §三 那条教训适用的场合（只是方向可能相反）。
6. **补齐各配置的逐日 IC 产物**，让 SPA 覆盖全部试验族而不是 4 个（GPU，1–2 天）。
7. **小额真钱 500 笔成交**（约 6 周）。**唯一产生统计资本而非消耗它的实验**，
   且直接钉死成本。paper trading 只能验管道、对成本完全无效。

**明确不建议**：从零训练截面 Transformer/GNN/learning-to-rank（本项目四个数据点
显示凡是从自己数据估的新映射都更差，且确认集只有一个、试验预算已透支）；
继续加微调轮数或搜学习率；在开发折上继续做模型变体 A/B（每多一次读数都在贬值证据）。

## 7. 今天新增的脚本

| 脚本 | 作用 |
|---|---|
| `scripts/k6_spanning.py` | K6 张成检验（6 因子），判「不可判定」 |
| `scripts/k6b_spanning.py` | K6b（+2 个动量、修正的一致性门槛），判「非翻版」 |
| `scripts/bootstrap_errorbars.py` | stationary bootstrap + Hansen SPA_c |
| `scripts/optimal_holding.py` | 持仓期的解析路线（e(h) × turn(T)） |

**全部未提交**（`git status`：4 个未跟踪脚本 + `ledger.md` 已改）。
提交前注意 CLAUDE.md §七：多会话并行时用精确路径 `git add`，不要 `git add -A`。

## 8. 坑（都踩过，未变）

| 坑 | 处理 |
|---|---|
| 装 `wrds` 会把训练 venv 的 pandas 降级 | 下载器用独立 venv |
| 训练中 CUDA OOM | 原命令重跑即自动续训 |
| 并发 CUDA 进程可共存，**但强杀其一会引发驱动崩溃** | 等它跑完，别 kill |
| 两个 GPU 评估同时跑会把单折从 19 分钟拖到 3 小时 | 排队，别并发 |
| PowerShell 传 `-c "..."` 引号被吃 | 写成脚本文件再跑 |
| `.ps1` 存成无 BOM 的 UTF-8 | PS 5.1 把中文注释解析成语法错误，必须带 BOM |
| 消融/粗筛期间改 batch size 或精度 | 会改变 RNG 消耗 → 采样路径变化 |
| 训练无声退出无 traceback | 查 `Get-WinEvent ... nvlddmkm`（Event 153 = GPU 复位） |
| Windows 下 python 默认 GBK 编码 | 读写 UTF-8 文件一律显式 `encoding='utf-8'` |

## 9. 2026-09-01 增量（本节覆盖上文已过期的确认集建议）

- **确认配置当前拟只留 FT/lb90 一个，但这不是FT显著优于ZS的结论。** K11 用冻结钱管道与
  单标量 ΔADV 审计：FT 毛年化10.48%、BE 21.92bp、ΔADV +0.00517；ZS 为4.78%、
  13.57bp、+0.00204，未过事前半强度资格线；但两臂毛差与ΔADV差的配对区间均覆盖0。
  若只给一个名额，按开发窗预期经济效用选FT；k=1还是k=2若要统计地拍板，尚需比较
  “至少一个预注册配置成功”的联合功效，不能用FT−ZS差不显著或1.96→2.24单独代替。
- **确认主终点改为 E+S 双主张，不用 MR 单调性作主终点：** E=毛主动收益（5%/t1.96/
  18折联合门，成本只报固定网格与BE）；S=单标量 ΔADV，并加 top500 IC 正号门。二者不合成
  总 PASS，也禁止确认后查看年份/ADV档/替代 universe。
- **ΔADV 折门由21修正为18。** v3 直接联合模拟同一31折上的量级、CI、折数和符号门：
  k18 全/半强度功效约97.3%/49.1%，无放大误判约2.2%；k21 为89.8%/34.4%/1.2%。
  旧 v2 “21优于18”只比较折数门单项，已在 ledger 标废。
- **K10 解决纯多/对冲纠结：** invprice 暴露减半保留87.8%毛收益，invprice 收益 beta
  从+.127降至+.101（bootstrap差CI全负），但仍保留79.3%且t=4.64；即纯多约束只能部分
  削弱载荷，不能替代真实多空对冲。若没有明确风险预算，默认仍用q=1；对冲是另一产品。
- 精确 K6b 持仓倾斜：invprice/amihud/idiovol/hlrange 的实际六袖套暴露分别
  +.0275/+.0569/+.1162/+.1175；四项都非零，但只有 invprice 有强条件载荷，故先跑它。
- 当前权威协议：`experiments/confirmation_protocol_v3.md`。状态仍为**草案且未授权打开折05–35**；
  2024-07起封存窗仍未读。新增权威产物：`outputs/k10_invprice_frontier.json`、
  `outputs/k11_zs_ft_candidate_audit.json`、`outputs/confirm_power_v3_joint.json`。
- **K12（2026-09-01）覆盖本节前两条的配置结论并新增一个上游阻塞。** 按新 CLAUDE.md，
  FT vs ZS 是 B层，单臂先验应取上游默认 ZS。HAC修正版50万次 k=1/k=2 联合模拟：
  双臂族误判最大2.44%、选错单臂至少救回75.27pp，但“仅高波动ZS强”时 E 功效
  83.96%→76.96%，损失7.1pp超过事前5pp预算，故字面规则取单ZS。不过当前协议
  **仍不能启动**：单ZS的 E MDE80=9.826%>SESOI 5%，S的ΔADV MDE80=.00394>.0026；
  按强制功效规则两者均未过关。
  需用户在开折05–35前选择：实质性上调SESOI、降级相应结论为探索性、或等待更多前向数据。
  权威产物 `outputs/k12_confirmation_config_power.json`，脚本
  `scripts/k12_confirmation_config_power.py`，协议已标成“未过功效关”。

## 10. 2026-09-01 深夜收尾（**新会话从这一节开始读**）

### 10.1 一句话

确认集**暂不启动**：K12 发现单 FT / 单 ZS / 双配置在两个主终点上**全部 MDE > SESOI**，
按 `CLAUDE.md` 的强制功效关都不能作为确认性检验。阻塞点不在门槛定得高，
**在现代成本没测**——见 `experiments/confirmation_protocol_v4_revisions.md`。

### 10.2 今天新增的两条强制规则（都在 `CLAUDE.md` §二）

1. **「判据还必须先过功效关」**（08-31 加）：预注册须同时给 SESOI、
   考虑序列相关后的 MDE、目标功效；MDE > SESOI 时不得设计成确认性检验，
   可降级为探索性但**不得据其未过门槛得出否定结论**。
   代价是 K7a 的一次空跑（门槛 0.007，MDE 0.024）。
2. **「比较分层 A/B」**（09-01 加）：实测本项目臂间比较**结构性不可分辨**——
   正交臂（ZS vs FT）的 99% 淘汰门槛是信号本身的 1.10–1.51 倍，**永不触发**；
   相关臂需差出信号的 45–66%。分辨 50%/30%/20% 的相对臂间差分别需 20/55/124 年。
   故：**A 层（MDE≤SESOI）做推断，统计资本全花在此；
   B 层（MDE>SESOI）按写死的先验优先序决定，不做 CI 淘汰，选择记为披露的设计假设。**
   **措辞强制**：B 层结果不得写「不可分」，必须写
   「在本样本量下该问题不可回答，按预先规则取 X」。

   连带后果：**所有记作「不可分」的历史臂间判读都应按 B 层重读**
   （lookback 粗筛第一轮、微调 vs 零样本、e1 vs e30、lb90 vs lb200 按钱、
   K6 一致性门槛、K7a、K8、K11 的 FT vs ZS）。唯一达到 A 层强度的是训练池 A/B。

### 10.3 需要人裁定的两件事（都写在 v4 修订清单里）

1. **ZS 还是 FT。** B 层优先序内部平局：①上游默认/②参数最少/③算力最低 → **ZS**；
   ④对机制变化最稳健 → **FT**（BE 21.9bp vs 13.57bp，余量多 61%，而成本是最大未知量）。
   选 ZS 的期望代价经收缩后为 0.7–2.8%/年。规则没写平局怎么破，需裁定并登记。
2. **E 端是否改成估计口径**（BE ± CI 对比实测成本），从而取消对 SESOI 的依赖。
   若改，则**成本实测成为前置条件，运行顺序变为：先测成本 → 再冻结 → 再确认**。

### 10.4 今天做完的实验（全部不消耗折 05–35）

| 编号 | 内容 | 判读 |
|---|---|---|
| K6c | 折01-04 因子年代对照 | 主判据「部分」；因子倾斜**不能**解释 A.1，反而加重 |
| K7a | 折01-04 零样本反事实 | **功效不足、不可判读**（逐日配对 t=0.41，CI 同时盖 0 与门槛） |
| K7b | 年代×流动性传输审计 | **推翻「基座不适配早期」**；信号在流动性谱上位置翻转（DiD CI [+0.0034,+0.0191]） |
| K8 | ZS+FT 等权秩合成 | IC +28% 而 top-decile −16%；探索性，不采用 |
| K9 | ADV 成本-alpha 前沿 | 工程无误但**约束了唯一零倾斜的特征**，主结论不成立 |
| K10 | invprice 前沿 | 暴露减半保留 87.8% 毛收益，但 β 只从 +.127 降到 +.101（降 21%）——**持仓约束是弱杠杆** |
| K11 | ZS/FT 候选资格审计 | 两臂**四个量全部不可分辨**（毛 t=0.70） |
| K12 | 配置族联合功效 | **两臂两终点全部 MDE > SESOI，协议阻塞** |
| — | EDGE 有效价差 | 实现与官方逐位一致；**但现代 top500 上不可用**（55.2% 的 s2 为负） |
| — | 容量 | **不必立项**：$1M 下冲击 ≤1.3bp，$30–50M 才咬人 |

### 10.5 三个仍然站得住的硬结论

1. **A.1 的机制**：不是信号消失、不是因子倾斜、不是基座不适配早期，
   而是**信号在流动性谱上的位置迁移**（2003-04 长在不流动端，近代长在流动端）。
   近端机制已闭合，**远端成因未解释**。
2. **2003-04 在任何成本假设下不可投**：EDGE 给该期 top500 半价差约 20.3bp，
   而该期 breakeven 单边仅 1.7bp，差约 12 倍。与 K7b 独立同向。
3. **现代成本仍是最大未知量**：EDGE 失效、Chen-Velikov 公开数据不可用、TAQ 拿不到，
   只剩小额真钱约 500 笔实测。而 breakeven 是 21.9bp（FT）/ 13.57bp（ZS）。

### 10.6 权威文件

| 文件 | 作用 |
|---|---|
| `experiments/confirmation_protocol_v3.md` | 冻结记录：当时决定了什么 |
| `experiments/confirmation_protocol_v4_revisions.md` | **待改项与理由，开工即用** |
| `experiments/ledger.md` | append-only 流水账（约 300 行） |
| `CLAUDE.md` | 强制规则，**每会话必读**，今天新增两条 |
| `docs/现状与优化空间_2026-08-31.md` | 给外部模型的自足简报（部分已被 K7b 更新） |

### 10.7 今天新增的脚本

`k6b_spanning` `k6c_era` `k7a_paired` `k7a_eval` `k7b_adv_transfer` `k8_ensemble`
`k9_cost_alpha_frontier` `k10_invprice_frontier` `k11_zs_ft_candidate_audit`
`k12_confirmation_config_power` `edge_spread` `edge_cost_model` `bootstrap_errorbars`
`confirm_power` `confirm_power_v2` `optimal_holding` `holdings_tilt` `mde_audit`

**全部未提交。** 提交时用精确路径 `git add`，不要 `git add -A`（多会话并行）。

### 10.8 明确不要做的事

- 不要为了过功效关而机械上调 SESOI（v4 修订 1 有详细理由）；
- 不要在开发折上继续做模型变体 A/B——按新的 B 层规则，那类比较**不可回答**；
- 不要从零训练截面模型；不要继续加微调轮数；
- 不要在未获用户单独授权前打开折 05–35 或 2024-07 起封存窗。

---

## 11. 2026-09-02：六项裁定 + K13 机械订单审计（**新会话从这一节开始读**）

### 11.1 一句话

**协议的形状定了：历史折回答「信号是否跨年代存在」，现代前向数据回答「是否能赚钱」；
双臂保留，成本先行，C_stop=4bp 且绑定 AUM。** K13 机械订单审计推翻了
「小额真钱 500 笔 / 约 6 周」这个从未核过的数：**500 笔只需 9–12 个交易日**，
但**笔数从来不是约束，交易日数才是**——按日聚类后有效样本量等于交易日数。

### 11.2 六项裁定（已生效，未写入 ledger 正文，待小试协议定稿一并登记）

| # | 问题 | 裁定 | 理由 |
|---|---|---|---|
| 1 | 配置数 | **k=2，预注册 FT 与 ZS** | 推翻原「三项硬门取交集」，改用 **minimax-regret**：双臂最大后悔 **7.11pp**，单 FT **82.4pp**，单 ZS **96.6pp**。原决策函数把 7pp 与近 100pp 的机会损失当作同等未达标，是结构缺陷。登记时必须写成「看过 K12 后因预注册效用函数不能表达非对称机会损失而修订」，**不得伪装成原规则自然推出** |
| 2 | 历史确认集 E 端 | **降为估计交付 / 跨年代压力测试**，不作现代可投性的确认检验 | `CI_lower(BE−C)>0` 与「净年化 t>1.96」是**恒等**的（$BE_{low}>C \iff G-dC>1.96SE_G$），只是把人定的 5% 换成有经济来源的成本门槛：概念更正确，**统计功效没有增加**。且 k=2 后 FT 毛年化 MDE80 由 6.53% 升至 **7.19%**，任何现实成本都过不去 |
| 3 | C_stop | **4bp，只门控现代经济部署路径，不门控折 05–35，且必须绑定明确 AUM** | 由 $G_{80\%}\ge0.478C+6.533$ 与保守真毛 7.5–8.4% 反解得 C_stop≈2.0–3.9bp；4bp 是可解释的乐观上界，6bp 需事前相信真毛≥9.4%，与「扣 20–28% 选择偏差」不自洽。**它是功效停机线，不是「超过 4bp 必亏」的经济断言** |
| 4 | ΔADV | **保留为次级机制终点**，不再声称功效已解决 | 删量级门没有增加样本信息。真值 0.0026 时功效 FT 51.5% / ZS 45.5%（含折数与符号门约 49%），仍不过功效关 |
| 5 | 全池 IC | **维持为带 CI 的区间估计，不做二元 PASS** | 找过外部锚：同模态树基线 0.00628 < k=2 的 MDE80 **0.00834**，过不了功效关；Simonsohn "small telescopes" $d_{33\%}$=**0.00865** 是唯一非循环的先验锚但需另算非零零假设下的功效——**暂不立项**。k=2 后 top500 IC MDE80 升至 0.0106–0.0108，只有全池 IC 尚有希望 |
| 6 | 成本定义 | **按代码真实基准**：$C=\text{sign}\times(\text{fill}-\text{CRSP }DlyOpen)/DlyOpen+\text{佣金费用}$ | `src/crsp_pipeline/labels.py:118-128` 建仓价是 t+1 的原始 `DlyOpen`，money 管道全部沿用。**决策收盘→开盘的漂移不是成本**（回测也吃了它），计入即重复相加。「官方开盘价→实际成交价的偏离」对纯 MOO **恒等于 0**，只作部分成交 / 无开盘竞价的异常处理项 |

**由 2 直接推出的年代分工**（措辞已按你的收窄）：
- 开发折 BE 对现代实测 C：免费 go/no-go（开发折已消耗，再用不额外付代价，且方向偏乐观）；
- 折 05–35：报毛收益、BE 及区间，作为**部署反事实 / 跨年代压力测试**——"把今天的交易条件施加到历史毛收益上还有没有余量"，**不是**历史实际净收益，**不能**单独证明今天可投；
- 最终现代经济结论：2024-07 起封存窗 + 前向真钱累积。

### 11.3 K13 机械订单审计（`scripts/k13_order_audit.py`，`outputs/k13_order_audit.json`）

只读折 36–42 的冻结分数 + 滞后 ADV20 + 信号日收盘价。**不读 `labels.parquet`、不读 `DlyRet`、
不产生任何 P&L / IC / 折级读数。** 预注册范围写在脚本 docstring：可用于定小试的 AUM、期限、
每臂目标笔数、两臂能否共用成交；**不得**用于选持有期、不交易带或 universe。

冻结构造：NT=6 套袖、TOPN=500、EXIT_PCT=0.30、等权 1/k（k≈50），每日只有一个套袖再平衡。

| | FT | ZS |
|---|---|---|
| 一边成交笔数（832 交易日） | 47,346 | 34,872 |
| 每日笔数 均值 / p50 / p90 / max | **56.9** / 56 / 68 / 100 | **41.9** / 42 / 56 / 76 |
| 到 200 / 500 / 1000 笔所需交易日 | **4 / 9 / 18** | **5 / 12 / 24** |
| 每日一边换手（占账面） | 0.18969 | 0.13971 |
| 不同名字数 | 946 | 837 |
| 买卖比 | 0.500 | 0.500 |
| 被交易名字收盘价 p10 / p50 / p90 | \$22.1 / \$89.9 / \$349 | \$18.6 / \$77.0 / \$301 |
| 名字在 top500 池内的 ADV 分位 p10 / p50 / p90 | 0.082 / 0.486 / 0.888 | 0.058 / 0.486 / 0.898 |

**每笔名义额是精确均匀的**（p10=p50=p90）：等权 1/k 除以 NT，恒为 AUM 的 **1/300 = 0.333%**。

| AUM | 每笔 | 每笔股数 p10 / p50 | 占 ADV20 p50 / p90 / p99 | 占开盘竞价\* p50 / p90 | 每日成交额(FT) |
|---|---|---|---|---|---|
| \$1M | \$3,333 | 10 / 37 | 0.14 / 0.29 / 0.39 bp | 0.07% / 0.14% | \$190k |
| \$3M | \$10,000 | 29 / 111 | 0.43 / 0.86 / 1.18 bp | 0.21% / 0.43% | \$569k |
| **\$10M** | **\$33,333** | **95 / 371** | **1.43 / 2.85 / 3.92 bp** | **0.71% / 1.43%** | **\$1.90M** |
| \$30M | \$100,000 | 286 / 1112 | 4.28 / 8.56 / 11.75 bp | 2.14% / 4.28% | \$5.69M |

\* 假设开盘竞价量 = ADV 的 2%（美股开盘竞价通常 1–3%，远低于收盘竞价）。**这是假设不是测量**，
CRSP `DlyPrcVol` 没有竞价拆分。**全部 AUM 档下都没有任何一笔超过 ADV 的 1%。**

**两臂重合极小**：同日同名同方向只有 2,836 笔 = FT 的 **6.0%** / ZS 的 **8.1%**；
并集 79,382 vs 分别相加 82,218，**共用只省 3.4%**。
→ **「两臂都测」实际就是 2× 的订单量，没有共用成交的捷径**（codex 的坚持得到确认）。
另有 **2,270 笔同日同名反向碰撞**（约 2.7 笔/日），共用账户下必须分子账户或显式排序。

### 11.4 由审计推出的小试设计参数（待写进协议）

1. **期限按交易日定，不按笔数定，且必须设最短观察期。** 笔数是免费的（500 笔 9–12 日就到）；
   按日聚类后**有效样本量 = 交易日数**。预注册规则（09-02 定稿）：
   **最少 30 个共同交易日**（5 个日级观测估不出日间方差/HAC，不许提前停）；
   此后每 5 日只算两臂日均成本的 NW(5) SE，**首次 `max(SE_FT,SE_ZS) ≤ 1bp` 时两臂共同停止，
   上限 60 日**；运行期间**不展示点估计**；最终 block bootstrap 必须重演检查与停止规则。
   两臂共同停止——一臂先停会改变另一臂的碰撞环境。
   （$SE(BE_{FT})=4.88$bp 是主导项；$SE(C)=1$bp 时 $SE(BE-C)=4.98$bp，$SE(C)=2$bp 时 5.27bp。
   **不要为成本精度延长小试**。）
2. **AUM 是阻塞字段，不是可选参数**（09-02 修订，推翻我原来的 \$10M 建议）。
   原推理「选 \$10M 好感受竞价印价」是**错的**：`fill − DlyOpen` 在原理上
   **看不见自己对开盘印价的冲击**——MOO 单共同决定那个印价，你推高它、基准跟着一起动。
   放大 AUM 不揭示冲击，只让基准被自己污染。
   **因此 AUM 只能由真实的目标部署规模决定**：是 \$10M/臂就写 \$10M，
   只是拿来当测量工具就必须保持阻塞、小试不启动。
   另：\$10M/臂的日名义成交 FT ≈ \$1.90M / ZS ≈ \$1.40M，**远超 Alpaca 的
   \$200k/日非零售审查提示线**，必须先拿到实际费率答复。
   自有冲击只能由容量分析或一次显式变更规模的设计承担，本小试不负责。
3. **C 是 AUM 特定的**（裁定 3 已要求绑定）。因为基准就是竞价印价本身，
   低参与率下 $C\approx$ 佣金 + CRSP `DlyOpen` 与主上市所官方开盘价的口径差 + 零股/部分成交效应；
   **冲击段只有在竞价参与率有意义时才出现**。测出的 C 不得外推到更大规模。
4. **券商已定为 Alpaca；费率表已查实，见 §11.4a。** 结论：全包费用占 C_stop 的
   **15–25%**，留给 (fill−DlyOpen) 的余量 **约 3.0–3.4bp**。可行但不宽裕。
   （被交易名字的价格尾部很贵——p99 那 1% 的成交单佣金就超过 4bp；这只是报告事实，
   **不得**据此在已消耗折上加价格下限，那是新的开发搜索。）
5. **口径**：主指标 = fill vs CRSP `DlyOpen`（按方向定号）+ 佣金费用；
   决策收盘→开盘漂移作诊断单独报、不进 C；仅在订单到达时存在有效 NBBO 时附报 Rule 605 口径有效价差；
   按交易日聚类 / bootstrap，**不得把上千笔订单当作上千个独立样本**；
   避免把有效价差、延迟、冲击重复相加。

### 11.4a 券商：Alpaca（已定，此前未写进任何文档）

**⚠ 最重要的一条：Alpaca 的「零佣金」对本策略不成立。**
`time_in_force=opg`（MOO / LOO，只在开盘竞价成交）**只对 Elite Smart Router 用户开放**，
而 Elite 是按股收费的。冻结口径是「t 日收盘算分 → t+1 开盘成交」，
**要匹配这个口径就必须上 Elite，佣金无法回避**；退回普通市价单在 9:30:00 打则拿不到官方开盘价，
与 `DlyOpen` 基准的偏差更大且方差更高。

| 项 | 数值 | 适用 |
|---|---|---|
| 标准账户股票佣金 | \$0（但**没有 OPG**） | 不适用于本策略 |
| Elite **All-In** | **\$0.0040 / 股**（含各类费用与回扣，**不含**监管费） | 确定 |
| Elite **Cost-Plus** | **\$0.0025 / 股**或更低（按量分档）+ 透传费用/回扣 | 竞价场费/回扣未知 |
| Elite 资格线 | 入金 **\$30,000** | \$10M 远超 |
| 入金 \$100,000 起 | 更低融资利率 + **免费行情订阅** + 专属支持 | 覆盖 Rule 605 侧报所需的 NBBO 数据 |
| SEC Section 31 | \$0.0000206 × 成交额，**仅卖出** = **0.206bp** | 透传 |
| FINRA TAF | \$0.000195 / 股，**仅卖出**，单笔上限 \$9.79（50,205 股封顶，本策略永不触及） | 透传 |
| FINRA CAT | \$0.000003 / 股，买卖双边 | 透传 |

**按 K13 的真实成交分布（等名义额 ⇒ 低价名字股数多，而本策略已知偏向低价端，
`mean(1/px)` = 0.01943 (FT) / 0.02231 (ZS)，是 `1/mean(px)` 的 8.7× / 4.7×）：**

| 方案 | 佣金均值 | 佣金 p90 / p99 | +监管费 | **全包** | 占 C_stop | 剩给 (fill−DlyOpen) |
|---|---|---|---|---|---|---|
| FT · All-In | 0.777bp | 1.81 / **5.09**bp | 0.122bp | **0.900bp** | 22% | **3.10bp** |
| FT · Cost-Plus | 0.486bp | 1.13 / 3.18bp | 0.122bp | **0.608bp** | 15% | **3.39bp** |
| ZS · All-In | 0.892bp | 2.15 / **5.45**bp | 0.125bp | **1.018bp** | 25% | **2.98bp** |
| ZS · Cost-Plus | 0.558bp | 1.34 / 3.41bp | 0.125bp | **0.683bp** | 17% | **3.32bp** |

监管费在 bp 上**与 AUM 无关**（费率全部按成交额或股数比例，TAF 上限从不触发）：
卖出 0.245bp、买入 0.0006bp、双边均值 0.122bp。年费在 \$10M 上 FT ≈ \$5,849 / ZS ≈ \$4,416。

**操作约束（必须进协议）**：
- OPG 提交窗口：**7:00pm ET 之后排队到次日开盘**；**9:28am ET 之后、7:00pm 之前提交会被拒**。
  Nasdaq 市价单进 cross 的截止就是 **9:28**——9:28:01–9:29:59 之间进的单**拿不到 NOOP**。
  本策略在 t 日收盘后算分，时间极充裕；协议应规定**当晚 7pm 后至次日 9:00am 之间提交**，留足缓冲。
- Alpaca **不是直连交易所**：「路由给做市商，由其代为送进主上市所开盘竞价」。
  这一层中介是 (fill−`DlyOpen`) 偏差的可能来源之一，**正是小试要测的东西**。
- **两个方案二选一要在第一笔成交前定死**：All-In 是确定数，Cost-Plus 期望更低但竞价场费/回扣未知。
  建议 **All-In**——C_stop 是功效停机线，用确定的费率才能干净地判定它。

**尚未解决的一个口径问题**：CRSP `DlyOpen` 是否等于主上市所官方开盘价（NOOP / NYSE 官方开盘）？
CRSP 的 open 来自合并行情，对某些名字首笔合并印价可能不是竞价印价。
**这个差直接进 C，且无法从现有数据核**——小试会把它一并测出来，但报告时必须与竞价冲击分开归因。

### 11.4b 两臂不能同时下单：Alpaca 反向单 403（K14，`scripts/k14_collision_and_sizing.py`）

**约束**（用户提供，已在 Alpaca 官方文档核实）：**市价/止损单总是被同标的反向的
市价/限价/止损/止损限价单挡下，返回 HTTP 403**；限价单只在「买限价 ≥ 卖限价」时挡。
**paper 账户同样执行**。例外只有 bracket / OCO / trailing stop，与本策略无关。
LOO 也逃不掉——要保证竞价成交就得给激进限价，两边必然交叉。

**官方文档只写「a customer's two orders」，没有任何跨账户 / 跨主体的措辞
——范围未定，必须由 Alpaca support 书面确认。** 这是当前设计的分叉点。

**K14 量化：这不是边缘情况。** 2,270 笔反向碰撞落在 **729 / 832 = 87.6% 的交易日**上，
均值 **2.7 笔/日**、p90 **5**、max **15**。必须预注册规则，不能临场处理。

| 方案 | 日历倍数 | 损失 | 问题 |
|---|---|---|---|
| **双账户并行** | 1× | 0 | **取决于 Alpaca 的跨账户政策——未确认** |
| **逐碰撞哈希让位**（已采纳） | 1× | **FT 2.39% / ZS 3.26%** | 对每个 `(trade_date, PERMNO)` 取固定 seed 的 SHA-256 最低位定谁获得该笔；落败臂当日站开，单边无反向单，403 不触发。**优于交易日奇偶**：总量同样对称，但日内大致均分——单日碰撞全归一臂的天数 152/588 vs 奇偶的 588/588，日间构成标准差 0.289 vs 0.500。**禁用 Python 内置 `hash()`**（进程间随机化） |
| 丢弃碰撞（双方都不交易） | 1× | FT 4.79% / ZS 6.51% | 被交替让位严格支配，无理由采用 |
| 净额合并 | 1× | 每臂归因全失 | 只抵消 **5.5%** 名义额、订单只省 9%（89.95/日 vs 98.8）。**买的太少、代价太大，否决** |
| 分期轮跑 | **2×** | 0 | 两臂成本落在不同日历窗，跨臂比较有机制混淆（但 B 层本就不做跨臂统计判定，可容忍） |

**丢弃是否有偏**（碰撞 vs 非碰撞）：

| | n | 价格 p50 | ADV 分位 p50 / p10 | mean(1/px) |
|---|---|---|---|---|
| FT 碰撞 | 2,270 | \$72.9 | 0.524 / 0.138 | 0.02302 |
| FT 非碰撞 | 45,076 | \$90.7 | 0.484 / 0.080 | 0.01925 |
| ZS 碰撞 | 2,270 | \$72.9 | 0.524 / 0.138 | 0.02302 |
| ZS 非碰撞 | 32,602 | \$77.3 | 0.482 / 0.054 | 0.02226 |

碰撞名字**更便宜但不更不流动**。丢弃对佣金估计的偏差只有 0.9%（0.01943→0.01925），
可忽略；对执行偏差段的影响未知但样本只占 <7%（交替让位下 <3.3%）。
**代价是可披露的、不是隐藏的。** 但要记住：让掉的正是两模型**分歧**的名字，
而两臂差异恰恰长在分歧处——所以让位规则必须**在两臂间对称**（奇偶交替即可），
不能固定让同一臂。

**⚠ 附带的操作后果：单账户下必须保留两个影子账本。** 真实持仓是**执行**的真相，
但 FT / ZS 必须各有独立的冻结目标与虚拟持仓账本，否则发生让位后次日无从判断
哪一臂本来该交易什么。每日流程：
`两臂影子目标 → 理想订单 → 碰撞哈希 → 合成可执行订单 → 成交后按规则回分至影子账本 → 与真实总持仓逐股核对`（不平即当日停机）。
同向同名订单（2,836 笔）预注册为**合并提交、同价按名义额比例分配**
（Alpaca 对拆单型非零售流有关注）。
小试的估计量是「fill vs `DlyOpen` 的分布」**不是 P&L**，账面漂移不影响其效力。

### 11.4c 期限定档：**先验定不出来，必须序贯**（K14 第 3 部分）

模型 `SE = σ·sqrt((1+(n−1)ρ)/(D·n))`，n = 每日笔数（FT 56.9 / ZS 41.9），
ρ = 同日各笔之间的相关。**所需交易日 D 几乎完全由 ρ 决定，与笔数无关**：

| σ_fill \ ρ | 0.0 | 0.2 | 0.5 |
|---|---|---|---|
| 3bp | 1 天 | 2 天 | 5 天 |
| 5bp | 1 天 | 6 天 | 13 天 |
| 10bp | 2 天 | 22 天 | **51 天** |
| 20bp | 8 天 | 86 天 | **204 天** |

（FT，目标 SE ≤ 1bp；ZS 几乎相同。SE ≤ 0.5bp 在 σ=20bp 下 250 天内无解。）

开盘竞价的偏差本质上是共同因子驱动的（整个竞价一起重定价），**ρ 大概率偏高（0.3–0.6）**；
而 σ 若如预期很小（MOO 基本就成交在竞价印价上，σ 可能只有 1–5bp），则 D 在 **5–25 天**。
**但这两个数都不可先验确定**——这正是必须用序贯规则的原因，不是偷懒。

**预注册**：跑到**按日聚类的 SE(C) ≤ 1bp 或 60 个交易日**先到者为止；
只监控 SE（讨厌参数），**不监控点估计**，避免可选停止污染估计量。
60 天这个上限现在有依据：它正好覆盖 σ=10bp / ρ=0.5 这一档（51 天）。
分期轮跑则是 2×，最坏 120 个交易日（约 24 周）——**在序贯规则下大概率远早于此终止**。

### 11.5 执行顺序（当前）

1. ~~裁定 k、E 端定性、C_stop 归属、IC SESOI、成本定义~~ ✅ 见 §11.2；
2. ~~机械订单审计~~ ✅ 见 §11.3；
3. **写 FT / ZS 两臂 MOO 成本小试协议**（含 §11.4 五项参数 + §11.4a 费率 + §11.4b 碰撞方案
   + §11.4c 序贯定档）→ **当前在这一步**；**阻塞项：需 Alpaca support 书面确认
   反向单 403 是否跨账户**——是则双账户并行，否则退回「丢弃碰撞」或「分期轮跑」；
4. ledger 的 k=2 minimax-regret 事后方法修订条目 + C_stop 预注册，与协议一并定稿登记
   （目前只有草稿，**尚未写入 append-only 正文**）；
5. 跑小试；小试期间**不并行按收益挑持有期 / 不交易带**（会形成新的开发搜索）；
6. 用开发折 BE + 现代封存数据 + 实测 C 做 go/no-go；
7. 若设计变化，重测与新订单生成规则对应的成本；
8. 冻结后重算 k=2 的全部联合功效，再请求授权开启折 05–35。

### 11.6 本节新增

| 东西 | 位置 |
|---|---|
| 机械订单审计脚本 | `scripts/k13_order_audit.py` |
| 审计结果 | `outputs/k13_order_audit.json` |
| 逐笔订单流水（FT / ZS） | `outputs/k13_orders_FT.parquet`、`outputs/k13_orders_ZS.parquet` |
| 碰撞与定档审计 | `scripts/k14_collision_and_sizing.py`、`outputs/k14_collision_and_sizing.json` |
| 市场侧结构序列 | `scripts/k15_market_structure_eras.py`、`outputs/k15_market_structure_eras.json`、`outputs/k15_market_structure_daily.parquet` |
| **成本小试协议草稿** | `experiments/cost_pilot_protocol_v1_draft.md`（状态：`draft — awaiting Alpaca cross-account and non-retail fee confirmation; not authorized for live execution`） |

**待修的小瑕疵**：`outputs/k11_zs_ft_candidate_audit.json` 的 `decision` 字段是 GBK 写出的，
UTF-8 读会乱码（违反 `CLAUDE.md` §七）。权威产物应当可读。

**仍然未提交。** 提交时用精确路径 `git add`，不要 `git add -A`（多会话并行）。

### 11.7 K15：两条市场侧结构序列（2000–2026 全样本，纯 CPU，不消耗任何折）

`scripts/k15_market_structure_eras.py` → `outputs/k15_market_structure_eras.json`、
`outputs/k15_market_structure_daily.parquet`。只读 CRSP 的
`PERMNO / DlyCalDt / DlyRet / DlyPrcVol`，**不读任何模型分数**。分块读取（2 年一块），
未违反 `CLAUDE.md` §七 的整表禁令。

#### A. σ_cs：**§4 的「−0.396pp/年、相对 −7.2%/年」是窗口伪迹，已推翻**

同一个量在不同窗口（top500 池，6 日前瞻收益的截面标准差，stationary bootstrap blk=21）：

| 窗口 | 斜率 pp/年 | 95% CI | 相对 | 判 |
|---|---|---|---|---|
| **全样本 2000–2026** | **−0.061** | [−0.113, −0.011] | **−1.10%/年** | 不含 0 |
| 疫情前 2000–2019 | −0.222 | [−0.285, −0.160] | −4.19%/年 | 不含 0 |
| 当初用的 2020H2–2023 | −0.890 | — | −14.04%/年 | 窗口起点即疫情尖峰 |
| 疫情后 2022–2026 | +0.125 | [−0.142, +0.413] | +2.11%/年 | **含 0** |
| 去疫情 | −0.091 | [−0.143, −0.039] | −1.67%/年 | 不含 0 |

**下降是真的，但只有 −1.1%~−1.7%/年，不是 −7.2%/年。** 原估计在测疫情尖峰的衰减。

水平（%）：

| 窗口 | 均值 | 95% CI |
|---|---|---|
| 2000–2019 | 5.314 | [4.942, 5.732] |
| **确认窗 2005–2020** | **4.763** | [4.487, 5.096] |
| **开发窗 2020H2–2023** | **6.339** | [5.847, 6.962] |
| 2024+ | 6.138 | [5.724, 6.546] |

**推论 1（对现代部署）**：开发窗 6.339% 与今天 6.138% 的区间大幅重叠，
**开发折的毛可以直接向今天传导，不需要离散度调整**（比值 0.968）。
诚实前瞻毛 = 10.48 × 0.75 × 0.968 = **7.61%/年**，与此前假设的 7.5–8.4% 一致——
**C_stop = 4bp 不需要改，但它现在有数据支撑，不再是假设。**
（此前我算过的「σ_cs 再衰减 25% → 毛掉到 5.83%」情景**已被否掉**。）
2024+ 比 2000–2019 高 15.5%，但两区间在边缘处刚好接触，**只能说边缘显著，不得强调**。

**推论 2（对确认集，更重要）**：确认窗比开发窗低 **24.9%，且两区间不重叠**。
按桥公式 `毛 ≈ 3.51 × IC × σ_cs`，**即使 IC 完美传导**，折 05–35 的预期毛也只有

> 10.48% × 0.75（选择偏差） × 0.751（离散度） = **5.91%/年**

对照 k=2 的 MDE80 = 7.19% → **毛终点在预期效应处功效仅约 61%**。
而 **IC 终点完全不受影响**（σ_cs 在毛里、不在 IC 里）：预期 IC = 0.0207 × 0.75 = 0.0155，
MDE80 = 0.00834 → **功效 >99.9%**。

**这给了「全池 IC 作科学泛化主终点」一个原理性理由**，而不只是「IC 比较方便」：
确认年代与开发年代在一个**机械进入毛、不进入 IC** 的因子上差了 25%，而该因子现已被测出。
它同时让确认结果**可归因**——观测毛显著低于 5.91% 则 IC 没传导；落在 5.91% 附近则
IC 传导了、差额被离散度吃掉。**这个分解在 K15 之前做不到**，并独立佐证了
§11.2 第 2 条（历史 E 端降为估计交付）。

#### B. mom_skip 流动性迁移：**不可判读，且本项存在流程违规**

假设：若底层动量异象的流动性画像在同期同向翻转，A.1 的远端成因即闭合。

| 量 | 点估计 | 95% CI | 判 |
|---|---|---|---|
| （流动 − 不流动）趋势 | −0.156 bp/日/年 | [−0.585, +0.275] | **含 0** |
| 全样本均值 | +1.784 bp/日 | [−0.935, +4.541] | 含 0 |
| 2003–04 | +1.882 bp/日 | [−5.044, +8.721] | 含 0 |
| 2020H2–2023 | −6.049 bp/日 | [−14.297, +2.440] | 含 0 |

**全部含 0。MDE80 约 0.836 bp/日/年**（20 年累计 16.7 bp/日的摆幅），
远大于任何可信效应量——**这个检验从设计上就分辨不了它要问的东西**，与 K7a 同型。

**⚠ 自记违规**：本项**运行前没有写下判据、SESOI、MDE 或目标功效**，
违反 `CLAUDE.md` §二。脚本 docstring 只陈述了假设，未给量级门槛。
因此 **B 只能记作探索性诊断，不得据其得出任何否定结论**——
特别是**不得**写成「迁移假设被推翻」或「迁移是模型自身性质」。
点估计方向与假设相反，仅此而已。A.1 的远端成因**仍未解释**。

**可行的修复**：改用 **IC 型估计量**（各流动性档内 mom_skip 与前瞻收益的秩相关），
而非十分位价差。本项目反复实测 IC 的精度远高于分位价差；6,524 天下 SE 量级约 0.002，
若档间差是 0.005–0.01 量级则 t = 2.4–4.8，**可能真有功效**。
**重做前必须先写判据。**

### 11.8 封存计算队列已启动（2026-09-02，**计算授权 != 读取授权**）

**授权口径（用户原文，很窄）**：允许 FT 固定配置训练 + FT/ZS 生成分数；
**不允许**生成或读取验证窗 labels、不允许计算任何 IC/收益/分层/年度/组合指标、
不允许人工打开 scores。**此次计算不视为消耗确认集；任何结果读取仍须另行授权。**
用户已独立核对：早停用训练窗内部带 purge 的 `inner_split`，不读外层验证窗。

**实现**
- `scripts/evaluate_fold.py --sealed`：写完 `scores.parquet` **硬性 return**，
  物理上不进入 label/metrics/report/daily_ic 路径。**不写 `labels.parquet`**
  （labels 解封时才需要，提前生成只增加误触风险）。控制台只打一行完成信息。
- `src/crsp_pipeline/sealed.py`：哨兵 `SEALED`、`SEALED_MANIFEST.json`、
  `assert_readable()` 守卫（沿路径向上找哨兵，不越过仓库根）、`audit_dir()`。
  `write_seal()` 在写哨兵前先断言目录里没有任何禁止产物。
- 清单固定记录：快照 ID、训练/推理代码 SHA-256、lookback/pool/seed/sample_count/AMP/batch/predict、
  折边界、`scores.parquet` 哈希与行数。
- ledger 条目形如 `sealed-compute | tag=... rows=N（未生成 labels、未计算任何指标）`，
  **无任何 RankIC / t / 收益字段**。

**验收**：fold36 冒烟通过——目录只含 `SEALED` / `SEALED_MANIFEST.json` /
`scores.parquet` / `sealed_run.log`，`audit clean=true`；守卫实测拦截了 k13 的真实
loader（`SealedReadError`）。`tests/test_sealed_mode.py` 5 passed，全仓 **121 passed / 1 skipped**。

**队列**：`scripts/run_sealed_confirm_queue.ps1`（带 BOM），
折边界由 `scripts/emit_folds.py` **机械生成**（不手写窗口）。
**33 折 = 05–35（31）+ 44–45（2）**（fold43 已按用户 2026-09-02 裁定移除：其验证窗 2024-01-03..2024-07-02 落在 Kronos 语料窗内，不是干净证据），每折 = FT 训练 → FT 封存打分 → ZS 封存打分。
严格单 GPU 串行、PID 互斥锁、失败重试 4 次（原命令重跑即续训）、按产物幂等跳过。
用 `Start-Process` 重定向而非 `*>`——PS 5.1 会把原生 exe 的 stderr 包成
NativeCommandError，配合 `ErrorActionPreference=Stop` 会在第一条 torch warning 上炸掉队列。
训练 inner loss 全部进 `outputs/sealed_confirm/_logs/`，控制台只显示折号/阶段/时刻/成败。
**实测节奏（fold05）**：训练 81 分钟（16:14→17:35，tokenizer 30 轮上限跑满、predictor 第 29 轮早停，best 24）、
FT 封存打分 17 分钟、ZS 封存打分约 17 分钟 → **约 2 小时/折**。
33 折 ≈ **66 小时 ≈ 2.75 天**（原估「1–2 周」偏保守，已更正）。
**队列是幂等的：中断后原样重跑即从断点继续。**

**运行中的一课**：harness 的后台包装进程会随会话中断而死（exit 127），
但 `Start-Process` 拉起的 PowerShell 驱动与 python 子进程**不会**随之消亡。
现由 `scripts/resume_sealed_queue_after_pid.ps1 -TrainingPid <pid>` 驱动
（该脚本非本会话所写，创建于 16:27；它 `Wait-Process` 等训练自然结束后再调用队列脚本——
做法正确，正是 CLAUDE.md §8「等它跑完，别 kill」的实现）。
**因为它在 17:35 才重新调用队列脚本，而 fold43 的移除在 16:27:58 已写入文件，
所以当前运行实例读到的就是 33 折的新表——fold43 已经排除，无需再干预。**
另有 `scripts/restart_sealed_queue.ps1`（本会话所写）作为通用的安全重启工具：
等待所有 `kronos_ft.train` **或** `evaluate_fold` 进程退出 → GPU 静置 30s → 重启队列。

**⚠ 多会话并行**：`resume_sealed_queue_after_pid.ps1` 表明当时可能有另一会话在同一仓库操作。
按 `CLAUDE.md` §七，提交时务必用精确路径 `git add`。

**冻结打分口径**（已与折 36/42 及 zeroshot 的 `metrics.json` 逐字段核对）：
`amp=bf16, batch_size=128, sample_count=5, lookback=90, predict=6`；ZS 的
`model_dir=outputs/zeroshot_base`。

#### ⚠ 连带发现：**干净封存窗只有 1 年，不是 18 个月**

机械生成给出**恰好 45 折**（`panel_raw` 与 `market_index` 均止于 **2025-12-31**，
这是 CRSP 的正常发布滞后，非快照缺陷）：

| 折 | 验证窗 | 判 |
|---|---|---|
| ~~fold43~~ | 2024-01-03..2024-07-02 | **落在 Kronos 语料窗内**（截至 2024-06）——**已移除，不计算** |
| **fold44** | 2024-07-03..2025-01-02 | **干净** |
| **fold45** | 2025-01-03..2025-07-02 | **干净** |
| fold46 | 需覆盖到 2026-01-02 | 机械规则直接丢弃：超出数据末日 |

**干净封存证据 = fold44 + fold45 = 整整 1 年**，而非 §9 假设的约 18 个月；
夏普 0.65 下**预期 t（非中心参数）由约 0.88 降到约 0.65**——这是期望值不是观测 t 的上限，观测值可以偶然更大；实质是 **fold44–45 功效很低，只适合作洁净性复核与效应估计，不承担最终二元裁决**。2025H2 数据在面板里但做不出对应折，
**须等覆盖到约 2026-01-02 的新 CRSP 快照，不得手写窗口。**

#### 守卫覆盖范围（诚实记录）

`assert_readable` 已接入 4 个在用读取点：`k9_cost_alpha_frontier`（k11/k13/k14 共用）、
`k11`、`k13`、`bootstrap_errorbars`。仓库另有约 18 个脚本读 `scores.parquet`
（k6/k6b/k6c/k7a/k7b/k8/k10/compare_*/analysis_*/optimal_holding/holdings_tilt/
xsec_context_probe/edge_cost_model）**尚未接**。风险评估：它们的折范围与目录名全部硬编码
（`range(36,43)`、`fold01..04`），走不到封存目录。

**⚠ 一处必须更正的夸大（用户 2026-09-02 指出）**：此前写过「封存目录没有 labels，
IC 在物理上算不出来」——**这是错的**。有 `scores.parquet` 和原始行情面板，
随时可以重新生成标签并算出 IC。**不写 labels 只能防止误操作，不构成隔离。**
**封存本质上是流程纪律，不是密码学隔离。**

因此按用户裁定，**不批量改那 18 个脚本**（路径硬编码、不会误读，批量改动反而增加工程风险），
改用一条**可执行的纪律测试**：
`test_no_ordinary_script_references_sealed_outputs` —— 扫描 `scripts/*.py`、`src/**/*.py`、
`tests/*.py`，任何文件（白名单外）出现 `sealed_confirm` 或 `eval_sealed` 即失败；
配套 `test_unseal_switch_is_not_used_casually` 禁止随手使用 `unseal` 解封开关。
**新写任何读取封存产物的脚本，必须先取得用户的读取授权。**

**另修一个测试 bug**：早期的目录审计参数化扫的是 `outputs/sealed_confirm/` 的子目录，
但**封存分数实际落在 `outputs/*/eval_sealed_*`**（`sealed_confirm/` 只放 `_logs` 与队列锁），
所以那 10 个参数化用例是**空跑通过**的。已改为按真实位置 glob，并拆成两条：
「任何封存目录（含正在写入的）都不得出现禁止产物」+「已完成封存的目录必须完整且只含允许文件」。
`tests/test_sealed_mode.py` 现为 **17 passed**（原 5）。

---

## 12. 2026-09-03：思路整理与三份调研（**未拍板，未改代码/协议**）

**背景**：GPT 建议锁训练计划、认为架构再动刀改变不了什么；用户不甘心。本节只做思路整理。

**结论一句话**：锁 Kronos 是对的（模型内比较在本数据量上不可回答），但「就这样了」是错的——
夏普 0.72、换手 48×、BE 22bp 是**一个 alpha 分量**不是产品；高 alpha 靠**广度**（多个低相关信号），
不靠深度。**推荐：Kronos 一字不改，小试等待期并行建信号#2，合成假设在解封前预注册进协议。**

| 文件 | 内容 |
|---|---|
| `docs/思路整理_2026-09-03.md` | 三条路（锁定等待 / 锁 Kronos 建广度 / 重开架构）+ 要拍板的 5 件事 + 现在就该做的事 |
| `docs/调研_信号2候选_2026-09-03.md` | 首推隔夜/日内分解（LPS 2019、BBM 2026 RFS），只需 CRSP open/close；不推荐特征 ML / 图像 CNN / 通用 TSFM |
| `docs/调研_合成与验证设计_2026-09-03.md` | 等权只损 0.03–3.3%；固定序贯检验不拆 α；**ΔIC 在 31 折上是 B 层**；稀释门槛 IC₂ ≥ 0.0085（6 日标签）；预注册 7 条 |
| `docs/调研_换手先验定参_2026-09-03.md` | **现行 30% 退出线 = 信度 0.75 解析反推的 28.3%**，可再归类为先验固定；单信号内换手已无法降；加慢信号可降 30–45%；小试用一套订单流 + 协变量成本模型 |

**本节记录的硬约束**：系统提交内存 43.1/45.2 GB（训练进程 16.7 GB），封存队列跑完前不得起重 CPU 任务。

### 12.1 深夜自主工作（用户睡前授权「跑实验、写缺失的架构」；**未跑任何真实数据**）

**新增四个包（全部合成数据测试，未导入 torch，未读 processed/ 与 outputs/）**，联合 `test_sealed_mode.py` 共 **224 passed**：

| 包 / 文件 | 内容 | 测试 |
|---|---|---|
| `src/signals/` | `SignalSpec`+`Signal` 接口；`overnight_intraday.py`（信号#2，冻结 spec_hash `896b0de2…20e2c`）；`kronos_adapter.py`（折号白名单 {1–4, 36–42}，读前 `assert_readable`） | 43 |
| `src/portfolio/` | `construction.frozen_long_only_returns`（从 `compare_arms_money.py` 62–120 行逐位抽出，5 种子 `array_equal`）；`combine.rank_sum_equal_weight / slow_filter` | 27 |
| `src/backtest/money.py` | 薄包装，统计量直通 `crsp_pipeline.signal_eval` | 13 |
| `src/execution/` | `fees`（Alpaca Elite 费率）、`collision`（SHA-256 让位，按协议 §4.1）、`shadow_ledger`、`orders`、`alpaca_client`（dry-run 默认，无密钥）、`daily_flow` | 44 |
| `scripts/signal2_devfold_readout.py` | 预注册读数执行器，`--dry-run` 4.6s 走通；**真实模式未运行** | — |
| `scripts/check_construction_parity.py` | 真实折 36–42 对拍新旧构造，**只写未跑**（需 ≥6 GB 空闲提交） | — |
| `experiments/signal2_prereg_v1.md` | 信号#2 预注册 v1（8 节 + 附录 A 上游矛盾） | — |
| `docs/系统架构_2026-09-03.md` | 分层现状表、四包接口、脚本三分类、每日流程 | — |
| `docs/调研_期权信号_2026-09-03.md` | **期权腿降级**：预测力与借券费同源（MPP 2025），top500 长腿掉 ≥2/3；「大笔卖 put 看多」无证据（Ge-Lin-Pearson 2016：有信息的是开仓买 call） | — |
| `docs/调研_流派与前车之鉴_2026-09-03.md` | 个人量化流派全景、Quantopian/Numerai/A 股多因子前车之鉴、定位；三条便宜诊断见 §12.4 | — |
| `docs/调研_信号源盘点_2026-09-03.md` | 价格/期权之外按信息源盘点。**免费可得的独立源：空头持仓（FINRA）、内部人 Form 4（EDGAR）、10-K 文本变化（EDGAR）**；WRDS 合规问题见 §12.5 | — |
| `docs/调研_新闻信号规模化_2026-09-03.md` | 「股票太多」是伪问题（全美股每天 600–900 条公司新闻，按条打分）；但 Lopez-Lira 2023 在 20bp 成本归零（本项目 BE 22bp）、信号活不到第 6 天且在小盘；**干净回测（ChronoBERT）与零样本固定读出不可兼得**。建议只做 1 人日的覆盖统计，暂不建信号 | — |

### 12.2 需要用户裁定（按重要性）

1. **信号#2 的处置规则**（`signal2_prereg_v1.md` 附录 A 第 1/3/4/5 条 + 顶部补注）。预注册 §4 写的是「按 IC 量级三档选合成规则」，与 `CLAUDE.md` §五（IC 不作决策依据）及 K8 实测（IC +28%、top-decile −16%）相抵；改用钱选又是开发折新搜索。**主会话倾向**：删 IC 量级门，只保留准入检查（符号 >0、≥4/7 折、ρ(FT)<0.30）；合成规则按 B 层先验定为等权秩相加，筛子作披露的未取方案；稀释与否交确认集 H1→H2 + 部署规则回答。**裁定前不得跑 `signal2_devfold_readout.py` 真实模式。**
2. **合成用哪一臂作 f₁**（附录 A 第 6 条）：k=2 下 H1/H2 是否各跑 FT、ZS 两遍，未规定。
3. **成本小试要不要双臂**：调研 C「成本对订单条数不敏感 → 一套订单流 + 协变量成本模型」；若采纳，K13/K14 的碰撞让位与双影子账本整套可不启用（代码已写好备用）。这与 k=2 不冲突（k=2 是确认集配置）。
4. **协议 v1 草稿的四处瑕疵（执行层实现时发现）**：① §0 估计量公式的费用项只挂在卖出侧，与 §11.2 裁定 6 双边口径矛盾（按 §0 字面会漏掉买入佣金 0.78bp，是 C 的大头）；② TAF 上限股数应为 50,206 而非 50,205（不舍入口径）；③ 名义额→整股的取整方式未冻结（低价名字残差占比更大，第一笔真钱前必须定死）；④ **§11.4b「已采纳：逐碰撞哈希让位」的数字（FT 让位 2.39% / ZS 3.26%、152/588、SD 0.289）不在 `k14_collision_and_sizing.json` 里**——该 JSON 只有 parallel/drop/net/staggered 四方案，哈希让位的唯一规范文本是协议 §4.1 代码块，数字出处待核。

### 12.3 队列结束后按顺序做（都不需要 GPU）

1. 用户裁定 12.2 第 1 条 → 改 `signal2_prereg_v1.md` → 登记 → 跑 `scripts/signal2_devfold_readout.py`（真实模式，逐折分块读）；
   1b. **封存队列已于 09-03 21:07 全部完成**（33 折，SEALED 哨兵齐全）；**NT=5 基准读数已运行并登记**（ledger 09-03 ablation-read）：FT 毛 12.68% / t 2.42 / BE 22.9bp / 净(8bp) 8.25% 夏普 0.86；ZS 毛 6.32% / BE 16.4bp。上线线 FT C≤11.2bp、ZS C≤6.3bp。**§2 的 NT=6 读数表保留为历史，现行冻结构造为 NT=5。**
2. ~~跑 `scripts/check_construction_parity.py`~~ ✅ 09-03 21:57 通过（lb90/lb200 均 array_equal，最大差 0）；
3. 读两份调研（流派、信号源），定第三条腿候选；
4. 把 §11.2 六项裁定、k=2 minimax-regret、退出线 30% 的信度解析再归类（28.3%）、信号#2 spec_hash 一并写入 ledger。

### 12.4 流派调研给出的三条便宜诊断（都不碰 Kronos、不消耗折，见 `docs/调研_流派与前车之鉴_2026-09-03.md`）

| # | 对照 | 本项目现状 | 该做的诊断 |
|---|---|---|---|
| 1 | Novy-Marx-Velikov 2016：**月换手 < 50% 的异象扣成本大多存活，高于者极少** | 年单边 48× ≈ **月换手 400%，是生存线的 8 倍**；BE 21.9bp 落在其测出的中换手执行成本区间 20–57bp 内 | 把这个对照写进 ledger 作小试判读背景：小试性质是「确认策略能不能活」而非「确认成本」 |
| 2 | Quantopian（Wiecki 2016，888 算法）：回测夏普对实盘 R²<0.025；**能预测的是组合形状**（对冲、集中度、尾部比） | 风格暴露已排除：K6b 8 因子张成保留 141%、JKP 13 主题保留 89%。**但 K9/K10 只报毛收益保留率，未报夏普**；持仓向 invprice/amihud/idiovol/hlrange 倾斜（相关 +0.29~+0.38）推高波动 | 在 K9 已有的 q 前沿上补报**净夏普与超额波动**（q=1/0.75/0.5/0.25/0）。若中性化砍波动多于砍收益，夏普上升——这是一条不碰模型的杠杆 |
| 3 | 个人的结构性优势是小资金能做小票；HXZ 2020：月截面离散度**微盘 19.1% / 小盘 11.9% / 大盘 8.9%**，而收益 ≈ IC × 离散度（CLAUDE.md §五） | 选 top500 有数据支持（K7b：现代信号长在流动端，top500 IC 0.0258 > 全池 0.0207），**但离散度那一半的代价从未定价** | 用 `k15_market_structure_eras.py` 换 TOPN=1500 报 σ_cs，与 top500 的 6.34% 并列，把取舍从默认变成有价签的决定 |

**更正**：该报告把超额波动反推为 14.6%（10.5%/0.72）是口径混用；§2 的 0.72 算在净超额序列上，超额波动为 **9.20%**。报告 §2.1 末条与 §5 涉及该数的推断按 9.2% 重读，方向不变、力度减弱。

### 12.5 信号源盘点的四条硬结论（`docs/调研_信号源盘点_2026-09-03.md`）

1. **⚠ 合规（比任何因子都重要，用户必须自行核实）**：WRDS 使用条款与各校政策写明**数据不得用于非学术或商业用途**。这直接封死 IBES / Compustat / CRSP 共同基金库作为交易信号源；**且本项目的 CRSP 快照本身就是经 WRDS 下载的**——用于个人真钱交易是否在授权范围内，须向授权方核实。本节只转述条款，不作法律判断。
2. **免费且合规的独立源只有三个**：空头持仓（FINRA 双月，Boehmer-Huszar-Jordan 2010「低空头持仓跑赢」，长多头直接可用，3–5 人日）；内部人机会型买入（SEC Form 4，Cohen-Malloy-Pomorski 2012 **市值加权 82bp/月**——本次唯一有「大盘可用」口径证据的非价格信号，5–8 人日）；10-K/10-Q 措辞变化（Cohen-Malloy-Nguyen 2020，余弦相似度零参数，样本止 2014、大盘与 2015 后 OOS 未核，4–8 人日）。**免费路线的独立信息源上限 = 价格 + 空头 + 内部人 + 文本 = 4 个。**
3. **全部非价格信号都是月频或更慢** → 对 6 日持有只能作**筛子**（可持有集），不能逐日秩相加（秩相加需 6 日标签 IC > 0.0085，倒推月频 IC > 0.016，无一达标）。这在实践上化解了调研 B 与 C 关于合成规则的分歧：**信号#2（隔夜/日内，逐日更新）仍可考虑秩相加；信号#3 起一律筛子。**
4. **「未来 6 日内出财报不买」方向错误，撤回。** Frazzini-Lamont 2007（公告月溢价 7–18%/年）、Savor-Wilson 2016（预定公告者年化异常收益 9.9%）：规避财报 = 主动放弃正溢价。唯一能推翻此结论的量（6 日窗口内一个财报日的方差贡献）未核；建议此层**不加任何规则**并登记为「已考察、决定不加」。
   另：FINRA 每日空头成交量文件只含场外、且做市商对冲使占比系统性上偏（FINRA 用户指南自述），**不作主力**；EDGAR 搜索日志 2017-07 后无数据、2020 后无 IP，变量已无法构造；散户订单流识别算法被 Barber 等 2024 部分证伪；Google Trends 不可复现。四条明确出局。

### 12.6 构造修订待登记清单（用户 09-03 已口头同意；**登记前不得执行任何读数**）

| # | 修订 | 依据（全部先验/解析，非收益搜索） | 状态 | 连带 |
|---|---|---|---|---|
| 1 | **持仓期 6 → 5 日（NT=5）** | `optimal_holding.py` 预写规则：T*=3–5 与现行 6 不符则改用 T* 并记为解析驱动；取 5 的非数据理由：恰好一个日历周、每个套袖固定星期几调仓、任何持仓恰跨一个周末（6 日周五进场跨两个） | 待写 ledger | 分数不重打（预测期仍 6 日）；重跑 K13 订单审计（每笔 1/250、日订单 +20%）；小试协议 §2 改 NT=5；K12 功效重算；新构造在 36–42 的基准读数**只能在登记之后**跑 |
| 2 | **退出线 30% 再归类为先验固定** | 信度 0.75 → 噪声 SD 0.5 → z_exit = 1.2816 − 0.7071 → 28.3%（`docs/调研_换手先验定参` §4） | 待写 ledger | 不改数，只改归类；披露「再归类发生在参数已用之后，依据是先验推导」 |
| 3 | **换仓改为成本触发**：`G(q_h) > 2c` 才换（剩余持有期内新旧名字的预期超额之差 > 一买一卖两笔成本；G 由实测衰减曲线 e(h) 与分位收益 R(q) 机械算出，c 为小试实测） | `docs/调研_换手先验定参` §6；参数全部来自已测原语 + 外生 c，不消耗折 | **小试测出 c 之后**才能定 | 与现行 buy/hold spread 同一函数族，q*=30% 是 c=c₀ 的特解；逐档报 q*(c) |
| 4 | **换手的主杠杆 = 加慢信号**（预期单边换手 −30–45%，毛 alpha 替换而非损失） | DeMiguel 等 2020 trading diversification；`docs/调研_换手先验定参` §3 | 依赖信号#2 准入 | — |

**明确不做**：手动放宽退出线（与 T*=3–5 反向）；按持仓已实现盈亏决定留不留（分数已含近 90 日价格路径，P&L 不是已验证信号，且 K6b 显示策略已站短期动量侧）；任何在 36–42 上比较构造变体收益的读数（B 层）。

### 12.6a K12 E 端功效按 NT=5 解析重算（09-03，ledger design-note）

| 臂 | 开发折天数 | 毛年化 | SE(7折,年化) | SE(31折,外推) | MDE80 | 预期确认毛 (×0.75×0.751) | 在预期效应处功效(双侧5%) | BE | SE(BE,31折) |
|---|---|---|---|---|---|---|---|---|---|
| FT | 839 | 12.68% | 5.23 | 2.43 | **6.80%** | 7.14% | 84% | 22.9bp | ±4.4bp |
| ZS | 839 | 6.32% | 7.43 | 3.45 | **9.65%** | 3.56% | 18% | 16.4bp | ±8.9bp |

**判读（已更正，见 ledger correction 条）**：FT 在预期效应处功效约 **84%**（刀口过线），ZS 约 18%。裁定 2 的功效理由对 FT 不再成立，只剩概念理由；**是否将 FT 的 E 端恢复为确认性检验，用户须在解封前裁定并写入 v4**。S 端不随 NT 变。K13 订单审计的 NT=5 机械换算：每笔名义额 AUM/250（原 1/300）、日订单约 +20%、名字的价格/ADV 分布不变——小试协议 v2 按此改写，K13 脚本不重跑。

### 12.6b E4 指标贴合度–功效（09-03 已跑，ledger diagnostic-result）

| 指标 | corr(M,U_fwd6) | 符号一致 | NW t |
|---|---|---|---|
| ic_full | 0.294 | 0.605 | 3.57 |
| ic_top500 | 0.309 | 0.598 | 3.41 |
| ic_top30 | 0.526 | 0.684 | 1.15 |
| decile_spread | 0.715 | 0.747 | 3.17 |
| prec50 | 0.559 | 0.661 | 2.05 |
| ic_x_sigma_expost | 0.293 | 0.598 | 3.37 |
| ic_x_sigma_trail | 0.323 | 0.590 | 3.20 |

**实盘监控主指标 = 十分位价差**（贴合 0.715、t 3.17）；全池 IC 次级诊断；前 30% 内部 IC 作退出线诊断。用途限制：不用于选模型/构造/合成。图：`outputs/e4_metric_alignment_power.svg`。

### 12.7 原始规则审视（`docs/审视_原始规则_2026-09-03.md`，用户 09-03 要求）

逐条核对预注册 v1 与登记簿前 130 行后的硬发现：
- **第二轮粗筛（补 seed 1、2）从未执行**（登记簿零条 seed=1/2 训练）；lb90 是按登记簿第 52 条「回落基准」定的，**不是**按预注册「平局取最小 lookback」——后者会选出点估计最差的 lb60。过程有瑕疵、结果正确，须补登记为修订。
- **预注册 §5.1「BE<25bp 不上线」从未引用也从未废止**，现 BE 21.9bp 按它不能上线；须登记为被 C_stop 框架取代。
- 8 折 seed 方差子集、第 21 折漂移复检、缓冲扫参三项**未执行且未废止**；seed 方差至今未估计。
- 「45% 才分得出」不是门槛是样本极限；没有一次筛选因「严」选错，有一次（lookback）差点因平局规则选错，一次（持仓期）因早期无规则选偏，一次（sample_count 5→20，+4.9%、4/5）因严放弃了可能的小改进。
- **五条修订建议**：登记 lookback 平局规则修订；登记 §5.1 三项废止/取代；CLAUDE.md 加「零参数且理论单调的改动可按理论采纳」例外（首个适用：实盘打分 sample_count=20）；B 层加 minimax-regret 平局条款并追认 §11.2 第 1 条；上线前补 §4.1 停机规则。
- **用户 09-03 裁定：五条全部采纳。** CLAUDE.md §二 已加两条（例外条款、平局条款）；六条登记簿条目 + §11.2 六项裁定原文已写成 `experiments/ledger_drafts_2026-09-03.md`，**待用户核后追加入 ledger.md 正文**；§4.1 停机规则列为上线前必做（调研清单第 4 项）。

### 12.8 研究计划书与论文实验候选（用户 09-03 要求）

`docs/研究计划书_2026-09-03.md`：题目、文献缺口、三个贡献、**预注册假设 H1–H5 + C**（写于确认集读取前）、
七项探索性实验（E1 采样噪声分解 / E2 路径离散度校准 / E3 学习曲线 / E4 指标贴合度–功效图 / E5 期限结构 /
E6 信度推退出线 / E7 跨市场）、效度威胁、时间线、发表渠道（现实档 ICAIF / JFDS / QF）、英文摘要草稿。
**H3（Kronos vs 扩展窗口树模型）须写入协议 v4**；E1–E6 都不消耗确认集，队列结束后按序跑。

**登记簿已补录（09-03，用户核定）**：审视六条 + §11.2 六项裁定原文 + **C_stop 角色修订**（4bp 降为披露；部署判据 = `C ≤ BE_dev×0.75 − 6bp`，FT 上线线 C ≤ 10.4bp / ZS ≤ 4.2bp；只看相对基准的净超额，不看绝对收益）。§11.2 裁定 3 的原文保留为历史记录，以 ledger 2026-09-03 修订条为准。

**未做**：未动 v3/v4 协议正文、未读任何封存产物、未推送。C 块：**小试改单臂 FT + 协变量成本模型**、**合成对两臂各算**已裁定登记；**信号#2 合成规则待专项调研**（opus agent 已派，产出 docs/调研_合成规则专项_2026-09-03.md）。

**用户 09-03 白天的指示**（已写入 `CLAUDE.md` §九）：agent 一律 opus；监控只报终态；助手须凭证据顶回用户直觉。**持仓期改 5 天**已论证（解析最优 3–5 + 日历周先验），待登记后执行；WRDS 合规走学校实盘基金（老师推荐的项目）。
